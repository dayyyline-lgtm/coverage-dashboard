# 대시보드 개보수 계획 (2026-08-08)

측정한 숫자만 적는다. 인상이나 추측은 넣지 않는다.

---

## 1. 지금 어떻게 돌아가고 있나

```
  트리거                      수집기 24개              산출물
  ─────────                  ──────────              ──────
  Cloudflare Worker  ─┐
  GitHub cron        ─┼──▶  fetch_*.py  ──┐
  이 PC 작업스케줄러    ─┘      refresh_live.py │
                              inject_amazon.py│
                                              ▼
                                   public/index.html  (903KB, 5,071줄)
                                              │        ├ 데이터 상수 33개 (551KB)
                                              │        ├ 화면 JS         (236KB)
                                              │        └ CSS             (32KB)
                                              ▼
                              Cloudflare Pages ──▶ 공개 사이트
                                              │
                              digest.py · watchdog.py · notify.py
                                    (같은 파일을 정규식으로 다시 읽음)
                                              ▼
                                        텔레그램 봇 3종
```

**핵심: 파일 하나가 데이터베이스이자 화면이자 API다.**

---

## 2. 측정된 문제

| | 숫자 |
|---|---|
| `index.html` 크기 | 125KB(7/21) → **903KB**(8/08) · 18일 만에 7.2배 |
| 그 안의 데이터 상수 | 33개, 551KB (파일의 61%) |
| 그 파일을 정규식으로 읽고 쓰는 스크립트 | **23개** (읽기 46곳 · 쓰기 12곳) |
| 그 파일에 쓰는 주체 | 워크플로 6개 + 이 PC 1개 = **7** |
| 사람 커밋 중 '고치는' 커밋 | 7월 12% → **8월 19%** |
| `index.html` 경로 하드코딩 | 23곳 |
| 파이썬 쪽 `asOf` 취급 | 52곳 |
| 워크플로 안의 시간 게이트 | 7곳 |

**고치는 커밋 비율이 오르고 있다.** 기능을 더할수록 고칠 일이 더 빨리 늘어난다는 뜻이다.

---

## 3. 문제가 어디서 나는가 — 유형 5가지

이번 주(8/04~8/08) 실제 사고를 원인별로 묶었다. 전부 실측으로 확인한 것이다.

### A. 파일 하나에 쓰는 주체가 7개 → 충돌·유실

- **8/08 아마존 데이터가 PC 에 갇힘.** 리베이스가 충돌한 채 멈춰 있었다.
  충돌 내용: 우리 `AMAZON`(08-08) vs 원격 `LIVE`·`APPRANK`·`TOURISM`·`SPOTIFY`(08-07).
  **서로 다른 상수인데 충돌했다** — 한 파일에서 줄이 붙어 있어 git 이 '인접 변경'으로 본다.
  어느 쪽을 통째로 골라도 하루치가 날아가서 자동으로는 못 푼다.
- 같은 일이 그날 저녁 내 커밋에서도 또 났다.

### B. 실패를 조용히 삼킴 → 초록불인데 고장

- **CGV 가 403 을 주는데 화면엔 '미편성'으로 나왔다.** 탐침이 예외를 삼키고
  `None` 을 돌려줬고, 호출부는 `None` 을 '편성 안 됨'으로 읽었다.
  로그엔 `미편성 — 전수 조사 생략` 만 15줄 찍히고 워크플로는 초록불.
  실제로는 **29시간** 배정 데이터가 멈춰 있었다.
- 거의 모든 워크플로 스텝이 `python X.py || echo "건너뜀"` 이었다.
  Actions 실패율은 0% 로 보이는데 예매율 수집은 timeout 으로 넘어가고 있었다.

### C. 기준값의 출처가 여러 개 → 판정이 서로 다름

- **화면이 오늘을 8/7 로 알고 있었다.** 날짜를 `LIVE.asOf` 에서 가져오는데,
  시세는 장이 열려야 갱신되므로 주말엔 며칠 전에 멈춘다.
  8/7 실적발표가 아직 '다가오는 일정'에 떠 있었다.
- 예매 전일비가 **다른 시각끼리** 비교하고 있었다. KOBIS 실시간 예매는
  남은 상영분이라 하루가 갈수록 줄어든다(05:38 145,190명 → 12:54 90,562명).
  전날 마지막 스냅샷과 빼면 증감이 아니라 시간차를 재는 꼴이다.

### D. 조건이 코드 곳곳에 흩어짐 → 분기 누락

- **스크린 게이트에 주말 분기가 없었다.** `if [ "$d" -le 5 ]` 안에만 슬롯이 있고
  `else` 가 없어 토·일엔 무조건 건너뛰었다. 개봉작은 주말이 흥행의 본판인데.
- 시세 백업 예약이 **21가지 도착 경우 중 0건**만 게이트를 통과했다.
  GitHub cron 이 3~12시간 밀리는데 게이트는 짝수 시만 받았다.

### E. 검증 없이 규칙을 추가 → 가정이 틀림

- **예상 관객이 +83% 빗나갔다.** `총좌석 × 기준일 판매율` 식이었는데,
  개봉일이 채운 30% 를 평일에 갖다 쓴 탓이다(8/07 은 실제로 15% 만 찼다).
- `[CI Skip]` 로 빌드를 아끼다 보니 **데이터는 들어왔는데 화면은 옛 값**인
  상태가 생겼다. 정작 재보니 전부 배포해도 한도의 79% 였다.
- 내가 스크린 전수 스윕 비용을 25분으로 잡았는데 실측은 45~49분이었다.

### F. (별도) 환경 가정

- `.bat` 에 UTF-8 한글 → cmd 가 cp949 로 읽어 줄이 깨지는데 **종료코드는 0**.
  조용히 아무 일도 안 하고 성공한 척했다.
- 파이썬 `print` 의 줄표(—) 한 글자에 아마존 수집 전체가 죽었다(같은 cp949 문제).
- `toISOString()` 은 UTC 라 한국 아침 9시 이전엔 어제가 나온다.

---

## 4. 왜 덧붙일수록 문제가 생겼나

기능 판단이 틀려서가 아니다. **더하는 방식**이 문제였다.

새 기능이 들어올 때마다 이 셋이 같이 늘었다.

1. `index.html` 에 상수 하나 추가 → 그 파일에 쓰는 주체가 하나 늘어남
2. 새 수집기에 `_get`/`_put` 정규식 한 벌 복사 → 같은 파일을 헤집는 코드가 하나 늘어남
3. 새 워크플로에 시간 게이트 하나 추가 → 서로 모르는 조건이 하나 늘어남

각각은 10분짜리다. 그런데 **결합이 곱으로 늘어난다.**
지금 상태에서 데이터 블록 하나를 더하면 건드릴 곳이 최소 4군데다
(상수 · 수집기 · 워크플로 게이트 · watchdog 한도).
그중 하나를 빠뜨리면 조용히 고장난다 — 위 B·D 유형이 정확히 그것이다.

`고치는 커밋 12% → 19%` 가 이 곡선이다.

솔직히 말하면 이번 주 사고 상당수는 내가 만든 것이다.
cp949 크래시, 비용 오추정, 로그 오독(실패율을 60~75% 로 잘못 셌다)은 전부 내 쪽이다.

---

## 5. 개보수 계획

원칙 셋. **진실의 출처는 하나 · 실패는 반드시 보이게 · 조건은 한 곳에.**

### 1단계 — 데이터와 화면을 가른다 (가장 큰 효과)

```
지금:  public/index.html  ← 데이터 33개 + 화면 코드가 한 덩어리
후:    public/data/*.json ← 수집기가 각자 자기 파일만 씀
       public/index.html  ← 화면만. 로드할 때 JSON 을 읽음
```

- **A(충돌)가 원리적으로 사라진다** — 수집기끼리 파일이 겹치지 않는다.
- `index.html` 이 903KB → 약 250KB.
- 덤: Ctrl+S 로 저장해도 껍데기만 간다(전에 얘기한 스크래핑 대응).
- 빌드 단계는 안 생긴다. 여전히 푸시하면 배포다.

**위험**: 지금 렌더링이 동기(`render();`)라 비동기 로딩으로 바꿔야 한다.
그래서 한 번에 안 옮긴다 — `JSON 이 있으면 그걸 쓰고 없으면 내장값` 이중 경로를
먼저 넣고, 수집기를 하나씩(아마존부터) 옮기고, 다 옮긴 뒤 내장 상수를 걷는다.
각 단계에서 화면이 안 깨지는 걸 브라우저로 확인하고 넘어간다.

### 2단계 — 수집기 공통 모듈

23개 스크립트에 복사된 `_get`/`_put` 정규식을 `blocks.py` 한 곳으로.
`load(name)` / `save(name, obj)` 둘이면 끝난다.
새 수집기를 만들 때 복사할 것이 없어진다.

### 3단계 — 조건을 한 곳에

- 시간 게이트 7곳 → `collector_health` 의 함수 하나로.
- 워크플로는 그 함수만 부른다. 주말 분기 누락 같은 일이 구조적으로 안 생긴다.
- 화면의 `TODAY` 는 이미 8/08 에 한 곳으로 모았다. 같은 방식.

### 4단계 — 배포 전 자동 점검

푸시 전에 도는 짧은 검사 하나.
- 데이터 블록이 전부 유효한 JSON 인가
- 각 블록의 `asOf` 가 기대 주기 안인가
- 화면 JS 에 오류가 없는가(헤드리스로 한 번 렌더)

지금은 이걸 매번 손으로 하고 있다. 오늘도 브라우저로 네 번 확인했다.

### 5단계 — 화면 코드 분할 (마지막)

`index.html` 의 화면 JS 236KB, 탭 14개, 렌더 함수 21개.
ES 모듈로 탭별 파일 분리(빌드 불필요). 1~4단계가 끝난 뒤에 한다.

---

## 6. 순서와 이유

| 단계 | 없애는 문제 | 규모 |
|---|---|---|
| 1. 데이터 분리 | A(충돌·유실) | 큼 — 단계적으로 |
| 2. 공통 모듈 | 복사로 번지는 버그 | 중간 |
| 3. 조건 통합 | D(분기 누락) | 중간 |
| 4. 자동 점검 | B(조용한 실패) | 작음 · 효과 큼 |
| 5. 화면 분할 | 편집 사고 | 큼 — 맨 마지막 |

**4단계를 1단계보다 먼저 해도 된다.** 작고, 이후 모든 작업의 안전망이 된다.

---

## 7. 하지 말 것

- 한 번에 다 바꾸지 말 것. 이 파일은 매일 봇 13건이 쓰고 있다.
- 빌드 단계를 도입하지 말 것(기존 규칙). ES 모듈·정적 JSON 은 빌드가 아니다.
- 최적화로 정확성을 깎지 말 것. `[CI Skip]` 이 그랬다 —
  아끼려던 빌드는 한도의 21% 였고, 대신 화면이 옛 값으로 남았다.
- 실패를 `|| echo` 로 넘기지 말 것. `runstep.py` 를 쓸 것.
- 측정 없이 비용을 추정하지 말 것. 25분/45분, 실패율 0%/60% 둘 다 틀렸다.
