# 작업 규칙

> **📌 2026-09-24 부터 전면 개편 진행 중 — `개편계획.md` 를 먼저 읽을 것.** 진단·단계·사용자 결정 대기 항목·진행 로그가 거기 있다.
> 이 날 바뀐 것: ① `events.yml` 타임아웃 30→**55분** + **모든 트리거 중복 게이트**(오늘 수집 커밋이 있으면 종료 · 수동 `force` 입력만 예외)
> + 스텝 순서(레터 필수 앞 / 스토어·게임비트·채용·KT&G 뒤) ② 아마존 트래커 절전 방지·네트워크 대기·`python -u`·`--skip-if-done`
> + PC 작업 **`AmazonBeautyTrackerRetry`(06:45, 빠진 마켓만)** ③ 실측: **네이버 데이터랩 전일치는 KST 10시경 공개** —
> 07:30 화면의 네이버 일별 끝이 그제인 것은 정상이고 `refresh.yml` 10:08 회차가 어제를 붙인다.
> ⚠ **로컬 클론 함정**: 아마존 트래커의 `reset --soft` 재주입은 자기 파일만 받아 나머지 작업 트리가 옛 상태로 남는다.
> 작업 전 `git status --branch` 와 **`git diff --cached --stat` 가 비어 있는지** 볼 것 — 2026-09-24 에 6주치 원격 작업(수집기 14개·CLAUDE.md 1,070줄)이
> '삭제'로 스테이징돼 있었다. `git add -A` 한 번이면 날아갔다. 어긋나 있으면 `git checkout origin/main -- .` 로 맞춘다.

신주현(캐리 펀드 리서치)의 커버리지 대시보드. 소비재·유통·미용·음식료·엔터/미디어·게임·호텔 38종목.

- 사이트: https://coverage-dashboard.pages.dev (Cloudflare Pages, **공개**)
- 사용자는 개발자가 아님. 터미널 명령을 시키지 말고, **내가 직접 수정하고 push까지 완료**할 것.
- 한국어로 응답.

## 구조

`public/index.html` **단일 파일**이 전부다. 데이터는 JS 상수로 내장:

| 상수 | 내용 | 갱신 스크립트 |
|---|---|---|
| `DATA` | 유니버스(종목·섹터·점수·견적·27E 추정·Pick) | **없음 — 직접 편집** |
| `FIN` | 연간·분기 실적 | **없음 — 직접 편집** |
| `LIVE` | 시세·컨센·리포트·섹터지수·환율 | `refresh_live.py` |
| `DART_EVENTS` | 공시·IR 일정 | `fetch_events.py` |
| `NEWS` | 종목별 뉴스 | `fetch_news.py` |
| `TREND` | 네이버·구글 검색 트렌드 | `fetch_trends.py` |
| `TRADE` / `TRADE_PRELIM` | 화장품 수출입 | `fetch_trade.py` |
| `TOPTOON` | 탑툰챗(탑코미디어) 대화·조회 누적 | `fetch_toptoonchat.py` |
| `GAMEMONEY` | 아이템매니아 게임머니 시세·거래순위 — 제우스(컴투스)·리니지M/W·아이온2·TL(NC) | `fetch_gamemoney.py` |
| `GAMEBIT` | 게임비트 쌀먹 거래대금(거래소 합산 분봉 volume) — 제우스·아이온2·리니지클래식·리니지M | `fetch_gamebit.py` |
| `BUZZ` | 지금 화제 — 구글 트렌드 · 네이트 · 나무위키 · 디시 흥한 마이너갤(종목 무관) | `fetch_buzz.py` |
| `BEAUTY` | 올리브영 베스트 100 — 화장품 섹터의 국내 채널 판매 랭킹 | `fetch_beauty.py` |
| `SOOP` | SOOP(아프리카TV) 게임별 시청자 — 치지직·트위치의 나머지 반쪽 | `fetch_soop.py` |
| `JOBS` | 채용 공고(사람인) — 현장직 공고 수 = 생산 확대의 선행 신호 | `fetch_jobs.py` |
| `C2MODEL` | 컴투스 매출 모델의 재료 — DART 분기실적 27개 + 네이버 월별 검색 93개월 | `fetch_c2model.py` |
| `REVISION` | 어닝 리비전 트래커의 계산 결과 — **겹치는 종목만**(증권사 컨센·리비전·부문 서프) | `fetch_revision.py` |
| `BOXOFFICE` | 영화 흥행 탭 — 극장판 치이카와(대원미디어) + 흥행작 3편: KOBIS 일별·실시간 예매 · 3사 좌석 · 하츄핑2 기준선 | `fetch_boxoffice.py` |
| `QOO10` | Qoo10 JP 뷰티 베스트셀러 톱200 — 브랜드별 최고 순위·진입 수·리뷰합 + 한국발 비중(일본 K뷰티 주 채널, 2026-09-24) | `fetch_qoo10.py` |

각 스크립트는 **해당 블록만 정규식으로 교체**하므로 서로 덮어쓰지 않는다.
데이터 변동이 없으면 파일을 건드리지 않는다(Cloudflare 무료 빌드 500회/월 절약).

### DATA 는 엑셀에서 오지 않는다 (2026-07-26 ~)

엑셀 파이프라인(`rebuild_from_excel.py`)은 폐지했다. **`index.html` 이 원본**이고,
종목 추가·견적 변경 등은 사용자가 말로 지시하면 내가 직접 `DATA` 를 편집한다.

`DATA` 필드는 이게 전부다:

```
rank · pick2 · sector · sub · name · score · fairMktcap · rev27 · op27 · eps27
```

**API로 구할 수 있는 값은 절대 `DATA` 에 넣지 말 것.** 남은 건 두 종류뿐이다.

1. **분류·판단** — `sector`/`sub`·`score`·`pick2`·`rank`, 그리고 `fairMktcap`(견적 시총, 십억원)
2. **27E 컨센(엑셀)** — `rev27`·`op27`·`eps27`.
   무료 API 는 26E 1개년까지만 준다(네이버·FnGuide 공통). 27E 는 엑셀이 유일한 출처다.

제거한 것: `price` `target` `upsideCons` `mktcap` `upsideOwn` `ret1w/1m/3m`
`eps26` `epsg26` `per26` `per12mf` `rev26` `op26` `pickLabel`, 그리고 `FIN` 상수 전체.

### 파생값은 반드시 페이지에서 계산한다

엑셀에도 `per27`·`epsg27` 같은 파생값 열이 있지만 **쓰지 않는다.**
엑셀 작성 시점의 주가·컨센에 고정돼 있어 시간이 지나면 틀리기 때문이다.
(`upsideOwn` 이 그렇게 29종목 전부 틀렸던 전례가 있다.)

병합 블록에서 계산하는 것:

| 값 | 식 |
|---|---|
| `per27` | 라이브 주가 ÷ `eps27` |
| `np27` | `np26`(컨센) × (`eps27`/`eps26`) |
| `revYoY27` | `rev27` ÷ `rev26`(컨센) − 1 |
| `epsg27` | `eps26`(컨센) → `eps27` 성장률 |
| `opg27` | `op26`(컨센) → `op27` 성장률 |
| `upsideOwn` | `fairMktcap` ÷ (`mktcapEok`/10) − 1 |

### 당사 추정 vs 컨센 (`MY_EST`)

실적 프리뷰 탭은 **당사 분기 추정 ↔ 컨센 ↔ 실제** 3자 비교다.
사용자가 주는 숫자는 `index.html` 의 `MY_EST` 한 곳에만 넣는다:

```js
const MY_EST = { "달바글로벌": { "2026.06": {rev:190.0, op:43.0} } };   // 십억원
```

- **예정** 보기 = 발표 전. 괴리 = 당사 ÷ 컨센 − 1, ±5% 밖이면 상회/하회 배지.
- **발표완료** 보기 = 발표 후. 내 오차(당사÷실제) 와 컨센 오차를 비교해 승자를 매긴다.

**컨센 스냅샷이 핵심이다.** 실적이 발표되면 그 분기는 API 에서 실적으로 덮여
컨센 값이 사라진다. 그래서 `refresh_live.py` 가 매 실행마다 현재 컨센 분기를
`LIVE.consSnap[종목][분기]` 에 적어 두고, 발표 후에도 그 값을 남긴다.
**이 스냅샷을 지우면 과거 서프라이즈를 영영 복원할 수 없다.**

### 실적 발표 알림은 DART 가 주 경로다 (2026-08-21)

**네이버로 '발표 시점'을 잡으려 하지 말 것.** 네이버 재무 API 는 정식 보고서(분기말+45일)가
올라와야 그 분기를 '실적'으로 바꾼다. 그래서 네이버 flip 만 보던 `notify.py` 는 회사가
발표하고 한참 뒤에야 알림을 쐈다(실측: 2Q 발표분 4건이 8/21 아침에 몰려서 도착).

지금은 **`notify.py` 가 `PRELIM`(DART '영업(잠정)실적(공정공시)', `fetch_prelim.py` 수집)을
먼저 보고**, 정식 실적이 잡히면 그쪽(네이버 series)을 쓴다. DART 는 회사가 내는 즉시 뜨고,
키 있는 공식 API 라 차단 위험이 없다(사용률 2%). 그래서 **잠정실적 수집 주기 = 알림 속도**다
— `refresh.yml` 에서 장중 짝수 시(08·10·12·14·16·18)에 돌린다.

- 단위는 셋 다 십억원(`PRELIM`·`cons.series`·`consSnap`)이라 그대로 비교된다.
- 중복 발송은 `notified.json` 의 `종목|분기` 키가 막는다 — DART 로 이미 쏜 건 네이버 flip 때 안 쏜다.
- ⚠ **DART 경로를 새로 켤 땐 백필을 먼저 하라.** 이미 발표된 분기가 `PRELIM` 에 쌓여 있어
  그대로 켜면 한꺼번에 쏟아진다(도입 때 27건이 나갈 뻔해 `prelim-backfill` 로 막았다).

컨센 스냅샷 자체(`consSnap`)는 `refresh_live.py` 의 deep 수집에서 갱신되는데, 그건 '그날 첫
회차(08시) + 16시 + 18시' 3회다. 어느 슬롯까지 받았는지는 `LIVE.deepSlot`(`YYYY-MM-DD#시`)에
남겨 같은 슬롯 중복을 막는다.

### 어닝 리비전 트래커와의 통합 = 데이터만 가져온다 (2026-09-26 · `REVISION`)

대시보드가 둘이다 — 여기(커버리지)와 **어닝 리비전 트래커**(revision-tracker-5xv.pages.dev ·
비공개 저장소 `dayyyline-lgtm/revision-tracker` · 이 PC 의 `Desktop\revision-tracker`).
사용자가 "합칠 수 있나, 번잡해지려나" 물었고, **겹치는 종목만 데이터를 가져오는 것**으로 정했다.
탭으로 통째 흡수하지 않은 이유: 겹치는 건 25종목 중 12개뿐이고(나머지는 반도체·기계·금융 —
커버리지 밖), 저쪽은 검수·업로드가 딸린 앱이라 탭 하나로 안 줄고, build.py 가 필수라
'빌드 없는 파일 하나' 원칙과 부딪힌다. **통째 합치자는 제안을 다시 꺼내지 말 것.**

- `fetch_revision.py` 가 저쪽 **공개 `data.js`**(키 불필요)를 받아 `computed[]` 만 추린다 —
  원자료(리포트별 추정치·검수 메모)는 안 가져온다. **계산은 저쪽 compute.py 가 원본**이다
  (PRE/POST 판정·120일 신선도 창·부호 가드). 여기서 다시 계산하면 두 곳이 어긋난다.
- 매칭은 종목명 → 안 맞으면 종목코드. 저쪽에 커버리지 종목을 새로 올리면 **다음 회차에 저절로** 붙는다.
- 화면은 **상세 패널(`openStock`) 의 '증권사 리비전' 섹션**(`revisionHtml`) 하나뿐이다. 분기 차트 바로 아래.
  발표된 분기의 실적 vs 증권사 컨센 → 발표 후 영업익 컨센 이동 → (접힘) 증권사별 목표가·리비전 ·
  부문 서프 → 다음 분기 증권사 평균 vs 네이버 컨센 → (접힘) 부문 컨센.
- 비율은 소수(0.16=+16%)이고, 분모가 0 이하면 `흑전`·`적전`·`N/M` **문자열**이 온다. 숫자로 가정하지 말 것.
- 저쪽은 사용자가 리포트를 올려야 움직이므로 몇 주씩 그대로인 게 정상이다 → `watchdog.LIMITS` 에 **넣지 않았다**.
  최신 리포트가 45일 넘으면 섹션에 경고 한 줄이 뜬다.
- ⚠ 저쪽 `RT_DATA` 구조(`computed[].sec1~sec5`)를 바꾸면 여기가 깨진다. 저쪽 CLAUDE.md 에도 적어 뒀다.

### 연간·분기 실적 = `LIVE.cons.series`

`refresh_live.py` 가 네이버 재무 API의 **전 기간**을 담는다.
원소: `{k:"202412", t:"2024.12.", e:컨센여부, rev, op, np, opm, eps, bps, pbr}`

| | 범위 |
|---|---|
| `cons.year.series` | 2023A · 2024A · 2025A · 2026E |
| `cons.quarter.series` | 1Q25~1Q26 실적 + 2Q26 컨센 |

`est`/`prev` 는 하위호환으로 남아 있으나 **새 코드는 `series` 를 쓸 것.**

**27E 는 `series` 에 없다.** 상세 패널 연간표는 API `series`(2023A~2026E) 뒤에
`DATA` 의 `rev27`·`op27`·`eps27` 을 2027E 열로 덧붙여 5개 기간을 만든다.

`upsideOwn`(당사 상승여력)이 대표적 사고 사례다. 엑셀 작성 시점 값이 그대로 박혀 있어
29종목 전부 틀린 숫자를 표시했다(평균 +31.9% vs 실제 +51.5%).
지금은 병합 블록에서 `fairMktcap ÷ (mktcapEok/10) − 1` 로 계산한다.
**단위 주의: `fairMktcap` 은 십억원, `LIVE` 의 `mktcapEok` 은 억원이다.**

`public/` 만 배포된다. 스크립트·README는 루트에 있어 공개되지 않는다.

## 하지 말 것

- **빌드 단계를 도입하지 말 것.** 파일 하나 + push하면 즉시 배포되는 단순함이 이 프로젝트의 핵심.
  React·번들러·프레임워크 제안 금지.
- `secrets_local.py` 를 커밋하지 말 것 (.gitignore로 차단됨). 토큰·키를 채팅에 노출시키지 말 것.
- 원본 엑셀(`*.xlsx`)은 사내 자료라 커밋 금지.
- **레이아웃을 크게 바꾸지 말 것.** 사이드바 개편을 시도했다가 되돌린 이력이 있다.
  구조 변경은 반드시 먼저 물어볼 것. 색·간격 조정은 자유.

## 디자인 토큰

테마는 **Rosé Pine** (다크 = main / 라이트 = dawn). `:root` 와 `:root[data-theme="light"]` 두 블록.

- 색은 **반드시 CSS 변수**로. 배지·태그의 반투명 배경은
  `color-mix(in srgb, var(--토큰) N%, transparent)` — hex 하드코딩 금지.
- **강조색(`--accent`)은 빨강·파랑을 피할 것.** 그 둘은 등락(`--up`/`--down`)에 예약된 색이라,
  파란 강조색을 쓰면 버튼·탭이 "하락"처럼 읽힌다. 현재는 라벤더 `#c4a7e7`.
- 강조색 위 글씨는 `--onacc` (흰색 아님 — 라벤더 위 흰색은 대비 부족).
- 라이트(dawn)는 원본 팔레트보다 보조색을 한 단계 어둡게 잡아 대비 4.5:1을 맞춘 상태.
  이 값들을 원본으로 되돌리면 표의 숫자가 안 읽힌다.
- 한국 관행: **상승 = 빨강, 하락 = 파랑.**

## 엑셀 내보내기

`public/index.html` 하단에 **의존성 없는 순수 JS XLSX 라이터**가 들어 있다.
ZIP(STORE) + CRC32 + inline string + styles.xml 레지스트리를 직접 구성한다.

- 원본 유니버스 엑셀의 Universe 시트 양식을 재현하는 것이 목표. 컬럼 정의는 `C[]` 배열.
- 파일명: `신주현_소비재_커버리지_YYYYMMDD.xlsx`
- 조건부 서식 데이터 막대: EPS증가율(0~200%), 낙폭(파랑, -60~0), 반등(빨강, 0~150).
- x14 확장의 **GUID는 8-4-4-4-12 자리를 지킬 것.** 어기면 엑셀이 "복구가 완료되었습니다" 오류를 낸다.
- 빈 셀도 `<c r=".." s=".."/>` 로 내보내야 테두리가 끊기지 않는다.
- 수정 후에는 브라우저에서 blob을 가로채 ZIP을 풀고 XML 파싱까지 검증할 것. 눈으로만 보지 말 것.

## 탑툰챗 — 주인공은 랭킹 API 의 '활동지수' 다 (2026-09-01 개편)

탑코미디어 AI 챗봇. **DAU 는 공개되지 않는다. 'DAU' 라고 부르지 말 것.**
전용 탭 `#toptoon` 이 있다(`renderToptoon` in `js/app.js`).

### 무엇을 쓰나 — 랭킹 API(주) + 홈 누적(보조)

사이트가 **랭킹 API 를 키·로그인 없이 열어 두고 있다.** 이게 훨씬 좋은 지표다 —
누적이 아니라 **유량**이고, **과거 기간까지 준다(백필!).**

```
/api/ranking/realtime                   지금 이 순간 활동지수
/api/ranking/weekly?periodKey=2026-28    주간 · 캐릭터 50명 + score
/api/ranking/monthly?periodKey=2026-07   월간
/api/ranking/{weekly,monthly}/periods    ★ 과거 기간 목록 = 백필의 열쇠
/api/ranking/user/weekly?periodKey=...   유저 랭킹 30명 · isNew = 신규 진입
```

### 지역이 넷이다 — 홈만 보면 둘밖에 안 보인다

주소는 **JS 번들에서 찾았다.** 홈 화면·메뉴에는 국내·일본밖에 없다.

```
curl -s https://chat.toptoon.com/ | grep -o 'src="https://showcase[^"]*\.js"'   # 청크 31개
# 청크를 받아 grep 'https://chat\.' → 다섯 주소가 박혀 있다
```

| 코드 | 주소 | 백필 시작 | 캐릭터 | 최근주 활동지수 |
|---|---|---|---|---|
| KR | `chat.toptoon.com` | 4월 1주 | 88 | 61,589 |
| JP | `chat.toptoon.jp` | 5월 4주 | 79 | 26,445 |
| TW | `chat.toptoon.net` | 5월 4주 | 82 | 7,150 |
| GLOBAL | `chat.global.toptoon.com` | **7월 1주** | 86 | 2,868 |

- `chat.cn.toptoon.net` 은 `chat.toptoon.net` 과 **응답이 완전히 같다**(같은 서비스). 하나만 받는다.
- **각 지역의 백필 시작 주가 실제 진출 시점과 일치한다** — JP·TW 5월, 북미 7월.
  데이터가 맞다는 교차검증이자, 새 지역이 열리면 그 주부터 데이터가 생긴다는 뜻이다.

### 캐릭터는 카탈로그 API 로 받는다 — 홈 긁기는 19% 과소집계였다

```
/api/characters?limit=50&page=N     전체 카탈로그 (limit 은 서버가 50 으로 자른다 · 지역당 2요청)
```

필드: `chatCount` `viewCount` `likeCount` `startAt` `createdAt` `genre` `isOfficial` `creationType`.

- 홈 HTML 을 긁던 옛 방식은 **홈에 노출된 것만** 잡혔다 — KR 88명 중 51명.
  대화 누적이 실제 1,574,260 인데 1,321,441 로 보였다(**-19%**).
- 카탈로그는 명단이 흔들리지 않아 증분도 안정적이다. 홈 파서(`parse`)는 예비로만 남긴다.
- ⚠ **기반이 다른 두 계열을 이어 붙이면 가짜 점프가 생긴다.** `hist.src` 에 `cat`/`home` 을
  적어 두고, src 가 바뀌는 날은 증분을 내지 않는다(51명→88명 = 하루 25만 건 급증으로 보인다).
- `startAt` 으로 **월별 신규 캐릭터 투입량이 백필된다**. 활동지수가 '결과'면 이건 '투입'이다.
  실측: 전 지역 합 5월 111 → 6월 71 → 7월 32 → 8월 41. 한국은 3월 20 → 8월 8 로 반감.
- `followCount` 는 전 지역 0 이다(미사용 필드).
- ⚠ **'작품'(여러 캐릭터가 묶인 것)은 `/api/characters` 에 없다.** `/api/content` 가 따로 준다.
  2026-09-01 실측: 한국 2개(대화 6,057 = 카탈로그의 +0.38%), 일본·중화권·북미는 0개.
  규모는 작지만 **둘 다 한국 실시간 랭킹 1·2위**다 — 회사가 새로 미는 포맷이라 빠뜨리면 안 된다.
  캐릭터 id 와 겹치므로 `c` 접두어를 붙여 담는다(작품 6 ≠ 캐릭터 6).
- 랭킹에서 `kind:"content"` 인 항목이 그것이다. `castCount` 가 묶인 캐릭터 수.

### '대화수' 단위는 검증됐다 — 대화방(세션) 수다 (2026-09-01 3중 검증)

① 사이트 문구가 `"{sessionCount}개의 대화"` 로 세고 `turnCount` 는 별도 개념이다.
② 턴이라면 누적 157만 × 120원 = 1.9억이 매출 상한 → 2Q 실적 ~15억과 모순(귀류).
③ 실측: 12분간 +56건 = 하루 환산 6,764건 — 세션 기대치(일 8,798)와 부합, 턴 필요치(137,000)의 1/20.
매출 모델: **분기 매출 = 분기 신규 대화방 수 × 방당 단가**. 단가 기본 2,050원(2Q 15~18.6억 중간 16.8억 ÷ 818,956방 · 범위 1,832~2,271원),
화면(`#ttPrice`)에서 조정 — **공시가 나올 때마다 (분기 AI 매출 ÷ 분기 대화방 수)로 재보정할 것.**

### 파생 지표 (2026-09-01 추가 · fetch_toptoonchat.py)

- **기존 캐릭터 바스켓**(`weekly[].bk`) — 지역별 초기 로스터로 **고정**(KR: 4월까지 출시 40명,
  타 지역: ~6월). **절대 재계산 금지** — 명단이 바뀌면 지수가 이어지지 않는다. `sites[].basket` 에 보관.
  전체가 늘어도 bk 가 빠지면 '신규 공급으로 돌려막는 중'. 주간 랭킹 재수집으로 4월까지 백필됨.
- **결제 믹스**(`users[].act/col/att`) — 유저 랭킹 상위 30명의 냥 사용 등급(1~5).
  사이트 공식 기준: 활동=채팅에 쓴 냥, 컬렉션=이미지 해금에 쓴 냥. 턴과금↔앨범해금 이동을 보는 유일한 창.
  실측: 활동 3.80(7월1주)→2.73, 컬렉션 2.77→1.57 — 상위 유저 결제성 활동이 식는 중.
  (최애 캐릭터는 30명 중 20명 비공개라 지표 불가 — 넣지 말 것.)
- **일일 4분해**(`hist[].br/t5/cv/bd`) — 확산(대화 늘어난 캐릭터 수)·집중(상위5 비중)·
  전환율(대화증분/조회증분)·기존몫. 스냅샷 이틀부터 생긴다.

### 없는 것 — 확인해 봤다

`/api/ranking/daily` `/api/stats` `/api/statistics` `/api/active-users` 전부 404.
i18n 사전에 `activeUsersStat: "지금 {count}명이 빠져드는 중"` 이 있어 **실시간 접속자 표시 기능은
존재하지만**, 그 숫자를 주는 공개 엔드포인트는 없다(아직 안 켠 기능으로 보인다).
**일별 지표는 `/api/ranking/realtime` 을 매일 같은 시각에 찍어 만드는 수밖에 없다.**

- ⚠ `tot` 는 **상위 n명의 score 합**이지 서비스 전체 활동량이 아니다.
  초기 주차는 n 이 27~46 이라 50 이 찬 주차와 **직접 비교하면 안 된다.**
  그래서 `n` 을 같이 저장하고, 화면은 `n<50` 을 흐리게 그리고 표에 '부분'이라 적는다.
  전주비·정점比 판정은 `n>=50` 구간에서만 한다.
- `RANK_TOP`(=20) 만큼만 캐릭터를 보관한다. 그 밖에서 올라온 건 '신규'가 아니라 **'권외↑'** 다.
  실제로 상위 회전이 매우 빨라서(8월 4주 상위 8명 중 4명이 전주 20위 밖) 이 구분이 중요하다.
- `RANK_TOP` 을 늘리면 옛 기간이 얕은 채 남는다 → `_periods()` 가 스스로 다시 받는다.
- 실시간 값은 **롤링 윈도 스냅샷**이라 매일 같은 시각에 찍어야 비교된다. 수집 시각(`t`)을 같이 남긴다.

### 홈 누적 대화수 — 합계끼리 빼지 말 것

홈 HTML(Next.js flight 페이로드)의 캐릭터별 누적치는 여전히 받는다. 단:

**누적이 단조증가하는 건 캐릭터별로만이다.** 합계는 '그날 홈에 노출된 캐릭터들의 합'이라
한 명이 홈에서 빠지면 그 사람 누적치가 통째로 사라진다. 실측:

```
KR 08-31 대화 1,320,016(51명) → 09-01 1,316,732(50명)   합계 -3,284
   같은 날 개별은 전부 증가 (신아영 +349 · 한나리 +316 · 장선영 +411)
```

→ 증분은 **어제·오늘 둘 다 있는 캐릭터(교집합)** 로만 낸다(`hist.dchat/dview/nc`).
그러려면 상위 16명이 아니라 **전 캐릭터 직전값**이 필요해 `sites[].snaps`(최근 2일)를 둔다.
누적 그래프도 같은 이유로 조정누적(`cchat`)을 쓴다.

- ⚠ 조회수 필드는 **`views`** 다. `viewCount` 는 일부(멀티 작품) 객체에만 있다.
- 같은 캐릭터가 인기/신규/트렌드 섹션에 중복 등장하므로 **id 로 합친다.**
- 북미는 전용 도메인을 아직 못 찾았다(`chat.toptoon.us`·`/en` 둘 다 실패).

## 게임머니 시세 — 아이템매니아 (2026-09-02 신설, `fetch_gamemoney.py`)

MMORPG 게임머니의 원화 시세 = 게임 경제 온도계. 오르면 사려는 쪽(신규·복귀)이 많고, 내리면 반대.
컴투스 신작 **제우스: 오만의 신**(8/26)이 첫 대상. 출시 직후 앱스토어 매출 2위(9/2).

```
/_xml/gamemoney_avg.xml.php?gamecode=G&servercode=S&count=N   일별 price·amount · 백필 60일 OK
/game_info/js/gamelist.js                                        게임/서버 코드 전부 (JS 배열)
```

- 코드: 제우스 6027 · 아이온2 5799 · 리니지M 3449 · 리니지W 4763 · TL 5092. '서버전체'는 없다 →
  대표 서버 평균(제우스 아테나1~3·아레스1, 아이온2 월드거래소 천족/마족, 리니지M 데포로쥬 등).
- ⚠ `multiple`(단위)이 게임마다 다르다 — 100·1,000·10,000머니. 계열 이름에 단위를 박아 둔다.
- `_ajax/` 경로는 봇 게이트(가짜 IP 프레임)가 있지만 `_xml/` 은 urllib 로 열린다.
- ⚠ XML 의 `amount` 는 **거래량이 아니라 전일 대비 등락액**이다(JS 가 ▲▼ 옆에 찍는 값 · 7,630→7,790 일 때 160).
  저장 필드 `a` 도 등락이다. **거래량의 대용은 `/game_info/rank_game/` 의 거래순위 TOP30**(일별, 서버 렌더링이라
  urllib OK) — 절대 건수는 비공개, 순위만 준다. `GAMEMONEY.ranks[]` 에 하루 1점. 9/2 실측: 리니지클래식 2위 ·
  아이온2 3위 · 제우스 11위 · 리니지M 12위(전일 6위).
- UI 의 '서버전체'(servercode=0)는 XML 에서 `result="fail"` — 그래프 없음. 대표 서버 방식 유지.
- 실측: 아이온2 월드거래소 7/4 890원 → 9/1 390원(-56%). 해외 검색은 뛰는데 국내 경제는 식는 중.
- 사용자가 다른 PC/세션에서 아이템매니아를 이미 따고 있었다고 한다 — 여기 것과 중복될 수 있으니
  나중에 합칠 때 어느 쪽이 원본인지 먼저 정할 것.

## 쌀먹 거래대금 — 게임비트 (2026-09-02 신설, `fetch_gamebit.py`)

아이템매니아는 '가격'만 준다. **양**(거래대금)은 게임비트(gamebit.co.kr)가 거래소(플랫폼 M/B) 합산
분봉 캔들에 원화 volume 을 실어 준다. 가격↑+대금↑ = 진짜 유입, 가격↑+대금↓ = 공급 고갈(이탈).

```
/{game}                                     index-config JSON (jsonDir·서버 목록·단위) · game = zeus/aion/lineage/lineagem/odin…
/jdata2/{game}/total_status.json            서버별 현재가·등락  → 전 서버 평균 시세
/jdata2/{game}/server_{sid}.json            최근 거래 내역 100건(q·p·total·platform M/B)
/v3_get_chart_data.php?game=&sid=&type=min  분봉 OHLC + volume(**게임머니 수량** · aion 은 만키나라 ×10,000)
```

## 스토어 판매·예약 순위 — PS·Steam·MS 스토어 (2026-09-04 신설, `fetch_storerank.py` → `STORERANK`)

'지금 팔리고 있나'. 동접(플레이)·리뷰(구매 후행)보다 구매 시점에 가깝고, **예약 순위는 출시 전 유일한 판매 신호**다.
첫 대상 = 붉은사막 Enhanced DLC '미지의 여정'(10/15 · $24.99) 예약. 절대 판매량은 어디도 안 주므로 **순위만** 하루 1점.

| 소스 | 주소 | 목록 | 지역 |
|---|---|---|---|
| PS 예약 | `web.np.playstation.com/api/graphql/v1/op` categoryGridRetrieve · 카테고리 `3bf499d7…` | ~110 (전체) | US·KR·JP·GB |
| PS PS5 전체 | 같은 API · 카테고리 `d0446d4b…` · `sortBy` sales30 / downloads30 | 상위 300 | US·KR·JP·GB |
| Steam 최고판매 | `store.steampowered.com/search/results/?filter=topsellers&cc=` | 100 (DLC·번들 포함) | kr·us·jp·de |
| Steam 위시리스트 | `filter=popularwishlist` (미출시작 = 예약 관심) | 100 | 글로벌 |
| MS 스토어 | `microsoft.com/{loc}/store/top-paid/games/xbox` · `/pc` · `coming-soon/games/xbox` (SSR JSON 카드) | 50 | us·kr·jp |

- **PS 예약 카테고리의 기본 정렬이 `sales30`(30일 최다판매)** 이라 목록 순서가 곧 예약 판매순위다. `sortingOptions` 에
  Best Selling / Most Downloaded / 이름 / 출시일이 있고 `sortedBy` 로 확인된다.
- 지역은 `x-psn-store-locale-override` 헤더. PS 는 **persisted query 해시**라 사이트 배포로 깨질 수 있다 —
  절반 넘게 빠지면 `note_health` → watchdog. 해시는 브라우저에서 카테고리 페이지를 열고
  `performance.getEntriesByType('resource')` 로 다시 딴다(네트워크 탭은 이 환경에서 안 잡혔다).
- Steam 공식 `IStoreTopSellersService.GetWeeklyTopSellers` 는 access_token 이 필요해 빈 객체만 준다(번들에서 확인).
  차트 페이지(`/charts/topselling/KR`)는 SSR 에 데이터가 없고 JS 가 그 API 를 부른다. 그래서 검색 페이지 topsellers 를 쓴다.
- 안 되는 것: 에픽(컬렉션 API 가 정적 큐레이션) · 아마존(urllib 캡차) · Xbox 옛 reco API(호스트 소멸).
- 첫날 실측(9/4): DLC 예약 미국 11위/121, 한국·일본·영국은 104~109위(꼴찌권 — 8/25 개시라 30일 집계가 덜 찬 것일 수 있음, 며칠 봐야 함).
  본편 PS5 판매 한국 3위·미국 18위·일본 26위, Steam 한국 3위·미국 14위, Xbox 미국 31·한국 23·일본 22위, PC 스토어 미국 11위.
- 화면: 소스별 한 그룹(지역이 계열). 목록 크기 n 이 소스마다 달라 `(n+1−r)/n×100` 으로 뒤집고 실제 순위는 `rawSer`.
  **소스끼리 점수 비교 금지**(그래서 한 그룹에 한 소스만).
- 매칭은 카드 제목 키워드 — `'PUBG'` 만 쓰면 Black Budget 도 잡히고 `'Crimson'` 은 Crimson Moon 과 겹친다. 좁게.

## 앱 매출순위는 양대 마켓 · 게임은 전부 (2026-09-18 개편 · `fetch_appstore.py` → `APPRANK`)

예전엔 **애플만**, 그것도 **종목당 대표작 한 줄**이었다. 둘 다 반쪽이었다 —
국내 게임 매출은 구글 비중이 더 크고, NC 는 리니지M·리니지W·리니지2M·아이온2 가
'가장 높은 하나'로 뭉개져 어느 게임이 움직였는지 알 수 없었다.

| 마켓 | 주소 | 목록 |
|---|---|---|
| 애플 | `itunes.apple.com/kr/rss/topgrossingapplications/limit=100/genre=6014/json` | 게임 매출·무료 100 |
| 구글 | `play.google.com/_/PlayStoreUi/data/batchexecute?rpcids=vyAe2` (POST) | 게임 매출·무료 100 |

**구글은 공식 무료 API 가 없다.** 스토어 웹앱의 '최고 매출' 탭이 부르는 내부 RPC 를 그대로 쓴다.
페이로드에서 없어도 되는 것을 전부 걷어내면 이것만 남는다(2026-09-18 실측):

```
[[null,[[8,[20,100]],null,null,FEATS],[2,"topgrossing","GAME"]]]     # 무료순위는 topselling_free
```

- ⚠ **`FEATS`(43개 숫자)는 필수다.** 빼거나 다른 값으로 바꾸면 응답이 **200 인데 목록이 0 개**다.
  (처음에 다른 라이브러리의 옛 목록을 넣었다가 빈 배열만 받고 헤맸다.)
- 파싱 위치: `resp[0][1][0][28][0]` 이 100개 목록, 항목마다 `[0][3]`=제목 `[0][0][0]`=패키지 `[0][14]`=개발사.
- **다시 딸 때는 XHR 을 후킹할 것.** 이 페이지는 `fetch` 가 아니라 XHR 을 쓴다 — fetch 만 후킹하면
  아무것도 안 잡힌다. 카테고리 페이지를 새로 열고(캐시된 탭은 재요청을 안 한다) 후킹한 뒤 탭을 누르면
  `f.req` 가 통째로 잡힌다. 쿠키·로그인은 필요 없다(파이썬 urllib 로 그대로 된다).
- 실패해도 **한쪽만 죽으면 기록만** 남기고 나머지를 저장한다(둘 다 죽어야 종료코드 1).

### 한국만 보면 반쪽이다 — 매출순위 다국가 (2026-09-21)

니케는 매출 주력이 **일본**이고(구글 JP 3위), 리니지M 은 **대만**에서 구글 2위다. 한국 차트만 보면
시프트업·NC·컴투스의 해외 매출이 통째로 안 보인다. 그래서 **매출순위는 KR·US·JP·TW 네 나라**를 받는다.
무료순위(신규 유입)는 쓰임이 적어 한국만 받는다 — 해외까지 받으면 요청이 두 배가 된다.

| | 애플 | 구글 |
|---|---|---|
| 국가 지정 | `itunes.apple.com/{us,jp,tw,kr}/rss/...` | RPC 의 `gl`·`hl` (US/en · JP/ja · TW/zh-TW) |

2026-09-21 첫 실측: 구글 JP **니케 3위** · 구글 TW **天堂M(리니지M) 2위** · 애플 TW **MLB 9局職棒 17위** ·
구글 US 니케 37위·PUBG 57위 · 애플 US PUBG 47위.

- ⚠ **나라마다 제목이 현지어다** — 日 `勝利の女神：NIKKE` · 臺 `MLB：9局職棒26` · `天堂M`.
  개발사로 안 걸리는 게임(퍼블리셔가 남인 니케·PUBG)은 **현지 표기까지** `TITLE` 에 넣어야 잡힌다.
- ⚠ **같은 게임의 나라별 시리즈를 하나로 묶지 않는다.** 이름이 달라 동일성 판정을 하다 틀리느니
  계열 라벨에 나라를 붙여 그대로 늘어놓는다(`天堂M(대만)`). 한국이 앞에 오게 정렬한다.
- ⚠ **나라끼리 순위를 직접 비교하지 말 것** — 시장 크기가 다르다(각주에 적어 뒀다).
- 컴투스 모델의 '국내 모바일 몫' 계산은 **한국 차트만** 쓴다(`cc==="KR"`). 해외를 섞으면 이중이 된다.

**매칭은 개발사 이름이 먼저다.** 자체 퍼블리싱은 이걸로 전부 걸린다(NC·컴투스·펄어비스…).
퍼블리셔가 남인 게임만 제목 키워드로 잡는다 — **니케는 iOS 퍼블리셔가 'Level Infinite'(텐센트)**라
개발사만 보면 시프트업 몫이 통째로 빠진다.
⚠ 이름이 겹치는 상장사는 빼야 한다 — `Com2uS Holdings` 는 컴투스홀딩스(다른 회사)다(`PUB_NOT`).

- 저장 단위는 **(마켓, 앱)** 이고 화면 그룹은 **종목 × 마켓**, 계열은 게임이다.
  ⚠ **두 마켓을 한 그룹에 섞지 말 것** — 목록이 달라 같은 5위가 다른 크기다.
- 무료순위는 계열로 만들지 않는다(선이 두 배가 된다). 최신값만 각주로.
- 개편 때 옛 `games[]`(종목당 iOS 대표작)의 이력은 같이 저장돼 있던 제목(`t`)으로 앱을 찾아 이어붙였다.
  2026-09-18 실측: 컴투스 iOS 16일치가 그대로 이어졌다.
- 2026-09-18 실측: 구글 제우스 2위 · 리니지M 3위 · 니케 16위 / 애플 리니지M 6위 · 제우스 10위 · 니케 12위.
  펄어비스는 양쪽 100위 밖(검은사막 모바일) — 빈칸이 정상이다.

## 컴투스 매출 모델 — 월별 추정 → 분기 (2026-09-21 · `fetch_c2model.py` → `C2MODEL` · `renderC2Model`)

"트렌드 데이터로 매출을 세워 보라"는 요청. **먼저 안 되는 것을 재고 시작했다.**

| 해 본 것 | 결과 |
|---|---|
| 게임 검색지수로 분기 매출 **레벨** 회귀 | 전 구간 **R²=0.18** — 못 쓴다. 연결에 미디어 등 비게임이 섞이고 2021~22 인수로 레벨이 통째로 올랐다 |
| 검색 전년비를 기저에 **곱하기**(β=0.43) | 백테스트가 **나빠진다** — MAE 10.0% → 13.5%. 검색지수가 0.4~1.1 대라 비율이 요동친다 |
| QoQ **변화율**끼리 | 서머너즈워 검색 ↔ 매출 상관 **+0.51** — 방향은 있다(레벨은 아니다) |

그래서 GAMEEST(붉은사막)과 같은 결론이다 — **수준은 공시 앵커, 신작만 트렌드로 가산.**

```
월 매출 = 기저_월 + 신작_월
기저_월 = [직전 4분기 실적 평균 × 그 분기 계절계수] × 월배분(w) × (1 + β·기존게임 검색 전년비)
신작_월 = Σ_일 [ A·구글순위^(−α) + A·r_ios·애플순위^(−α) ]
분기    = 월 3개 합
```

- **기저 방식은 후보 다섯을 백테스트해서 골랐다**(2019~2026Q2 · n=23):
  4Q평균×계절 **10.0%** · 전분기×계절비 10.2% · 4Q평균 12.2% · 전년동기 15.2% · 전년동기×드리프트 16.1%.
  최근 8분기로 좁히면 MAE 6.0%. 계절계수는 하드코딩하지 않고 공시에서 계산한다(실측 Q1 0.857 · Q2 1.075 · Q3 0.961 · Q4 1.106).
- **월 배분(w)은 분기 크기를 바꾸지 않는다.** 일수 비례를 바탕에 두고 검색 분포로 기울일 뿐,
  3개월 합은 어떤 w 에서도 같다. 그래서 '검색은 레벨을 설명 못 한다'와 충돌하지 않는다.
- ⚠ **A(1위 일매출)가 모델의 전부다.** 반드시 **시장 규모와 자기일관적**이어야 한다 —
  Top100 합 = A × Σ(1..100)r^(−α), α=0.9 면 **6.43배**(1위가 Top100 의 15.6%).
  A=12억 → 구글 연 3.3조 · 애플 포함 4.5조로 한국 모바일게임 시장 추정과 맞는다. 화면이 이 환산을 늘 찍는다.
  (처음에 0.85 를 곱할 자리에서 나눠서 A 를 15~25억으로 잘못 봤다. **정합성 식을 손으로 다시 검산할 것.**)
- ⚠ **가장 약한 고리는 구글 순위가 2026-09-18 부터만 있다는 것**이다(양대 마켓 개편일).
  그 전 구간은 겹치는 날들의 구글÷애플 비(실측 **0.143** · 제우스는 안드로이드 편중)로 애플 순위를 환산해 채운다.
  출시 직후 7일(8/26~9/1)은 첫 관측 순위로 캐리백한다. 표·각주에 그 사실을 적는다.
- 2026-09-21 실측: **3Q26 추정 199.4십억**(기존 155.1 + 제우스 44.3) vs **컨센 200.3** → 괴리 −0.5%.
  즉 **컨센은 제우스 3Q 기여를 ~450억으로 이미 반영**하고 있다는 뜻이다. 역산하면 A=12.2억.
  모델이 독립적으로 컨센을 재현한 셈이라, 이제 쟁점은 3Q 레벨이 아니라 **4Q 지속성**이다.
- **공시가 나오면 A 를 역산해 다시 맞출 것.** 화면 하단이 늘 역산값을 찍어 둔다(GAMEEST 와 같은 규율).

### 왜 앱 매출순위로 전부 세우지 않나 (2026-09-21 실측)

검색은 대리지표지만 **앱 매출순위는 매출에 직접 붙는다.** 그래서 컴투스 전 게임을 순위로 환산해
화면에 표로 올린다(제우스 · 컴프야V26 · 컴프야2026 · 서머너즈워 · MLB9). 다만 **그것만으로
레벨을 세울 수는 없다** — 국내 차트로 잡히는 몫이 작기 때문이다:

```
2026-09-21 · A=12억 기준 일매출 추정
  제우스 12.39 (구글 1위·애플 14위)  ← 신작, 기저에 가산
  컴프야V26 1.05 · 컴프야2026 0.37 · 서머너즈워 0.21 · MLB9 0.10   합 1.73억/일
  직전 분기(2026Q2) 실적의 하루 평균 17.3억 → 기존 게임 국내 몫은 그 10% 뿐
```

**서머너즈워는 매출 대부분이 해외**라 한국 구글·애플 차트에 거의 안 나타나고, 미디어 등 비게임도 있다.
그래서 순위는 **신작 가산과 추이**에만 쓰고 레벨은 공시 기저에서 잡는다.
⚠ 기존 게임을 순위로 또 더하면 **이중계상**이다(직전 4분기 실적에 이미 들어 있다). 표의 '취급' 칸이 그걸 밝힌다.
⚠ 순위 이력이 한 분기 이상 쌓이면 그때는 '기존 게임 국내 몫의 변화'도 순위로 잴 수 있다 — 지금은 3주뿐이라 못 한다.

### 재료를 왜 따로 받나

화면의 `LIVE` 는 분기 **6개**뿐이라 계절성·기저 오차를 잴 표본이 안 된다. `fetch_c2model.py` 가
**DART 주요계정으로 27분기**(2019~), **네이버 데이터랩 월별로 93개월**을 받아 `C2MODEL`(5KB)에 담는다.

- 네이버 데이터랩은 **한 요청으로 7.7년치**를 주고, **한 요청 안의 키워드끼리만 같은 스케일**이다.
  게임 키워드는 반드시 **한 요청에 묶어서** 받을 것. 요청을 쪼개면 계열 간 크기 비교가 깨진다.
- DART 4Q 는 따로 안 준다 — **사업보고서(연간) − 3Q 누적**으로 만든다.
- ⚠ 새 수집기는 **환경변수 우선 → `secrets_local` 폴백**으로 키를 읽을 것.
  러너에는 `secrets_local.py` 가 없어서 최상단 import 한 줄로 그 자리에서 죽는다.

## 게임 판매·매출 추정 모델 (2026-09-04 · `GAMEEST` in js/app.js)

**순위만으로 절대 매출을 뽑는 방법은 없다.** 목록 총액이 비공개라 스케일 상수를 잡을 수가 없다.
그래서 **수준(level)은 판매량에서 세우고, 순위는 추이·지역배분에만 쓴다.** 억지로 순위→매출을 하면 틀린다.

붉은사막은 드물게 앵커가 다 있는 사례다:

| 앵커 | 값 | 출처 |
|---|---|---|
| 출시 | 2026-03-20 · 1일 200만 · 4일 300만 · 26일 500만 · 83일 600만장 | 회사 발표·보도 |
| 플랫폼 비중 | Steam 53% · PS5 34% · Xbox 13% | 알리네아 애널리틱스 2026-05 |
| Steam 매출 | $190M+ (2026-07) | 알리네아 |
| 1Q26 / 2Q26 | 매출 3,285억(op 2,121억) / 1,926억(op 676억) · 북미·유럽 81% | 공시 |
| 출시 전 기준선 | 4Q25 매출 955억 | 공시 |

3층 구조 — 계수는 전부 화면 입력(탑툰챗 '방당 단가'와 같은 방식):

1. **설치기반** = 600만장(6/11 앵커) + 그 뒤 **Steam 리뷰 증분 × 리뷰당판매 ÷ Steam비중**.
   리뷰당판매 기본 **20.5** = 318만장(600만×53%) ÷ 그 시점 리뷰수(약 15.5만, 관측 페이스로 역추정).
   업계 통상 20~60배인데 **대형작일수록 낮다** — 교차검증: $190M ÷ 318만장 = $60/장(정가 $69.99의 85%, 지역가격 감안 타당).
2. **매출** = 판매량 × 순ASP. 기본 **52,000원** = (1Q+2Q 기여 3,301억) ÷ (그 기간 633만장).
   분기별 실현 ASP 는 크게 다르다(1Q 63,047원 · 2Q 40,315원) — 출시분기는 정가+디럭스 믹스, 2Q 는 할인.
3. **DLC 예약** = 설치기반 × 부착률(기본 15%) × 순단가($24.99×0.7×환율) × 예약비중(기본 25%).

**지역 배분이 순위의 유일한 정당한 쓰임이다.** 점유 ∝ (그 나라 PS5 설치대수) × 순위^(−α).
⚠ **시장규모 가중을 빼면 안 된다** — 순위만 쓰면 한국이 70%로 나와 공시(북미·유럽 81%)와 정반대가 된다.
가중을 넣으면 본편 미국+영국 73%로 공시와 방향이 맞는다(독일·프랑스 미포함이라 81%와 정합). 이게 백테스트다.

⚠ **바닥 순위는 검열(censored)돼 있다.** DLC 의 한국·일본·영국 104~109위는 '목록 끝(110개)'이라 실제로는
더 아래일 수도, 갓 진입한 것일 수도 있다. 며칠 추이를 봐야 갈린다.

⚠ **기준선 가정이 가장 약한 고리다.** 4Q25 매출 955억을 모든 분기의 본업으로 빼는데, 본업이 그 뒤 커졌다면
붉은사막 기여가 과대계상된다. 백테스트 오차(1Q −18% · 2Q +29% · 3Q −31%)의 상당 부분이 여기서 온다.
**공시가 나올 때마다 백테스트를 보고 계수를 다시 맞출 것.** 새 판매량 발표가 나오면 `GAMEEST.units` 에 손으로 추가한다.

## 시청자 표본은 '언제 찍었나'가 값이다 (2026-09-04)

치지직·트위치는 회차마다 순간값을 찍고 그날 최댓값만 남겼는데, refresh.yml 이 08~20시라 **국내 방송 피크(21~24시)를 통째로 놓쳤다.**
이제 `hist[]` 에 `h`(최댓값이 찍힌 KST 시), `n`(표본 수), `a`(표본 평균)를 같이 남기고, **`viewers.yml` 이 KST 21·22·23시에
세 번 더 찍는다**([CI Skip] · repo-write 그룹). 화면 각주에 '최댓값은 21시 표본(하루 n회 관측)'으로 나온다.
⚠ 워커 Cron Trigger 에 `0 12,13,14 * * *`(wf=viewers.yml)을 사람이 넣어야 정시성이 산다 — GitHub cron 은 밀린다.

## Steam 동접 — SteamCharts 해상도가 기간마다 다르다 (2026-09-04 외부 대조로 발견)

`chart-data.json` 은 최근 ~1개월 시간별 · 그 전 일별 1점 · 3개월 전은 **월별 1점(그 달 최고)** 이다. 월별 점을 일별 선에
이어 붙였더니 3/1·4/1·5/1·6/1 네 점이 x축 네 칸을 차지해 '출시 직후 절벽' 아티팩트가 생겼다(넉 달 감쇠가 나흘처럼).
지금은 앞쪽 성긴 점을 버리고(`_daily_peak` 의 while), 시간별 자료가 있는 날은 **평균동접(`avg`)** 도 같이 낸다 —
peak 만 뛰고 avg 가 안 오르면 '잠깐 몰렸다 빠진' 유입이다. 정규화는 소수 1자리(정수면 저점 1틱이 수백~수천 명).
리뷰 Δ는 수집이 빈 날을 날수로 나눠 '하루당'으로(안 나누면 이틀치가 하루 스파이크로 보인다).

## 트렌드 탭 y축은 주제마다 다르다 (2026-09-18)

탭 이름은 '트렌드 비교'지만 검색 트렌드는 138개 계열 중 15개뿐이다. 나머지는 동접·시청자·순위·
거래대금·수출금액이라 **y축이 0–100 지수가 아니다.** 그런데 범례는 전부 `상대값 0–100` 이라고
적고 있었고, 실제로 셋 다 틀렸다:

| | 증상 | 조치 |
|---|---|---|
| 순위 계열 | 축이 0–30(`31−순위`)·0–100(`101−순위`) — 지수가 아니다. 범례·표에 **뒤집힌 점수**가 그대로 찍혔다(거래순위 '최근 20' = 실제 11위, 앱스토어 '최근 87' = 실제 14위) | `rankN`(+`rankOf`)·`fmt`·`rawSer` |
| peak 아는 실측 계열 | 축 눈금을 실제 단위로 적을 수 있는데 지수로만 보여 크기를 알려면 각주를 읽어야 했다 | `peak` → 눈금이 `1.1만명` 처럼 실측 단위 |
| 계열별 따로 정규화 | 게임머니·쌀먹 거래대금·전서버 시세·디시 글수·수출(지역/전국)은 **계열마다 자기 최고=100** 인데 겹쳐 그려 놓고 '0–100' 이라고만 적어 선 높이를 비교해도 되는 것처럼 읽혔다 | `selfNorm:true` → 축 캡션에 '선 높이끼리 비교 불가' |

축의 정체는 `yAxisInfo(G)`(js/app.js) **한 곳**에서 정하고 눈금·차트 캡션(왼쪽 위)·범례가 같이 쓴다.
**새 계열을 만들 때 축이 검색지수가 아니면 `rankN`·`peak`·`selfNorm`·`stack` 중 하나를 반드시 달 것.**
안 달면 조용히 '검색지수 · 기간 고점=100' 으로 표시된다.

- 순위 눈금은 **순위 쪽에서 만들어 되돌린다**(축을 등분하면 '23.5위' 가 나온다). 간격은 목록 크기가
  아니라 **지금 보이는 순위 폭**으로 정한다 — 목록 크기로 고정했더니 60~80위에 머무는 계열(Steam 100위
  목록)에 선이 두 개만 그어졌다.
- ⚠ `selfNorm` 그룹은 여전히 겹쳐 그린다. 시점·모양을 겹쳐 보는 게 목적이라 그대로 뒀지만,
  **크기를 비교해야 할 일이 생기면 국가별(`multi`)처럼 격자로 쪼갤 것.**

### 곁가지 블록은 접는다 — 차트가 본문이다

지금 화제(디시·구글·네이트·나무)·올리브영 베스트·채용 공고 셋은 **종목 무관 블록**인데 트렌드 탭
맨 위에 펼쳐져 있었다. 실측(1440px): 632 + 353 + 1,169 = **2,228px** — 정작 본문인 트렌드 차트가
탭 높이의 62% 아래로 밀렸다. 지금은 `sideFold()` 로 **한 줄 요약 + 접기**(합 120px)다.

- 요약 줄에 핵심 수치를 얹어 **접힌 채로도 읽히게** 한다(커버리지 N종목 · 최고 순위 · 현장직 건수).
  지금 고른 종목이 있으면 맨 앞으로 끌어온다.
- 펼침 여부는 `localStorage`(`fold_<key>`)에 기억한다 — 블록이 종목 바꿀 때마다 다시 그려지므로
  안 그러면 펼쳐 둔 것이 도로 접힌다. `toggle` 은 버블링하지 않아 **캡처 단계**로 받는다.

### 주제가 많은 종목은 '분류' 로 한 번 거른다

한 종목의 주제가 20개를 넘었다(펄어비스 21 · 탑코미디어 21 · NC 17). 버튼을 한 줄에 다 깔면
차트가 화면 밖으로 밀린다. 그래서 주제 줄 위에 **분류 줄**을 뒀다 —
플레이 · 시청 · 판매·순위 · 리뷰 · 게임경제 · 커뮤니티 · 검색 · 기타(`topicKind`).

- 주제가 **8개 미만이면 분류 줄이 안 뜬다**(하이브 5개는 거를 게 없다).
- ⚠ 판정 순서가 중요하다 — `거래순위` 는 이름에 '순위' 가 들어가지만 게임경제 쪽이라 먼저 걸러야 한다.
- 분류를 바꾸면 그 분류의 첫 주제로 옮긴다. 하이라이트 칩(`selectTrend`)으로 건너뛸 때도
  그 주제의 분류로 맞춘다 — 안 하면 필터에 가려 주제 줄이 비어 보인다.
- 하이라이트 칩에서 **순위 계열은 %를 쓰지 않는다.** '전일비 +481%' 는 (301−순위) 점수가 5배가 됐다는
  뜻이라 사람이 읽는 순간 틀린 그림이 된다. 순위는 `144위 ▲130` 처럼 **몇 위에서 몇 위로**만 적는다.

## 트렌드 차트는 '원값이 본선' — 평균은 켜야 보이는 추세선 (2026-09-26 개편)

2026-09-24 에 일별 계열을 '7일 이동평균이 본선 · 원값은 흐리게 · 숫자·표도 평균 기준'으로 바꿨는데, 사용자가
**"평균으로 때리니까 너무 헷갈려"** 라고 했다. 실측으로 확인한 부작용:

| 증상 | 예 |
|---|---|
| 하루 스파이크가 7일짜리 고원이 된다 | 아이온2 일본 — 원값 36 인데 선·큰 숫자는 47.4, '주간 +127%' |
| 사람 수에 소수점 | 카드 '599.1명' |
| 원값이 흐린 회색이라 무슨 일이 있었는지 안 보인다 | 메디큐브 원값 66~100 이 선에선 75~85 로 평평 |
| 버그 | 범례 '최근 null'(마지막 날 값이 비면) · 순위 범례가 '94위'(실제 7위) |

지금 규칙 — **그리는 선과 적는 숫자는 수집한 값 그대로**:
- 본선 = 원값. '+ 추세선' 토글(기본 꺼짐 · `localStorage.trendTrendline`)을 켜면 7일 평균을 **얇은 점선으로 겹친다**(대신하지 않는다).
- 하루 튐을 뉴스로 안 읽게 하는 일은 선이 아니라 **변화율 계산**이 맡는다 — 일별은 `weekOverWeek()` =
  **최근 7일 합 vs 그 전 7일 합**(이번 주 vs 지난주, 양쪽 5일 이상). 전일비는 툴팁에만. 주별·월별은 전기비.
- 표의 '4주 전'은 **같은 요일의 실제 값**. 기준값이 그 계열 최고의 1/4 밑이면 %를 안 낸다(앨범 발매 달 +7,594% 류).
- 값은 `trendReal()`·`trendRealFmt()` 한 곳에서 실제 단위로(명·건·원·위 · 12,620만원 → 1.26억원 · 사람 수는 정수).
  ⚠ 그룹의 `fmt` 는 받는 값이 제각각이다 — **순위 그룹은 뒤집힌 점수**(31−순위), **누적 막대는 실제 값**.
  그래서 `trendRealFmt` 는 순위를 직접 'n위'로 적고 `fmt` 는 누적 막대에만 쓴다.
- **겹쳐 보기 / 나눠 보기**(`trendLayoutOf`) — 계열 셋 이상에 계열별 최고가 5배 넘게 벌어지면 자동으로 나눠 본다
  (메디큐브에 눌려 바닥에 깔리던 달바의 반등이 보이게). 나눠 보기는 칸마다 자기 축이되 **실제 단위 눈금**을 단다.
  국가별(multi)은 예전처럼 공통 축 격자. 주제를 바꾸면 자동으로 돌아간다.
- '주목할 추이': 일별은 이번 주 vs 지난주·4주 전 주(7일 합). **한 날이 이번 주 합의 절반을 넘으면 '튄 하루'로 제외**.
- 점검: 38종목 × 주제 × 추세선 켬/끔 × 겹쳐/나눠 = 1,110 조합을 그려 오류·null·NaN 0건(2026-09-26).

## 트렌드 탭 구간 — '최근 N일' 슬라이더 (2026-09-04)

`trendDays`(null=전체) 하나로 창을 잡고, 빈도(일별/주별/월별)는 요청 일수를 가장 잘 덮는 쪽을 `winPlan()` 이 고른다.
단기 30·중기 90·장기 전체는 프리셋. 짧게 볼수록 y축을 구간 안 최대까지 확대하고 범례에 '전체 고점=100' 을 적는다.
겹침·격자 차트는 null 에서 선을 끊는다(`trendPath`) — 예전엔 null 이 0 으로 바닥에 찍혔다.

## 왜 갱신이 멈춰 있었나 — 세 가지 다른 고장 (2026-09-08 진단)

사용자가 "업데이트가 제때 안 되는 게 있다"고 해서 전 상수의 `asOf` 를 훑었더니, **원인이 셋 다 달랐다.**
증상은 같아 보여도(숫자가 옛날 값) 고치는 자리는 전혀 다르다.

### ① `runstep.py` 가 '왜' 를 덮어쓰고 있었다 — 진단 자체가 불가능했다

수집기들은 죽기 직전에 자기 사유를 남긴다:

```python
note_health("게임머니", "전부 실패: 봇 게이트")   # 수집기가 적고
sys.exit(1)                                    # 죽으면
# runstep 이 같은 키에 "…실패 (종료코드 1)" 을 덮어썼다 ← 사유 소멸
```

**`GAMEMONEY` 이 이 때문에 엿새를 헤맸다.** `git log -S` 로 확인해 보니 도입 커밋(`b496d98`) 이후
블록이 **한 번도 갱신된 적이 없다** — Actions 에서 100% 실패, 로컬에서는 4초 만에 성공.
그런데 기록에는 "종료코드 1" 뿐이라 원인을 좁힐 수가 없었다.

**재시도는 '시각'이 아니라 '조건'으로 걸 것** — 여기서 한 번 더 넘어졌다.

게임머니 재시도를 `KST 15시` 한 시각에 걸었는데, 하필 **그날 15시 슬롯이 통째로 빠졌다.**
실측: 그날 refresh 회차가 08·09·10·11·12·13·14·16·18 — 15시·17시가 없다(워커가 거르고
GitHub 백업 cron 은 3~12시간 밀린다). 재시도가 아예 안 돈 것이다.
**'한쪽만 두면 그게 죽는 날 데이터가 없다'는 이 문서의 경고를 그대로 밟았다.**

지금은 `collector_health.due_today(상수명, after_hour)` 로 판정한다 —
**"오늘치가 아직 없고 지금이 그 시각 이후면 받는다."** 슬롯 하나가 빠져도 다음 회차가 이어받고,
이미 받은 날은 조용히 건너뛴다. `after_hour` 는 '이 시각 전에는 소스가 어제치를 안 준다'는 뜻이다
(Steam 9시 = UTC 일자가 닫힌 뒤 · 게임머니 13시).
**새로 '하루 한 번' 스텝을 만들 때 시각 비교를 손으로 쓰지 말고 이걸 쓸 것.**

게임머니 실패 원인은 **2026-09-09 확정됐다 — 러너 IP 차단(TCP 무응답)**, 아래 ⑤ 참고. 당시엔 요청 헤더에
같은 함정이 있었다 — **HTML 페이지와 XML 에 `Accept: application/json` 을 보내고 있었다.**
올리브영이 바로 그 이유로 403 이었으므로 먼저 고쳤다(XML 은 xml Accept + XHR 헤더,
순위 페이지는 `doc=True`). 그래도 실패하면 그때는 IP 차단이 유력하다 —
이제 `runstep` 이 사유를 보존하므로 다음 실패 회차에 정확한 문구가 남는다.

지금 `runstep.py` 는 ① 자식 출력을 잡아 두되 그대로 다시 찍고(Actions 로그는 그대로)
② 수집기가 **이번 실행 중에** 남긴 사유가 있으면 그걸 살리고 ③ 없으면 **stderr 마지막 줄**을 쓴다.
`PYTHONIOENCODING=utf-8` 도 넣는다(러너 기본 인코딩에서 한글 print 가 죽는 일이 있었다).

⚠ **성공했는데 수집기가 경고를 남긴 경우엔 그 경고를 지우지 않는다.** 부분 차단이 그렇게 사라졌다.

### ② Steam 동접은 **수집 시각이 틀렸다** — 늘 이틀 전 값이었다

`fetch_steam` 은 SteamCharts 의 **UTC** 시간별 점을 날짜별로 묶고, '진행 중인 UTC 당일'은 버린다
(하루 peak 이 안 정해져 급락처럼 보이므로 — 맞는 처리다). 그런데 이걸 `events.yml`(**KST 05시**)에서
돌렸다. KST 05시 = **UTC 20시(어제)** 라, 어제 UTC 는 늘 '진행 중'이라 버려졌다.
→ 9/8 아침에 수집했는데 마지막 동접 점이 **9/6** 이었다.

**`refresh.yml` 의 평일 KST 10시 · 주말 12시로 옮겼다**(UTC 01·03시 = 어제 UTC 가 닫힌 뒤).
⚠ **`events.yml` 로 되돌리지 말 것** — 되돌리면 같은 증상이 조용히 다시 생긴다.

같은 함정을 가진 소스: **UTC 기준으로 하루를 끊는 것**은 전부. 새 수집기를 KST 새벽에 돌리기 전에
"이 소스의 하루는 어느 시간대 기준인가"를 먼저 물을 것.

### ③ `shop.yml` 은 `runstep` 을 안 거쳐 사유가 아예 안 남았다

`python fetch_shop.py` 를 직접 불렀다. 2026-09-07(월) 회차가 그렇게 조용히 빠졌고,
**주 1회라 아무도 며칠간 몰랐다.** runstep 경유로 바꾸고 커밋 단계에 `health.json` 을 넣었다
(빼면 실패 기록이 원격에 안 올라가 watchdog 이 못 본다 — 2026-09-01 에도 같은 걸 밟았다).

### ④ 트렌드는 늘 '이틀 전'에서 끝났다 — 05시가 네이버 전일치보다 이르다 (2026-09-09)

"9/9 인데 9/8 트렌드가 없다"로 발견. 원인이 **둘 겹쳐** 있었다. 하나만 고치면 증상이 그대로다.

1. **네이버 데이터랩은 전일치를 KST 05시엔 안 준다.** 실측: 9/9 05:56 회차·07:40 로컬 탐침 둘 다
   9/7 까지, 9/8 16:53 실행은 9/7(=전일)까지. 공개 시각은 오전~오후 어딘가다(정확한 시각 미확인).
2. **`fetch_naver` 가 러너(UTC)의 `date.today()` 로 '진행 중인 날'을 잘랐다.** KST 05시 = UTC 20시(어제)라
   어제(KST)가 '오늘(UTC)'로 버려졌다 — 네이버가 줬어도 축에 안 들어왔을 구조. Steam(②)과 같은 함정.
   지금은 KST 오늘로 자른다. **`date.today()` 를 수집기에 쓰지 말 것 — 러너는 UTC 다.**

고친 방식: `refresh.yml` 매 회차에 `collector_health.trend_daily_stale()`(일별 축 끝 ≠ 어제) →
`fetch_trends.py --naver-only --if-stale`. `--if-stale` 은 키워드 하나(1요청)로 어제 날짜가 왔는지 먼저
보고 아니면 즉시 끝나므로, 공개 전 회차는 요청 1건이다. 공개된 첫 회차가 받고, 값이 그대로면 SKIP.
⚠ `due_today('TREND')` 로는 못 잡는다 — 05시 회차가 asOf 를 이미 오늘로 찍어 둔다.
⚠ 네이버만 받으므로 그날 구글 칸은 None(선 끊김)이고, 다음날 05시 전체수집이 채운다(정상).

같이 발견: 9/8 저녁 커밋 `53a884c`(채용 공고 신설)가 **push 되지 않은 채 로컬에만** 있었다.
사이트엔 하룻밤 동안 JOBS 가 없었고, 9/9 05시 events.yml 도 jobs 스텝 없이 돌았다. 재주입 절차로 올렸다.
**push 뒤엔 `git status` 로 'ahead' 가 0 인지 확인할 것.**

### ⑤ 러너에서만 안 되는 세 가지 — 원인이 전부 달랐다 (2026-09-09 러너 진단)

health.json 에 남은 게임머니·올리브영·MB(메가박스) 실패를 **러너 안에서 단계별로 찍어**(`diag_probe.py`,
결과 `diag/net_probe.json`, `diag.yml` 이 `diag_probe.py` push 때 돈다) 갈랐다. 이 PC(KT 가정용)에선 셋 다 0.2초에 200 이다.

| | 러너(Microsoft AS8075 · 미국) | 원인 | 조치 |
|---|---|---|---|
| 아이템매니아 | **TCP 연결 자체가 timed out**(80·443 · 헤더 없는 curl 도 동일) | 클라우드 IP 대역 드롭. 헤더·재시도로 절대 안 풀림 | `reachable()` 5초 사전점검 → 즉시 사유 기록. **한국 IP 에서만 수집 가능 → `kr_collect.py`** |
| 올리브영 | 05:51 은 403, **07:55 같은 헤더로 200** | Cloudflare WAF 봇 점수가 러너 IP 마다 흔들림 | `_get` 403 재시도 3회(8·16초) + `refresh.yml` 오후 재수집(`due_today('BEAUTY',9)`) |
| 메가박스 | 200 · 1.4초 | 차단 아님. 요청 수십 건 중 하나가 40초 넘기면 체인 전체 실패 | `mb_post` tries 2→3 |

- **"타임아웃 = 서버 느림" 이 아니다.** 아이템매니아는 DNS 는 되고 TCP 만 죽는다 — 그건 방화벽 드롭이다.
  같은 러너에서 네이버·게임비트·메가박스가 다 열리므로 러너 네트워크 문제도 아니다.
  새 소스가 Actions 에서만 타임아웃이면 **`diag_probe.py` 에 주소를 넣고 push 해서 단계부터 볼 것.**
- 게임머니는 도입(9/2)부터 Actions 갱신 0회다. 지금 화면의 GAMEMONEY 는 전부 이 PC 에서 손으로 돌린 것이다.
  **`kr_collect.bat` 를 이 PC 작업 스케줄러에 걸어야 매일 쌓인다**(게임머니+올리브영 · 안전장치: 미커밋 변경·미push 커밋이 있으면 안 건드림).
  러너 쪽 스텝은 그대로 둔다(5초에 끝나고, 대역이 열리는 날 저절로 이어받는다).
- Cloudflare 워커 프록시 경유는 **검증 못 했다**(플레이그라운드 편집기가 자동화 입력을 안 받음 · Radar 스캐너는 폼 제출이라 보류).
  해외 일반 IP(Anthropic 페처)는 응답을 받았으니 '전 해외 차단'은 아니고 클라우드 ASN 차단이다. 워커 IP 가 통할지는 미지수.

### ⑥ 네이버 종목뉴스 페이지가 사라졌다 — 410 Gone (2026-09-18)

`finance.naver.com/item/news_news.naver` 가 **HTTP 410** 을 준다(로컬·러너 둘 다). 네이버가 내렸다.
뉴스 탭이 통째로 비어 있었는데, 수집기가 **'0건 = 변동 없음' 으로 조용히 넘어가** 눈에 안 띄었다
(`health.json` 에는 '41/41종목 실패' 로 남아 있었다 — 그걸 봤어야 했다).

지금은 모바일 앱이 쓰는 JSON 을 쓴다:

```
https://api.stock.naver.com/news/stock/<종목코드>?pageSize=20&page=1
```

- 응답은 `[{total, items:[...]}]` **묶음 배열**이고 한 묶음이 같은 사건의 기사 뭉치다.
  **묶음마다 대표 1건만** 쓴다 — 연관기사까지 담으면 종목당 10건이 한 사건으로 다 찬다.
- 필드: `title`(HTML 엔티티) · `officeName`(언론사) · `datetime`(`YYYYMMDDHHMM`) · `mobileNewsUrl`.
  주소가 없으면 `officeId`/`articleId` 로 `n.news.naver.com/mnews/article/<oid>/<aid>` 를 만든다.
- ⚠ **'변동 없음' 을 성공으로 읽지 말 것.** 수집 0건과 '값이 그대로'는 다른 사건이다.

### 고장이 아니었던 것 — 헷갈리지 말 것

- **`MOVIE` 하츄핑2 의 9/2~9/4·9/7 구멍** — `MOVIE.scanned` 에 그 날짜가 **들어 있다.** 즉 KOBIS 를
  조회했고 그날 **일별 Top10 밖**이었다는 뜻이다. 수집은 정상이고 소스의 한계다.
  ⚠ 구멍을 보고 수집기를 고치려 들기 전에 **`scanned` 부터 확인할 것.**
- **`CIRCLE`(주간 차트)·`TRADE`(월간)** 은 원래 주기가 길다. `watchdog.py` 의 `LIMITS` 가 그래서 소스마다 다르다.

## SOOP 시청자 — 치지직의 집계 방식이 작은 게임을 못 본다 (2026-09-08 신설 · `fetch_soop.py` → `SOOP`)

국내 스트리밍 3대 플랫폼 중 **SOOP 만 빠져 있었다.** 치지직과 양강이라, 빼 놓으면
'국내 시청'이라면서 절반만 보고 있는 셈이었다.

```
sch.sooplive.co.kr/api.php?m=categoryList&szKeyword=&nPageNo=N   20개씩 · is_more 로 페이징 · 13요청에 300개
```

**여기서 중요한 건 SOOP 을 더했다는 것보다 집계 방식의 차이다.**

| | 방식 | 결과 |
|---|---|---|
| 치지직 | 인기 라이브 **상위 400개**를 훑어 카테고리별 합산 | 그 아래 게임은 **구조적으로 0** |
| SOOP | 카테고리 자체가 `view_cnt` 를 들고 있다 | 300개 카테고리 **전부** |

실측(2026-09-08): 치지직 상위 400 안에 **제우스·리니지클래식·서머너즈워가 아예 없다.**
SOOP 에서는 제우스 178 · 리니지클래식 180 · 서머너즈워 19 로 잡힌다.
**컴투스 신작 추적은 SOOP 에서만 가능하다** — 치지직 GAMES 에 제우스를 추가해도 0 만 찍힌다
(0 은 '시청자 없음'이 아니라 '상위 400 밖'이라 거짓말이 된다. 그래서 추가하지 않았다).

- ⚠ **세 플랫폼의 절대값을 더하지 말 것.** 집계 기준이 다르다. 같은 게임의 **추이**를 나란히 보는 용도다.
- ⚠ 카테고리명 띄어쓰기가 다르다 — SOOP "아이온 2"(공백) vs 치지직 "아이온2". 매칭 전에 **공백을 지운다.**
  안 그러면 조용히 0 이 찍힌다.
- ⚠ 화면 주입에서 `Math.max(...vals)||1` 을 **peak 계산에 먼저 쓰면 안 된다.** 내내 0 인 계열의
  peak 이 1 로 바뀌어 0 판정이 영영 안 걸린다(스텔라블레이드가 SOOP 에 방송이 없는데 계열로 떴다).
  `rawPeak` 을 먼저 보고 0 이면 계열 자체를 만들지 않는다.
- 치지직의 카테고리별 시청자 API 는 없다(`/categories` 404 · `home/categories` 는 수치 없음 ·
  `categories/GAME/{이름}/lives` 는 200 이지만 빈 배열 — categoryId 가 따로 필요해 보인다).
  페이지를 40장까지 늘리면 잡히겠지만 네이버는 **차단 위험 1순위**라 요청을 5배로 늘릴 자리가 아니다.
- 수집 시각은 치지직·트위치와 **같은 규칙**이어야 나란히 읽힌다 — `refresh.yml` 매 회차 + `viewers.yml`
  저녁 21·22·23시. 저장 필드도 같다(`v` 최댓값 · `h` 그 시각 · `n` 표본수 · `a` 표본평균).

## 채용 공고 — 생산 확대의 선행 신호 (2026-09-08 신설 · `fetch_jobs.py` → `JOBS`)

공장이 생산직·검사원·지게차를 뽑는다는 건 **이미 물량이 잡혔다**는 뜻이다. 캐펙스는 공시가 분기 뒤에
나오고 수출은 선적 뒤에 잡힌다. 채용은 그 앞에서 움직인다.
첫날 실측: **삼양식품 13건 전부 현장직**(원주 7·익산 4·밀양 2) · 빙그레 4건 전부 현장(경산 2·논산·광주) ·
에이피알 평택 SMT/이물검사 · 제닉 논산 제조팀 · 티앤엘 안성 오퍼레이터 · 코스맥스 평택 생산기술.

```
saramin.co.kr/zf_user/search/recruit?searchword=회사명&recruitSort=reg_dt&recruitPageCount=50   ua(doc=True)
```

- ⚠ **회사명 정확일치**로 걸러야 한다. '삼양식품' 검색에 협력사·인력파견이 절반 섞인다(13/50).
  검색어가 아니라 `corp_name` 을 (주)·주식회사 떼고 비교한다. 종목별 인정 이름은 `COS` 에.
- ⚠ **대기업은 이 소스에 없다** — 아모레·LG생건·CJ·신세계·롯데·농심·하이브·NC·크래프톤은 자사
  채용 사이트만 쓴다(실측 0건). **0 은 '채용 안 함'이 아니라 '이 소스에 안 올림'**이다. `BIG` 으로 표시.
- ⚠ **현장직 판정은 직무 태그 단위로.** v1 은 제목+태그를 한 문자열로 붙여 단어를 찾았는데
  **'온라인 MD' 가 '라인' 으로 현장직**이 됐다(23건 오탐, 달바 영업 17건이 전부 현장으로).
  사람인 태그는 표준 어휘라 태그 단위로 보면 정확하다. 제목은 거부어(영업·MD·시스템·Web·하우스키핑)로만 쓴다.
  'QA' 도 뺐다 — 소프트웨어 QA(컴투스 'Web QA')가 잡힌다. 공장은 QC·품질 태그가 따로 있다.
- **지역이 곧 공장이다** — 밀양=삼양 밀양2공장, 평택=에이피알·코스맥스, 세종=한국콜마, 논산=제닉·빙그레,
  안성=티앤엘·앨엔씨. 빙그레 **경산**은 옛 해태아이스크림 공장(26-04 합병).
- 신규(`new`)는 등록일 문자열이 아니라 **전일 rec_idx 집합과의 차분**이다. 첫날은 없다.
- 종목별 현장직 공고 수를 트렌드 계열('○○ 현장직 채용')로 편입한다 — 검색 그룹이 없던 삼양식품·콜마·
  코스맥스가 이걸로 트렌드 탭 종목 목록에 들어온다(2점 이상 쌓이면).
- 안 되는 것: 워크넷(별도 인증키) · 원티드(IT 위주, 생산직 없음) · 잡코리아(사람인과 중복) · 알바몬(URL 변경).

## 올리브영 베스트 — 화장품의 첫 국내 채널 지표 (2026-09-08 신설 · `fetch_beauty.py` → `BEAUTY`)

화장품이 커버리지 최대 섹터(8종목)인데 **국내 채널 지표가 하나도 없었다.** 갖고 있던 건 전부 해외다 —
관세청 수출(`TRADE`) · 아마존(`AMAZON`) · 러시아/일본 쇼핑(`SHOP`).

```
/store/main/getBestList.do   베스트 100(판매순) · SSR HTML · 브랜드+상품명, 목록 순서가 곧 순위
```

- ⚠ **기본 헤더로는 403 이다.** `collector_health.ua(doc=True)`(Accept: text/html + `Sec-Fetch-*` +
  `sec-ch-ua` + `Upgrade-Insecure-Requests`)로 바꾸면 **200 · 373KB**. 기본값 `Accept: application/json` 은
  사람이 주소창에 쳐서는 절대 안 나오는 조합이라 WAF 가 그걸로 가른다.
  **새로 뚫을 사이트가 403 을 주면 `doc=True` 를 먼저 시도할 것.**
- 지표는 **브랜드 노출 수 + 최고 순위**다. 개별 상품 순위는 기획전·증정 때문에 하루 단위로 크게 흔들린다.
- ⚠ **실리콘투(유통)·한국콜마/코스맥스(ODM)는 자기 브랜드가 없어 직접 매칭이 원리적으로 불가능하다.**
  '인디 비중'(대형사가 아닌 브랜드 수 / 100)이 그들의 **대리지표**이고, **대리지표라는 말을 화면에서 빼면 안 된다.**
  인디 브랜드가 전부 그 회사들의 고객인 건 아니다.
- ⚠ `BRAND_STOCK` 은 **소유·판권이 확인된 것만** 넣는다. 비디비치=신세계인터내셔날 · 홀리카홀리카=엔프라니 ·
  롬앤=아이패밀리에스씨 · 클리오/페리페라/구달=클리오 — 전부 커버리지 밖이라 안 넣었다. 넣으면 조용히 틀린 숫자가 된다.
- ⚠ 화면은 **종목 무관 블록**이다. 종목별로 접으려다 되돌렸다 — 아모레퍼시픽·LG생활건강·실리콘투·한국콜마·
  코스맥스가 `TREND_STOCKS`(검색 트렌드가 있는 종목)에 없어서 정작 가장 관련 있는 종목에서 섹션이 영영 안 떴다.
- 첫날 실측(9/8): 피지오겔 3위(LG생건) · 에스트라 6위(아모레, 5개) · 달바 15위 · 에이프릴스킨 22위 ·
  메디큐브 65위. 인디 비중 62%.
- **안 넣은 것 — 화해(hwahae).** `__NEXT_DATA__` 로 깔끔히 열리고 `rank_delta` 까지 주는데,
  기본 랭킹이 '급상승'이고 `is_advertised: true` 다. 9/8 1위가 **리뷰 52건짜리 무명 브랜드**였다 —
  판매 신호가 아니라 광고 노출이라 뺐다. 나중에 넣으려면 '카테고리별'(`ranking_type: RANKING`) 쪽을 볼 것.

## 지금 화제 — 종목 분류 '전'의 자리 (2026-09-07 신설 · `fetch_buzz.py` → `BUZZ`)

트렌드 탭의 나머지는 전부 **종목 → 주제**다. 그래서 **우리가 이미 보기로 정한 것만 보인다.**
여기만 반대 방향이다 — 한국 인터넷 전체에서 뜨는 것을 먼저 늘어놓고, 그 중 커버리지가 걸리면
배지를 단다. 커버리지 **밖에서** 올라오는 것을 알아채는 게 목적이다. 트렌드 탭 맨 위(`#buzzSec`).

| 소스 | 주소 | 무엇이 |
|---|---|---|
| 구글 | `trends.google.com/trending/rss?geo=KR` | 급상승 검색어 + **approx_traffic**(2000+·500+) + 관련기사 3건 |
| 네이트 | `nate.com/js/data/jsonLiveKeywordDataV1.js` | 실시간 이슈 10개 + 등락기호. **euc-kr** |
| 나무위키 | `search.namu.wiki/api/ranking` | 실시간 검색어 문자열 10개. 점수·순위변동 **없음** |
| 디시 | `gall.dcinside.com/m` SSR | 흥한 마이너갤 **300위 전부**(3개 `<ul>` 이 한 응답에) |

- **검색어 셋 중 규모가 붙는 건 구글뿐이다.** 나머지는 순위만이라 1위가 얼마나 큰지 알 수 없다 — 그래서 구글을 맨 위에 둔다.
- ⚠ 구글 옛 주소 `/trends/trendingsearches/daily/rss?geo=KR` 는 **404** 다(2026-09-08 실측).
- 네이트는 '이슈 문구'와 '실제 검색어'가 **따로** 온다. 배지 매칭은 둘 다 봐야 한다(문구에만 종목이 있는 경우가 있다).
- 소스가 넷이라 `note_health` 는 **셋 이상 죽을 때만** 남긴다. 하나둘 흔들리는 건 늘 있는 일이다.

- 사이트 정의: 전체 마이너갤 중 **300위 이내 = 흥한갤, 20위 이내 = 대흥갤.**
- ⚠ `json2.dcinside.com/json0/gallmain/gallery_hot.php` 는 **정식 갤러리 100위**다(마이너 0개).
  우리 커버리지 갤은 거의 다 마이너갤이라 그쪽은 안 쓴다. 다른 `*_hot_m.php` 류는 전부 404.
- ⚠ **둘 다 순간 스냅샷이라 과거가 없다.** 백필 불가 — 켠 날부터 쌓인다.
  그래서 `refresh.yml`(장중 매시간)과 `events.yml`(05시) 양쪽에서 찍는다. 요청 2건·3초.
- ⚠ 나무위키 '오늘 등장 횟수'는 **그날 수집 회차 `n` 으로 나눠야** 비교된다(수집이 장중에 몰리는
  시각 편향). 그래서 `days[].n` 을 반드시 같이 저장하고 화면은 비율(%)로 적는다.
- ⚠ **디시 순위는 작을수록 뜨겁다.** 59→43 은 '상승'(한국 관행상 빨강). 화살표 방향을 뒤집지 말 것.
- `DCGALL`(글 수)과 겹치는 게 아니라 보완이다 — 글 수는 **양**, 순위는 **전체 대비 상대 온도**다.
  글이 줄어도 순위가 버티면 판 전체가 식은 것이고, 글은 그대로인데 순위가 밀리면 우리만 식은 것이다.
### 본문은 '상장사로 번역한 것'이다 (2026-09-08 개편)

순위 목록 자체는 리서치가 아니다. "치지직 1위 · F1 3위"는 그냥 인터넷 잡담이다.
쓸모는 **상장사로 번역했을 때** 생긴다 — 블루 아카이브 8위는 '넥슨게임즈(225570)' 로 읽혀야 의미가 된다.
그래서 화면 본문은 **상장사가 걸린 것만**이고, 원본 네 목록은 `<details class="fold">` 로 접었다(확인용).

- **커버리지 밖이라도 국내 상장사는 담는다.** 우리가 안 보는 종목이라도 그 IP 가 뜨는 건
  섹터 온도이자 경쟁 지형이다. 커버리지는 강조색, 나머지는 회색으로 코드와 함께 병기하고
  표에 '커버리지 밖' 구분선을 넣는다.
- ⚠ **해외 상장사는 넣지 않는다** (2026-09-08 정리). 디시 마이너갤 순위로 넥슨(도쿄)·텐센트(홍콩)·
  닌텐도·MS·코나미를 따라가는 건 무리다 — 한국 커뮤니티 한 곳의 순위가 그 회사 실적·주가에
  닿는 경로가 없다. 한때 넣었더니 표의 절반이 해외 종목이라 정작 국내 종목이 안 읽혔다.
  블루 아카이브는 **넥슨게임즈(225570, 코스닥)가 개발사**라 그쪽으로만 남겼다.
  던파·메이플·마비노기·포켓몬·우마무스메·유희왕도 같은 이유로 뺐다(국내 상장 주체 없음).
- ⚠ **사전을 바꾸면 저장된 `co` 도 다시 판정해야 한다.** 옛 값이 남아 있으면 사전에서 뺀 뒤에도
  그대로라 정리가 안 된다(해외를 걷어낸 날 실제로 11개가 안 지워졌다).
  매 실행 이름으로 다시 보고, 안 걸리면 종목명으로 한 번 더 본다 —
  `DCGALL` 이 먼저 시드한 짧은 이름("제우스"·"컴프야")은 사전 키워드("제우스 오만"·
  "컴투스프로야구")에 안 걸리기 때문이다. 300위 목록에 있으면 **이름을 정식 갤 이름으로 맞춘다.**
- 사전은 `fetch_buzz.py` 의 `LISTED` **한 곳**뿐이고, `BUZZ.listed` 로 화면에 실어 보낸다.
  **화면에 사전을 또 두지 말 것** — 배지가 두 곳에서 갈라진다(옛 `BUZZ_ALIAS` 를 그래서 없앴다).
- 한 항목이 여러 상장사에 걸리는 게 정상이다 — **블루 아카이브 = 넥슨게임즈(개발) + 넥슨(퍼블리싱)**.
  값이 리스트인 이유다. 화면은 커버리지를 앞에 세운다.
- ⚠ **확실한 것만 넣는다.** 개발사·퍼블리셔·모회사가 갈리는 경우가 많아 짐작하면 틀린다.
  리센스·QWER·이세계아이돌은 소속사 확인이 안 돼 뺐고, 스텔라이브·미호요·쿠로게임즈·스마일게이트·
  하이퍼그리프는 비상장이라 뺐다. **틀린 배지는 없느니만 못하다.**
- ⚠ 사전을 넓히니 300개 중 **69개**가 걸렸다. 전부 400일씩 담으면 블록이 800KB 다.
  그래서 **보관 180일 + 비커버리지는 상위 60위에 든 갤만 추적 시작 + 총 80개 상한**으로 줄였다
  (`DAYS`·`NEW_RANK_CUT`·`MAX_GALLS`). 실측 33개·15KB.
  커버리지 종목은 순위와 무관하게 계속 담는다 — 권외로 밀린 것도 정보다.
- 추이 칸은 **디시 갤에만 있다**(순위 이력 hist). 검색어는 그날 등장 여부뿐이라 비는 게 정상이다.
- 2026-09-08 실측(해외 제거 후): 추적 갤 21개 · 커버리지 9종목 + 그 밖 2종목(네이버=치지직 5위,
  넥슨게임즈=블루 아카이브 8위). 화면에서 소스 접두어는 검색어(구글·네이트·나무)에만 붙인다 —
  대부분이 디시라 다 붙이면 같은 글자가 줄마다 반복된다.

## KT&G 유라시아 — 카자흐스탄 허브를 회사 밖 데이터로 잰다 (2026-09-21 편입 · 42번째 · `fetch_ktg.py` → `KTG`)

키움(9/21) 'KT&G 해외담배 질적 성장 (feat. 카자흐스탄)' 이 계기다. 리포트의 주장 — ESSE 초슬림이 알마티
브랜드 1위, 카자흐 점유율 5.1%('22)→9.6%('25) 3위(JTI 40%·PMI 30%대), 유통 커버리지 50→80%('27), 현지 생산분
40% 를 키르기스·러시아·우즈벡·타지크·아르메니아로 수출, 몽골(EAEU 임시협정 26.07), HnB 직접사업(올해) —
을 **회사 자료가 아닌 곳**에서 매일 잰다. `DATA` 에는 아로마티카처럼 **추적 전용**(score·fairMktcap 비움).

### 하루 만에 확인된 것 (2026-09-21 실측)

| 주장 | 독립 데이터 | 결과 |
|---|---|---|
| 현지생산이 수출을 대체 | 관세청 한국→카자흐 궐련(240220) | 2024 월 $0.3~0.6M → 2025 $0.05~0.13M → **2026 0**. 러시아·우즈벡·타지크도 2026 에 0 수렴 |
| 알마티 ESSE 브랜드 1위 | Elitalco(알마티 온라인 리테일) 누적 구매횟수 | **ESSE 6,133건 1위**(7 SKU) · Chapman 5,429(20 SKU) · Parliament 1,685 · Marlboro 901 · Winston 533. 단일 SKU 1위도 ESSE EXCHANGE W 3,664 |
| 유통 커버리지 확장 | hh.kz KT&G 판매법인 공고 8건 | 사바트 지원직이 **코스타나이·우랄스크**(북부·서부 신규 권역) |
| 몽골 | 관세청 한국→몽골 궐련 | 2026.1~8 $16.5M(2025 연간 $14.7M 이미 초과). KT&G 몽골 점유 50%+ 라 사실상 KT&G 계열 |
| HnB 직접사업 | Kaspi.kz(카자흐 최대 마켓) 기기 검색 | IQOS 기기 13 SKU·리뷰 760 vs **lil SOLID 0 SKU** — 아직 시작 전. lil 카드가 뜨는 날이 개시 신호 |

- 카자흐 산업 전체(통계청 → energyprom·DATA HUB 기사): 2025 생산 24.4십억개비(+36%), 26.1~5 수출 6.7십억(3.7배),
  1Q26 수출 $42.5M(+77% · 키르기스 57%·우즈벡·몽골), 수입 -63%(러시아산 중단). 세 공장(PMI·JTI·KT&G) 이 전부
  알마티주 일리구라 지역 통계로도 KT&G 만 못 가른다 — **산업 전체 맥락으로만 쓸 것.**
- 소매가: 법정 최저 970₸(26.01) → **1,060₸(26.08)** → 1,120₸(27.01). 담배소비세 18,051₸/1,000개비(26) → 25,607(30).
  ESSE 는 알마티 1,340~1,365₸(Winston 1,370 · Parliament 1,510 · Chapman 1,540), 서부(아티라우) 1,060₸ = 최저가.

### KT&G 해외 전체 — 숫자의 출처 지도 (2026-09-21 조사 · 카자흐 편입 뒤 확장)

회사 IR(2Q26 실적발표 p9·p12·p17 · 2025 사업보고서 p40·p48·p80~82)에서 뽑은 것. 다시 찾지 말 것.

- 해외궐련 판매량(억개비): 2022 493.6 · 2023 531.5 · 2024 586.5 · **2025 652.2** · 1Q26 180.1(+15%) · 2Q26 179.2(+7.3%).
  매출: 2025 1조8,775억(+29%, 국내 1조5,921억을 처음 추월) · 2Q26 5,577억(+18.9%) · 영업이익 +45.6%. ASP/개비 2Q26 31.1원(+11%).
- **권역별 수량 비중(2Q26)**: 중동·아프리카(MEA) **43.6%**(2Q25 31.3%) · 아태 **30.6%**(42.8%) · 미주·유럽·CIS 25.8%(25.9%).
  ⚠ 2Q26 성장은 **중동이 끌고 아태가 빠진 것**이다 — 비중×수량으로 환산하면 MEA 52→78억(+49%), **아태 71→55억(−23%)**, AME&CIS 43→46억(+7%).
  회사는 '전 권역 성장'이라고만 말한다. 아태(인도네시아)가 왜 줄었는지는 공개 자료에 없다 — 컨콜에서 물어야 할 질문.
- 글로벌 비중(수량): 57.0%(2023) → 59.5% → 62.5% → **65.1%(1H26)**. 해외 생산 비중 49%(2025) → 60%+(2028 목표).
- 해외 법인 실적(사업보고서 · 백만원 · 2025/2024): 인니 Trisakti 매출 727,185/504,425 순익 96,134/55,800 ·
  카자흐 판매 116,868/73,996 순익 17,970/10,752 · 카자흐 생산 63,406/– · 러시아 판매(Global Rus) 110,206/73,639 순손실 (2,847) ·
  러시아 생산(Rus) 133,634/103,705 **순손실 (17,821)** · 튀르키예 Tutun 110,545 순익 701 · 우즈벡 TAS 12,578 순손실 (1,127).
  러시아 법인 등기부(rusprofile)엔 Global Rus 2025 매출 234억 루블로 나온다 — 사업보고서 원화(1,102억)와 안 맞으니 **둘을 섞지 말 것.**
- 별도 담배 수출 2025 1조1,462억(+15%) · **반제품 수출 2,953억(+87%)** — 카자흐·인니 공장으로 나가는 원료가 급증(현지생산의 실물).
- **중동 허브 = Alokozay(두바이 제벨알리)**: ESSE 중동·CIS 독점 유통, 2020.03 계약 약 2.2~2.3조원, **2027-06-30 만료** — 갱신이 이벤트다.
  관세청 한국→UAE 궐련 2026.1~8 $259M(+56%) = 한국발 수출의 60%. 이란·이라크·아프간(→파키스탄 밀수 전력 2012)으로 재수출되는 그레이 채널이라
  전쟁·제재·환율에 흔들린다. 사업보고서엔 **KT&G Pars(이란 법인, 자산 6백만원·부채 533억)** 가 휴면 상태로 남아 있다.
- **인도네시아**: 2013 6천만 → 2024 95.8억개비(160배) · 점유 4.4% 4위(BAT·JTI 위) · Juara(2018) 2023 12.6억 · 수라바야 2·3공장 350억 캐파 26.02 가동 ·
  2025·2026 담배소비세 **동결**(의회 지지). 그런데 2Q26 아태 수량이 −23% — 소비세 동결 수혜와 어긋난다. 지켜볼 것.
- **몽골**: 2020 JTI 제치고 1위 → 50%+ · 2024 23.5억개비 · 한-몽 CEPA 원칙 타결(26.07.09) — 담배 관세 30% → 타르 11mg 미만 즉시 철폐.
  관세청 한국→몽골이 그대로 프록시(2026.1~8 $16.5M). 카자흐 발(EAEU 임시협정) 이 늘면 이 계열이 준다.
- **러시아**: 칼루가 공장(2010 · 46억 캐파) + Global Rus. 해외 NGP 의 핵심(3Q25 스틱 22.2억 +13%, 러시아 릴 솔리드 업그레이드) — 2Q26 22.2억(+0.5%) 정체.
  hh.ru 실측 11건: 블라디보스토크·치타·**도네츠크** 지역대표 + **예레반(아르메니아) 재무·경영지원 GM** — 아르메니아 거점 신설 흔적.
- **대만** 보헴(쿠바 시가잎) 2024 11억개비(대만 수출의 76%) · **인도** 2025.06 Kedara 와 진출, 8개 SKU, 위조 ESSE 단속 130건(수요의 방증) ·
  **유럽** 루마니아(24.04)→포르투갈·안도라·스페인 · 이스탄불 공항 면세(26.08, 5종) · **튀르키예 공장** 증설 120억 캐파(북아프리카·중남미 전초) ·
  **니코틴파우치** ASF(LOOP · 스웨덴/노르웨이/아이슬란드) 알트리아와 공동인수 2025.12 · 26.08 남아공 출시.
- **PMI 파트너십 Phase 2(2026~28)**: 최소 보증 160억본 → **110억본**으로 줄고 대신 KT&G 가 여러 플랫폼의 해외 직접 상업화 권리 확보 —
  한화(26.05): "아태·유라시아 권역부터 연내 직접 출시". 카자흐 HnB 직접사업이 그 첫 자리.
- **미국**: 2021.12 영업 중단. MSA 비참여 제조사 에스크로 약 1.54~1.63조원이 25년 뒤부터 반환 — 2026부터 반환 개시 구간(현금 이벤트).
- ⚠ 관세청 한국발 수출은 **KT&G 만이 아니다.** BAT코리아 사천공장(연 260억개비, 70% 수출 · 일본·대만·중국·싱가포르)이 섞인다.
  일본 $146M→$54M→$5.6M 급감은 BAT 쪽 사정으로 봐야 한다. UAE·나이지리아·벨리즈·몽골·그레나다·아루바는 KT&G 계열로 읽어도 된다.

### 네 소스와 함정

| 소스 | 무엇 | ⚠ |
|---|---|---|
| 관세청 nitemtrade 2402·2403 | 국가별 월별 수출(천$). KZ·RU·UZ·KG·TJ·AM·MN | 조회기간 1년 이내만 → 연도별 창. 240399 에 가열담배 스틱이 섞인다(러시아 월 $0.3~2M) |
| Elitalco `newelitalco.kz/ru/catalog/cigarettes/?page=N` | 75 SKU · JSON-LD(이름·브랜드·가격) + 본문 "N раз"(누적 구매) | **누적**이라 일별 증분이 신호. 가격 '1' = 미표시. 한 도시 한 리테일러. 온라인 담배 판매는 불법이라 언제든 닫힌다 |
| Kaspi `/yml/product-view/pl/results?text=…&c=750000000` | 기기 카드 reviewsQuantity·unitPrice | 스틱·궐련은 안 판다. 액세서리(스틱니차·패널·케이스)를 걸러야 한다 |
| hh.kz `search/vacancy?employer_id=9856755(판매)·10258777(생산)` · hh.ru `employer_id=2746402`(러시아 판매법인) | 공고 수·제목·도시 | `api.hh.ru` 는 이 IP 403 — HTML 만. 생산법인 주소 село Кокозек = 공장. 러시아는 보조(막히면 생략) |

- 다른 알마티 리테일러(vkusmart 1,210₸·astanaalko·sherlock 서부 1,060₸)는 구매수가 없어 가격 참고만. tabak-vostoka 엔 ESSE 없음.
- Wolt/Glovo IQOS 매장(kz.iqos.com 이 안내)은 JS 렌더·공개 API 없음. 카자흐 통계청 Taldau 는 3,699개 지표 중
  담배 생산 단독 지표가 없다(산업생산 사전 안) — 크롤 결과 `Temp/taldau_idx.json`.

### 검색 트렌드 — 'esse' 는 되고, 우즈벡 'esse' 는 에세이다

- 구글 토픽 ID(`/m/0nbct0r` ESSE) 는 카자흐에서 임계 미만(5/54주). **문자열 'esse'** 는 KZ·RU 모두 연관검색어가
  담배 변종(exchange·change·mango·himalaya·blue·one)이라 쓴다(KZ 5년 주별 199/262 유효).
- ⚠ **우즈벡 'esse' = 에세이**(9월 개학에 4→36 급등). 구글로 못 잡는다 → 얀덱스 `сигареты esse` 지역 171.
- 얀덱스 Wordstat 에 **지역 필터**를 붙였다(`fetch_yandex(regions=[...])` · geos 항목 `region` · 얀덱스 그룹 `yregion`).
  geobase: 카자흐 159 · 러시아 225 · 우즈벡 171 · 키르기스 207 · 타지크 209 · 아르메니아 168.
  '카자흐 담배 브랜드(얀덱스)' 는 ESSE·Winston·Parliament·Kent·Chapman 을 **절대 검색수**로 견준다.
  ⚠ 처음 켤 때 count 크기가 나라 규모와 맞는지 볼 것 — 틀린 ID 는 400 이 아니라 조용히 빈 값이 온다.
- 몽골은 구글 4/54주라 검색으론 안 보인다 — 관세청 몽골 계열이 몽골 지표다.
- 이 PC 는 구글 트렌드 429 에 걸린다(탐침 40회). 새 그룹은 러너의 05시 전체수집이 채운다.

## 영화 흥행 탭 — 치이카와 + 흥행작 (2026-09-26 되살림 · `fetch_boxoffice.py` → `BOXOFFICE` · `renderBoxTab`)

사용자 지시로 **대원미디어(048910)를 엔터·기획에 추적 전용으로 편입**(44번째)하고, 그 회사가 수입한
**극장판 치이카와: 인어 섬의 비밀**(9/30 개봉 · 배급 CJ ENM)을 같은 시기 흥행작 3편과 견주는 **전용 탭 `#boxoffice`**
를 되살렸다("옛날처럼 영화흥행 탭을 만들고 비교"). 하츄핑 수집기(`MOVIE`·`MOVIE_SCREENS`)는 그 영화에 맞춰
짜여 있어 건드리지 않고, **영화 목록(`FILMS`)만 바꿔 끼우는 범용 수집기**를 새로 만들었다.

| 소스 | 무엇 | 어디서 |
|---|---|---|
| KOBIS 일별(웹 통계 `findDailyBoxOfficeList.do`) | **그날 상영된 전 영화**(9/25 실측 98편) · 키 불필요 · 전 영화 합 = 시장 총량(`market`) | 러너 매 회차 |
| KOBIS 실시간 예매(`findRealTicketList.do`) | 예매 순위·예매율·예매관객 | 러너 매 회차 |
| 3사 좌석(`--seats`) | CGV·롯데·메가 회차별 판매석 — **예매가 열린 상영일 전부**(오늘~14일 뒤 탐침) · 요청 함수는 `fetch_screens` 것을 import | **서버 `boxseats.yml`** 하루 2번 |
| 하츄핑2 기준선(`ref`) | `archive/booking.jsonl`·`screens.jsonl` 에서 개봉 전 예매·개봉일 좌석 | 수집기가 매번 다시 읽음 |

- **영화는 KOBIS 영화코드로 고정**한다(치이카와 20261807 · 암살자(들) 20255033 · 타짜 20256161 · 오디세이 20250654).
  체인 매칭은 정규화 제목 키워드 — 메가는 괄호를 `&#40;` 로 주고, CGV 는 `타짜-벨제붑의 노래` 로 준다.
  `오디세이` 는 '스페이스 오디세이' 재개봉을 걸러야 해서 `not` 을 둔다.
- ⚠ **암살자(들) 배급 하이브미디어코프는 하이브(HYBE)와 무관한 영화사다.** 배지·뉴스 매칭에 섞지 말 것.
- ⚠ OpenAPI(키 필요)는 Top10 만 준다 — 하츄핑 기록에 구멍이 있는 이유. 웹 통계는 순위 밖도 준다.
- ⚠ 예매관객은 '남은 상영분'이라 하루 중에도 준다. 하츄핑2 대비표는 **날마다 15시 전후 스냅샷**끼리 잇는다.
- 추적 기한 `UNTIL`(2026-11-15)이 지나면 수집기가 스스로 멈추고, watchdog·신선도 보드는 블록의 `until` 을 보고 판정에서 뺀다
  (하츄핑 때처럼 끝난 영화를 손으로 LIMITS 에서 지우지 않아도 된다).
### 좌석은 '열린 날짜 전부 · 날마다' — 서버에서 (2026-09-26 사용자 지시)

"예전처럼 좌석을 일별로, 열려 있는 거 전부". PC 작업 스케줄러 안은 **사용자가 거절**했다("서버에서 해 왔던 방식으로").
그래서 `boxseats.yml` 이 시세 워크플로에 얹혀 평일 12·18시 · 주말 12·20시(+cron 20:40 백업)에 돈다.

- **탐침 → 전수.** 날짜마다 체인별 대형 지점 몇 곳(CGV 강변·용산·영등포·왕십리 · 롯데 월드타워·건대입구·김포공항·수원 ·
  메가 강남·코엑스)만 찔러 대상 영화가 걸린 (날짜, 체인)만 전수한다. 첫 실측(9/26): 9/26~10/6 + 10/10 이 열려 있었고
  날짜마다 연 체인이 달랐다(10/5·10/6 CGV 만 · 10/10 롯데·메가만).
- **두 잡.** 수집(20~30분)은 repo-write 그룹 **밖**에서 아티팩트로만, 커밋 잡만 그룹 안에서 원격 최신 위에 `--merge` 로 끼운다.
  긴 수집을 그룹 안에 두면 시세 회차가 줄을 서고 GitHub 이 대기 회차를 취소한다.
- ⚠ **러너에선 CGV 403 · 메가 간헐 시간초과**(diag 2026-09-26). 대개 롯데(+메가) 기준으로 쌓인다.
  빠진 체인을 직전 값으로 이어받지 **않는다** — 점마다 체인별 판매·총좌석·스크린을 남기고,
  화면이 **두 수집에 다 있는 체인끼리만** 증감을 계산한다(`common`/`sumBy` in `renderBoxTab`).
  이어받기를 하면 한국 IP 에서 받은 CGV 값이 서버 회차마다 복사돼 굳은 숫자가 된다.
- 탐침이 이틀 연속 전부 실패한 체인은 그 회차에서 바로 포기한다(막힌 체인에 20분씩 헛돌지 않게).
- 수집일마다 1점(같은 날 여러 번이면 마지막 값) · (영화, 상영일)당 30점 · 지난 상영일은 10일까지 화면 블록, 그 뒤는
  `archive/boxoffice_seats.jsonl`(append 전용 — 다음 극장판의 기준선)에만.
- 한국 IP(이 PC)에서 `python fetch_boxoffice.py --seats` 를 손으로 돌리면 3사가 다 잡힌다 — 개봉일 같은 날 한 번씩.
- 화면: **상영일 × 영화 최신 판매석**(체인 오픈 여부·전일 대비) + **영화별 상영일 × 수집일 채움 추이**.

- 대원미디어 트렌드 탭에도 `극장 일별 관객`·`극장 예매관객(10시 기준)` 두 계열을 주입한다(`injectBoxofficeTrends`).
  상세 창에는 '🎬 치이카와 흥행 보기' 버튼이 탭으로 보낸다.
- 첫 실측(9/26 15시 · D-4): 치이카와 예매 2위 19.0% · 129,727명(하츄핑2 D-1 최고 102,208명의 1.27배) ·
  **개봉일 3사 좌석 100,899석/496스크린(판매율 42%)** = 하츄핑2 D-1 41,693석의 2.42배. 개봉 전 유료 시사 누적 21,303명.
  같은 날 9/30 좌석은 암살자(들) 7,704석 · 타짜 2,101석(개봉 1주차 평일).

## 극장 흥행은 트렌드 탭 안으로 들어갔다 (2026-09-07) · **수집 중단 (2026-09-21)**

> 2026-09-26: 전용 탭 `#boxoffice` 는 위 '영화 흥행 탭'으로 되살아났다. 아래는 하츄핑(SAMG) 보관 기록 이야기다 —
> 트렌드 탭 SAMG 섹션은 그대로 두었고, 새 탭에서 하츄핑1·2를 비교선으로 켤 수 있다.

> ⏸ **영화 수집(`MOVIE`·`MOVIE_SCREENS`)은 2026-09-21 자동 실행에서 뺐다.**
> 하츄핑2 가 꼬리 구간이라 3사 순회(회당 20~30분)가 값에 비해 비쌌고, 이미 반쪽이었다 —
> 메가는 8/30 이후 미편성, CGV 는 9/13 이후 러너에서 403(예매석의 55~60%).
> **CGV 는 죽은 게 아니라 러너 IP 문제다** — 같은 요청이 이 PC 에선 200 이다.
> 스크립트·워크플로·데이터는 그대로 두고 트리거만 껐다. 되살리는 법과 3사 API 규격·
> 탐침 대안은 **`영화수집_방식.md`** 에 있다. 되살릴 땐 그 문서의 되돌림 목록부터 볼 것.


전용 탭(`#boxoffice`)을 없애고 트렌드 탭의 `#movieSec` 으로 접었다. 하츄핑2(8/5 개봉)가 한 달
지나 탭 하나를 차지할 무게가 아니게 됐고, SAMG 를 볼 때 트렌드와 흥행을 두 탭에서 오가는 것도
이상했다. **게임 종목의 `geSec` 과 같은 방식** — `MOVIE.movies[].stock` 에 있는 종목을 고를 때만 뜬다.

- 예매·스크린(개봉 전 선행지표)과 일별 상세 표는 `<details class="fold">` 로 접었다.
  예매·스크린은 **개봉 전이면 펼친 채로** 뜬다(`days.length<=3` 판정).
- `#boxoffice` 로 들어오는 옛 링크는 `activateTab` 이 트렌드+SAMG 로 돌려보낸다. 지우지 말 것.
- 갱신 시각은 섹션 제목(`#movieSecTitle`)에 적는다 — KOBIS 는 주기가 달라(2시간) 탭 헤더로 못 뭉친다.
- 랜딩 팝업 IIFE(`samgOverlay`, 154줄)는 index.html 에 그 요소가 없어 죽어 있었다 — 2026-09-07 제거했다.

## 디시 갤러리 글 양 — 글번호 차분 (2026-09-02 · fetch_dc.py → `DCGALL`)

커뮤니티 글 양 = 관심의 온도. 검색 트렌드보다 거칠지만 훨씬 빠르다(게임은 런칭·사건이 그날 찍힌다).
**갤러리 안에서 글번호는 연속 증가한다(삭제돼도 번호 소비)** → 하루 사이 최대 글번호 차이 = 그날 글 수.
목록 1페이지(50글)면 충분하고 urllib 로 차단 없이 열린다(간격 0.5s, 갤당 하루 1요청).

| 종목 | 갤러리 id | 하루 글 수(9/2) |
|---|---|---|
| 컴투스 | `zeusthegodofpride`(제우스 오만의 신) · `cpyv22`(컴프야 V26) | 2,700 (8/30~31 정점 5,500) · 1,300 |
| NC | `aion2` · `lineage2classic`(리니지 클래식) | 3,600 · 600 |
| 탑코미디어 | `toptoon` · `toptoonchat`(신설, 339글) | 50 · ~1 |

- ⚠ **id 를 이름으로 짐작하지 말 것.** `zeus` = 최우제 갤, `com2usbaseball` 은 옛 갤(현역은 `cpyv22`),
  정식 리니지갤(`board/lineage`)은 7월 말 이후 글이 없다. 제목을 `title` 에 같이 저장해 검증한다.
- ⚠ 목록 1페이지에도 **오래된 개념글·공지가 섞인다**(리니지갤 마지막 행이 2005년). 날짜는 반드시
  **같은 행의 글번호와 짝지어** 읽는다(`fetch_page` 가 행 단위 파싱).
- **백필은 페이지 샘플링** — 1·3·6·…·6,400 페이지의 (글번호, 날짜)를 앵커(`x:true`)로 두고 화면이 선형 보간한다.
  '구간 평균'이지 정확한 일별치가 아니다. 사이트 특성상 아이온2는 6월 중순, 리클 4월, 컴프야 3월까지.
  매일 05시에 쌓이는 점(x 없음)부터가 정확하다. 페이지 범위를 늘렸으면 `--backfill` 로 다시 샘플링.
- 서머너즈워 갤들은 전부 죽어 있어(마지막 글 2024~2026 산발) 넣지 않았다.
- ⚠ **갤 이름은 게임 이름과 다를 수 있다.** 아스트라에 오라티오(NC 신작)는 후보가 넷이었다 —
  개발사 이름을 딴 `dynamisone`(디나미스원), 제목을 딴 `astraeoratio`, 배경을 딴 `astora`(89 도쿄),
  미니갤 `astraemini`. 2026-09-18 실측으로 **`astraeoratio` 만 현역**이다(9,019글 · 나머지는 4~2,468글에
  1페이지가 2024년까지). **글 수와 1페이지가 덮는 기간을 같이 보고 고를 것.**
- **순위(BUZZ)도 트렌드 계열이다** — `디시 갤 순위(종목)`. 글 수가 '양'이면 순위는
  '전체 마이너갤 대비 상대 온도'다. 둘이 갈리면 판 전체가 움직인 것이다.
  ⚠ 커버리지 종목만 그룹으로 만든다. **비커버리지 갤도 `stock` 필드가 채워져 있으니**
  커버리지 판정은 `co[3]` 으로 할 것(그냥 `stock` 을 보면 넥슨게임즈·네이버까지 종목 트렌드가 생긴다).

- ⚠ **volume 은 원화가 아니다.** 원화로 읽었더니 아이온2 하루 9,040억이 나왔다. 원화 = 수량 × 캔들 종가 ÷ 단위수량
  (`defaultGameUnit` '1만다이아'→1e4 · '1천만키나'→1e7 · 'ADENA'→1e4 · '1천다이아'→1e3, `unit_qty()`).
  서버 JSON 거래내역 합(제우스 8서버 7,130만원)과 맞아떨어진다. 9/1 실측: 제우스 1.6억 · 아이온2 3.8억 · 리클 10.6억/일.

- 사이트가 **~2주치만** 들고 있다(제우스 8/27~, 아이온 8/19~). 첫 수집 때 그만큼 백필, 이후 매일 쌓는다.
- 전 서버를 다 받는다(제우스 30·아이온2 44·리클 31). 리니지M 은 280서버라 **앞 20서버 표본으로 고정**(`sample`).
- ⚠ 단위가 게임마다 다르고('1만다이아/W'·'1천만키나/W'…) 아이템매니아 '게임머니'와 같은 물건이 아닐 수 있다.
  **두 소스 가격을 직접 비교하지 말 것** — 방향만 겹쳐 본다. 실측 제우스: 게임비트 99,613원/1만다이아 vs 매니아 8,082원/1,000머니.
- 오늘 행은 `partial:true`(수집 시각까지) — 판단은 전일까지.
- 다른 후보도 봤다: EnchantLab(리니지클래식 전용, 3거래소 합산·매물 수·7일), 게임마켓, 아이템베이(코드 파싱 미완).

## '변동 없음' 비교는 반드시 원본을 복사해서 (2026-09-01)

수집기 대부분이 끝에 이런 검사를 한다 — 값이 그대로면 파일을 안 건드려 Cloudflare 빌드를 아낀다.

```python
old   = json.loads(블록)
sites = {s["code"]: s for s in old["sites"]}   # ← 얕은 참조. 여기가 함정
sites[code]["hist"] = ...                       # old 까지 같이 바뀐다
if old == out: return                           # 자기 자신과 비교 → 무조건 SKIP
```

`fetch_toptoonchat.py` 가 이걸로 **하루치에 멈춘 채 이틀을 보냈다.** 워크플로는 계속 초록불이었고
`index.html` 도 매 회차 커밋됐다 — 그 블록만 안 바뀌었을 뿐이라 아무도 몰랐다.
도입 첫날만 블록이 없어 비교를 안 타서 저장됐고, 그 뒤 전 회차가 조용히 건너뛰었다.

**old 에서 꺼낸 객체를 제자리 수정할 거면 `copy.deepcopy` 로 원본을 보존할 것.**
(`fetch_appstore.py` 처럼 비교 시점에 상수를 **다시 파싱**하는 방식도 안전하다.)

같이 고친 것: `events.yml` 이 `health.json` 을 커밋하지 않아 `runstep` 의 실패 기록이
원격에 안 올라갔다. **새 워크플로에도 `health.json`·`watchdog_state.json` 을 `git add` 에 넣을 것.**

## 빙그레 (2026-09-08 추가 · 종목 40번째)

**2026-04-01 해태아이스크림을 흡수합병했다**(존속법인 빙그레 · 2020년 인수 후 100% 자회사였던 것을 합병).
'자회사'라고 쓰인 옛 자료를 그대로 믿지 말 것 — 이제 해태 물량은 빙그레 실적·수출에 그대로 들어온다.

공장: **남양주(도농 1·2공장, 본사)** · 경기 광주 · 충남 논산 · 경남 김해 (+합병으로 해태 공장).

### 수출 — 지역 프록시가 이례적으로 깨끗하다

빙과(210500)는 롯데웰푸드·해태도 있는 품목이라 전국 합계는 빙그레 프록시가 될 수 없다.
그런데 시군구로 쪼개니 **남양주 한 곳이 85.9%** 였다(26.1~6 누적 $50.1M/$58.3M · 2위 전남 화순 2.8%).
논산·김해·광주 공장 몫도 남양주(본사)로 신고되는 것으로 보인다. 그래서 **남양주 = 빙그레 프록시**다.

| 계열 | HS | 무엇 |
|---|---|---|
| 빙과 | `210500` | 아이스크림·빙과류 **전국**(업계 합계). 6자리 밑에 세분 없음 |
| 가공유 | `0402999000` | 기타 가공유류 = 바나나맛우유 등. 6자리(040299)로 부르면 가당연유가 섞인다 |
| 빙과(남양주) | `210500` 시군구 | **빙그레 몫에 가장 가까움** |
| 바나나맛우유(남양주) | `040299` 시군구 | 전국 040299 의 ~90% |

26.07 실측: 빙과 전국 $10.2M(미국 2.2 · 유럽9 1.2 · 중국 0.7) · 남양주 $5.7M ·
가공유 전국 $3.7M(**미국 1.8 = 절반**) · 남양주 $3.5M.
⚠ **빙과는 계절성이 매우 크다** — 26년 월별 $6.0M(2월)~$16.2M(7월). 전년동월과 비교할 것.

### 트렌드 — 'Melona' 는 되고 'Banana Milk' 는 안 된다

- `빙그레 IP`(네이버 일별 30) — 바나나맛우유·메로나·붕어싸만코·투게더. 국내 실신호.
- `메로나 국가별`(구글 **주별** 26주) — 한국(네이버)·미국·브라질·캐나다·일본·전세계.
  ⚠ **일별로 잡으면 0 이 줄줄이 찍힌다**(쿨로아600 에서 겪은 것). 빙과는 계절성이 커서 6개월은 봐야 한다.
  2026-09-08 구글 실측(93일): 전세계 93/93 · 캐나다 34/93 · 브라질 15/93 → 주별이 맞다.
- `빙그레 브랜드(해외)`(구글 주별) — Binggrae·Melona. 실측 'Binggrae' 전세계 79/93 · 미국 16/93.
- `바나나맛우유 국가별`(구글 주별) — 미국·캐나다·필리핀·베트남·일본(バナナ牛乳)·전세계(Binggrae Banana Milk).
  'Banana Milk' 는 일반명사라 처음엔 뺐다가, **미국에서 이 검색은 사실상 빙그레 현상('Korean banana milk')**
  이라는 애널리스트 판단으로 넣었다. Mooala 등 일반 바나나우유가 섞이므로 절대 크기가 아니라
  **추이·급등 시점**을 보는 용도다. 메로나 그룹에는 섞지 않는다(스케일이 다르다).
- ⚠ 구글 계열은 `--naver-only` 회차에 안 채워진다 → **주 1회 전체수집 뒤에야 뜬다.**
  `topicsOf` 가 없는 그룹을 걸러내므로 그때까진 '빙그레 IP' 만 보인다(오류 아님).

## 제닉 — 수출은 공장(논산)이 아니라 고객사 본사(강남)로 잡힌다 (2026-09-09)

제닉은 마스크팩 ODM 인데 **논산 330790 은 25~26 누적 $1.7M 뿐**이다. 물량은 주 고객
**바이오던스(강남구 본사)** 명의로 신고된다 — 강남구 330790 이 **전국 시군구 1위($304M · 2위 송파 $190M)**.
그래서 `REGION_ITEMS` 에 `마스크팩 전체(강남)` 을 두고 제닉 프록시로 쓴다.

- ⚠ 시군구 API 는 **6자리만** 받는다(10자리·4자리는 "6자리로 입력" 오류) → 330790 = '마스크팩 전체' 기준이지 3307904000 이 아니다.
- ⚠ 강남에는 다른 화장품 본사도 많다(에이피알 등). **'강남 발 마스크팩' 전체**를 바이오던스·제닉 프록시로 읽는 것이지 순수 제닉 몫이 아니다.
  화면 문구에서 이 단서를 빼면 안 된다.
- 26.04 $41.3M 단독 급등(전후 $19·15M) — 제닉 2Q26 매출 492억(1Q 294억)과 방향이 같다. 26.07 $15.2M(전국의 13%).
- **'제조지 기준' 가정을 종목마다 다시 확인할 것.** 파마리서치(강릉)·티앤엘(안성)은 공장으로 잡히고,
  빙그레(남양주)·제닉(강남)은 본사로 잡힌다. 새 지역 프록시를 만들 땐 공장 시군구와 본사 시군구를 **둘 다 찍어 보고** 큰 쪽을 쓴다.
- 지역 프록시는 이제 **트렌드 탭에도** 뜬다(`injectTradeRegionTrends`). 종목·전국 짝은 `fetch_trade.py` 의 `REGION_META`
  한 곳에서 오고(`item.stock`/`item.natl`), 같은 HS 의 전국 계열이 있으면 나란히 그려 '전국 비중'을 요약에 적는다.
  두 계열은 크기가 달라 각자 정규화하고 실액은 `rawSer`(만달러)로 툴팁에 찍는다.

## 달바 러시아 — 구글로는 안 보인다, 얀덱스가 창이다 (2026-09-21 신설)

달바글로벌은 러시아가 실질 시장인데 지표가 **일본(라쿠텐)뿐**이었다. 러시아는 얀덱스 점유가
구글보다 높아 구글 트렌드로는 신호가 얕고, 무엇보다 **구글은 그룹 안 상대값이라 '누가 더 큰가'를
못 본다.** 얀덱스 Wordstat 은 절대 검색수를 주므로 경쟁 K-뷰티와 크기를 진짜로 견준다.

| 그룹 | 계열 | 붙는 종목 |
|---|---|---|
| `러시아 K-뷰티(얀덱스)` | d'alba · medicube · round lab · cosrx · laneige · beauty of joseon | 달바글로벌 · 에이피알 · 아모레퍼시픽 |
| `달바 러시아 제품군(얀덱스)` | 화이트트러플 · 워터풀 · 패치 · 선케어 · 크림 | 달바글로벌 |

### 표기 — 아포스트로피는 무시된다. `d'alba` = `d alba`

두 표기가 **완전히 같은 값**이고(주 4,165), 띄어쓴 쪽이 붙여쓴 `dalba`(1,468)의 **3배**다.
`alba` 단독은 13,777 이지만 연관어가 소프라노·망고·플라밍고 — 이탈리아 지명·일반명사다.
그런데 **`d alba` 는 오염되지 않았다.** `topRequests` 상위가 근거다(2026-09-21 실측):

```
d alba white truffle 3,043 · serum 2,760 · cream 2,075 · отзывы 1,698 · патчи 1,383 · waterfull 1,096
```

전부 달바 제품이다. 아로마티카(`vela aromatica`=향초)·달바 구글(`dalba`=이탈리아 Alba)에서
두 번 밟은 일반명사 함정을 이 목록으로 가렸다. **새 브랜드 키워드는 `topRequests` 를 먼저 볼 것** —
`associations` 는 '같이 검색된 것'이 아니라 **철자가 비슷한 것**(далбек·dalbek·вальб)을 주므로
오염 판정에 쓰면 안 된다. ⚠ `topRequests` 는 `numPhrases` 가 필수다(없으면 400).

### 검색 1위와 판매 1위가 정반대다 — 같은 축에서 읽지 말 것

| | 얀덱스 검색(주) | 와일드베리즈 리뷰 누적 |
|---|---|---|
| 메디큐브(에이피알) | **13,570** | 454 |
| 달바 | 4,165 | **25,552** |
| 라운드랩 | 5,784 | 18,761 |
| 코스알엑스 | 2,326 | 3,586 |

모순이 아니다. 메디큐브 러시아 검색은 거의 전부 **부스터프로 디바이스**다
(`medicube booster pro` 17,816 · `микротоковый массажер` 15,359) — 고가 기기라 마켓플레이스
상위 100 에 리뷰가 안 쌓인다. 달바는 저가 스킨케어라 반대다. **검색은 유량(지금 관심),
리뷰는 스톡(누적 판매)** 이고 채널도 다르다. 어느 쪽이 '이긴다'고 읽으면 틀린다.

- SHOP(와일드베리즈)에 달바·메디큐브와 경쟁(라운드랩·코스알엑스)을 넣었다. ⚠ `sort=popular`
  **상위 100개의 리뷰합**이지 브랜드 전량이 아니다. 같은 잣대로 매주 재는 데 의미가 있다.
- ⚠ 와일드베리즈는 **429 가 잦다.** 예전 `fetch_shop.py` 는 성공한 회차에만 쉬고 실패하면 곧장
  다음으로 넘어가, 대상이 4→8개로 늘자 **뒤 5건이 통째로 빠졌다.** 지금은 실패 경로에도 간격을
  두고 429 를 백오프로 되받는다. 요청 간격은 5초.
- 지역: 러시아 225 만 쓴다. 카자흐 55 · 벨라루스 63 · 우즈벡 2(주당) 로 CIS 는 노이즈다.
- 제품군 계열은 **구문끼리 포함관계**가 있다(`white truffle serum` 은 양쪽에 잡힌다).
  계열 합이 브랜드 총량이 아니다 — 라인별 추이를 보는 그래프다.

### 새 그룹만 채우는 법 — `--only` + `trend_only.txt`

얀덱스 키는 **Actions secrets 에만** 있어 로컬에서 못 돌린다. 그렇다고 19그룹을 전부 다시 받으면
느리고 구글 429 를 부른다. 그래서:

```
trend_only.txt 에 그룹 이름을 한 줄씩 적고 push  →  ytrend.yml 이 그 그룹만 수집·커밋
```

`fetch_trends.py --only=<이름조각>` 은 나머지 그룹을 **기존값 그대로 옮긴다**(덮어쓰지 않는다).
⚠ `asOfNaver`·`asOfFull` 도 이전 값을 유지한다 — 한 그룹만 받아 놓고 전체 시각을 갱신하면
국내·구글 계열이 신선한 것처럼 보인다.

키워드 탐침은 `probe_yandex.py`(→ `yprobe.yml` → `diag/yandex_probe.json`). 표기·크기·지역·
연관어를 한 번에 찍는다. **새 브랜드를 얀덱스에 올리기 전에 이걸 먼저 돌릴 것.**

## TS트릴리온 — 미국은 검색이 아니라 아마존으로 본다 (2026-09-23 편입 · 43번째 · 317240)

탈모샴푸 'TS샴푸' + 청소년 샴푸 'GD'(남·여 라인 · 재구매율 95% · 국내 매출의 ~25%). 홈쇼핑 비중 80%('22) → 9%(1H26),
올리브영·다이소·자사몰·해외로 옮겼다. 1H26 매출 151억(+17.8%)·영업이익 11.8억(약 16배). 청담글로벌과 5년 410억 공급계약(26.02),
**9/22 미국 미니소 200곳 GD 입점**(다음 후보: 캐나다 미니소·H마트·샘스클럽·TJ맥스, 캐나다 월마트). `DATA` 는 추적 전용.

| 그룹 | 계열 | 첫 실측(2026-09-23) |
|---|---|---|
| `탈모샴푸 브랜드`(네이버 주별 52) | TS샴푸·닥터포헤어·려샴푸(아모레)·닥터그루트(LG생건) | TS샴푸 59→45 하락, 닥터포헤어(1월 정점 100)에 밀림 |
| `청소년 샴푸(GD)`(네이버 주별 52) | GD샴푸·청소년샴푸·초등학생샴푸 | GD 14~37 박스권, 3월 개학 54~55 · 9/14 36 |
| `TS샴푸 국가별`(주별 26) | 한국(네이버)·미국·전세계(구글 'TS shampoo') | 미국 3/23~7/6 신호(정점 4/13) → **7/13 주부터 0** |

- ⚠ **구글 미국은 거의 임계 미만이다.** 'GD shampoo' US 52주 중 2주, 토픽 ID(`/g/11fcpqdkw_`·`/g/11y0525z81`·`/g/11c5zdw8x7`)는
  전부 0 이라 못 쓴다. 전세계 'TS shampoo' 는 지역 1위가 한국(100)·미국 66 이라 **한국의 영문 검색**이 섞인다. 대만은 검색량 부족.
  'TS'·'GD' 는 흔한 약어라(`taramps ts` 앰프가 관련검색에 뜬다) 절대 크기로 읽지 말 것.
- **미국 실판매는 아마존**: 스토어 표기 `TS SHAMPOO`, 제목이 전부 'TS …' 로 시작 → `inject_amazon.BRAND_STOCK` 키는 **`TS`**
  ('TS Shampoo' 로 두면 'TS GD Teen Shampoo' 가 안 걸린다). 9/23 실측: GD 청소년 샴푸 리뷰 222·월 200개+·샴푸 1,010위(뷰티 36,630위) ·
  TS 샴푸 플러스 리뷰 319 · New TS 쿨 70. 베스트셀러 100 과는 거리가 멀어 브랜드 검색(`search.always`)으로만 잡힌다.
- 올리브영 브랜드 표기는 'TS'·'티에스' 로 넣었다(베스트100 진입 시 잡힘) · 사람인 'TS트릴리온'.

## 아로마티카 — 구글 'Aromatica' 는 브랜드가 아니라 향초다 (2026-09-14 편입 · 41번째)

2025-11-27 코스닥 상장(`0015N0`). 비건·클린뷰티 자사 브랜드, **로즈마리 두피케어가 주력**이고
북미가 실질 시장이다. 25년 매출 512억·영업이익 51억 → 26E 686억/95억(네이버 컨센).
`DATA` 에는 **추적 전용**으로 넣었다 — `score`·`fairMktcap` 을 비웠고, 견적은 지시가 오면 채운다.

- ⚠ **구글 트렌드에 'Aromatica' 를 문자열로 넣지 말 것.** 스페인어·이탈리아어 일반명사다
  (`vela aromatica` = 향초). 실측: 관련검색 1·2위가 vela / vela aromatica, 지역 1위가
  콜롬비아(100) · 브라질(30) · 이탈리아(20) — 브랜드 신호가 아니다.
  달바를 `dalba` 로 잡았다가 다른 것을 세고 있던 것과 **같은 함정**이다.
  → **구글 토픽 ID `/g/11dxd51wxs`** 로 잡는다(지역 1위 한국, 나머지가 동남아 = 브랜드).
  같은 이름의 토픽 `/g/11yzw55xwn` 은 체코 중심의 다른 것이니 쓰지 말 것.
  토픽 ID 는 화면에 그대로 찍히면 못 읽으므로 **`glabels`** 로 표시 이름을 준다
  (2026-09-14 신설 · 일반 그룹 전용 · 구글 계열 보존 비교도 이 라벨 기준으로 바뀌었다).
- 국가별은 한국(네이버)·미국·일본·전세계 **넷만**. 동남아는 지금 넣으면 0 이 줄줄이 찍힌다
  (0 아닌 주: 싱가포르 9/52 · 베트남 9 · 홍콩 4 · 말레이시아 3 · 태국 2). 신호가 붙으면 그때.
- 국내 비교는 `K-뷰티 브랜드` 그룹에 합류시켰다 — 네이버 30일 실측 메디큐브 100 ·
  **아로마티카 51** · 달바 34 · 라운드랩 8 로 자릿수가 같아 한 그래프에서 읽힌다.
- **아마존 — 리스트에 없다고 안 파는 게 아니다.** 2026-09-14 미국 실측: 로즈마리 루트 인핸서
  리뷰 4.5천·지난달 9천개+, 스칼프 스케일링 샴푸 리뷰 1.8천·1천개+, 두피 스크럽 1.9천·900개+.
  그런데 **Beauty 톱100 은 아니다.** 트래커의 `search.brands: auto` 는 '이미 추적 중인 브랜드'만
  검색하고 추적은 **리스트 진입으로만** 시작되므로, 그냥 두면 이 브랜드는 영영 0 으로 남는다.
  → `inject_amazon.BRAND_STOCK` 을 단일 출처로 삼아 ① 추적 목록(`merge_coverage_brands`)과
  ② 브랜드 검색 목록(`seed_search_always` → `search.always`)에 **자동으로 합친다.**
  ⚠ `config.yaml` 은 .gitignore 이고 **트래커가 도는 PC 에만 있다** — 저장소 파일만 고쳐서는
  그 PC 의 목록이 안 바뀐다. 그래서 커버리지 종목 브랜드는 설정이 아니라 코드로 보장한다.
- 올리브영(`fetch_beauty.BRAND_STOCK`)·사람인(`fetch_jobs.COS`)에도 넣었다. 올리브영 베스트100
  에는 2026-09-11 기준 아직 없다 — 들어오면 그날부터 잡힌다.

## 수출 HS 부호 — 품목명으로 고를 것 (2026-08-01)

**부호는 반드시 관세청 품목명(`statKor`)으로 고른다. 금액이 맞는지로 고르면 틀린다.**
실제로 세 번 틀렸다. 증권사 표와 숫자가 맞아떨어져서 맞다고 믿었는데,
그건 그쪽이 **상위 부호를 그 이름으로 부르고 있었을 뿐**이었다.

| 잘못 잡은 것 | 실제 |
|---|---|
| 기초 `330499` (1,009M) | `3304991000` 기초화장용 제품류 (555M) |
| 마스크팩 `330790` (117M) | `3307904000` 마스크 팩 (67M) |
| 라면 `190230` (175M) | `1902301010` 라면 (165M) |

확인법: `nitemtrade` 를 `hsSgn=<6자리>` 로 부르면 **10자리 + `statKor`(공식 품목명)** 이 같이 온다.
6자리로 부르면 6자리만, 10자리를 보려면 6자리를 인자로 넣어야 한다.

전체 지도는 `fetch_trade.py` 상단 주석에 26.06 실측 금액과 함께 적어 뒀다.
품목을 새로 넣거나 바꿀 때 **반드시 그 표를 먼저 볼 것.**

응답에 6자리와 10자리가 섞여 오므로 `series()` 는 **자릿수까지 본다** —
접두어만 보면 `330499` 가 6자리 행과 10자리 행을 동시에 잡아 두 배가 된다.

화면(수출 데이터 탭) 차트 오른쪽 아래에 지금 보는 계열의 HS 부호가 뜬다.
품목을 바꿀 때마다 갱신되므로, 나중에 무엇을 본 건지 되짚을 수 있다.

## 자동 갱신

> **2026-08-07 대전환: 저장소를 공개로 바꾸고 시세를 매시간으로.**
> 공개 저장소는 **GitHub Actions 분이 무제한**이라, 지금까지 분 한도(2,000분/월) 때문에
> 2시간 간격으로 눌러 두던 시세를 **매시간 수집**으로 올렸다(refresh.yml 의 schedule cron 을
> 장 시간대 UTC 매시간으로). 아래 문서 중 '2,000분 한도로 게이팅한다'류 서술은 GitHub Actions
> 한정으로는 더 이상 구속이 아니다. **단, Cloudflare Pages 빌드 500건/월은 그대로다** — 그래서
> 수집은 매시간이어도 **배포(빌드)는 `deploy_slot()` = 평일 장중 09~16시 매시간**으로만 한다
> (나머지 시간·홀수 시 수집분은 `[CI Skip]`, 다음 배포가 같이 싣는다). 관련 변경:
> ① `refresh.yml` schedule cron 매시간 ② dedup 90→**45분**(매시간 회차가 자기끼리 안 걸리게)
> ③ 뉴스·KOBIS영화는 그대로 **2시간마다**(짝수 시 — 네이버 차단·KOBIS 타임아웃 회피)
> ④ 커밋 배포판정에서 'schedule 이면 무조건 배포' 예외 제거(매시간이라 그거 두면 한도 초과)
> ⑤ `deploy_slot()` 08·10·12·14·16·18 → **평일 09~16 매시간**.

> **2026-08-07 아침 파이프라인 분리: 05시 수집 → 06시 레터.**
> 예전엔 `events.yml` 하나가 05:45에 수집+레터를 같이 했다. 이제 둘로 쪼갰다:
> **`events.yml` = 수집 전용(KST 05:00)**, **`letter.yml` = 데일리 레터(KST 06:00)**.
> 수집이 레터보다 1시간 앞서 끝나므로, 느린 **트렌드 전체(네이버·구글·얀덱스)를 수집에 포함**시켜도
> 레터가 안 밀린다(예전엔 이것 때문에 네이버만 받고 구글·얀덱스는 주1회 `trends.yml` 이었다).
> `trends.yml` 은 이제 **주1회 백업**으로만 둔다. 뉴스봇도 06시 슬롯으로 옮겼다.
> **트리거는 워커의 cron '시(hour)'로 가른다** — UTC **20시=수집(events)**, **21시=레터(letter)+뉴스봇**,
> 그 외=시세(refresh). ⚠ **Cloudflare Cron Trigger 를 `0 20 * * SUN-THU`(수집)·`0 21 * * SUN-THU`(레터)로
> 바꿔야** 정시성이 산다. GitHub cron(`0 20`·`0 21 * * 0-4`)은 밀리는 백업. `digest.py --once` 가 중복 발송 차단.

**Cloudflare Worker의 Cron Trigger**가 GitHub Actions를 호출한다.

- Worker: https://coverage-cron.dayyyline.workers.dev — `?wf=refresh.yml&key=<TRIGGER_KEY>` 로
  수동 실행 가능 (`events.yml`, `trends.yml` 도 동일). 성공 시 `{"ok":true,"status":204}`
- 🔑 **`TRIGGER_KEY` 없이는 전부 404 다** (2026-08-04 추가). 예전엔 검사가 없어서
  이 주소만 알면 누구나 워크플로를 무한히 돌릴 수 있었다 — 한 달 2,000분을 몇 분이면 태운다.
  키는 Cloudflare > Workers > coverage-cron > Settings > Variables and Secrets 에 Secret 으로.
  **열어 두는 쪽으로 되돌리지 말 것.** 특히 저장소를 공개로 바꾸면 이 주소가 그대로 노출된다.
- 소스: `cloudflare-worker/worker.js`, 설치 안내: `cloudflare-worker/설정방법.md`
- Cloudflare cron은 요일 숫자(`0-4`)를 거부한다. **`SUN-THU` 같은 이름**으로 써야 통과.
- ⚠ 워크플로가 초록불인데 커밋이 없으면, 커밋 단계의 경로가 `public/index.html` 인지부터 확인.
  (index.html을 public/로 옮긴 뒤 옛 경로를 보고 있어서 계속 "변경 없음" 처리된 이력)

> **정정(2026-08-06): `refresh.yml` 에 schedule 을 다시 붙였다.**
> 2026-08-02엔 워커·GitHub cron 이중 발화가 아까워 schedule 을 지우고 워커만 남겼는데,
> 워커(무료 cron)가 오후 슬롯을 통째로 빼먹는 날이 나왔다 — 08-06 엔 오전만 뜨고 09~18시
> 시세가 하나도 안 떠 **21시간 멈췄다.** 다른 워크플로(events·movie·screens·trade)는 전부
> GitHub schedule 백업이 있어 멀쩡했고, **시세만 워커 단독이라 죽었다.**
> 그래서 `refresh.yml` 에 `cron: "5 23,1,3,5,7,9,11 * * *"`(슬롯의 UTC 합집합)을 되살렸다.
> 이중 과금은 gate 의 **중복 방지**로 막는다 — 직전 '시세 자동 갱신' 커밋이 90분 안쪽이면
> (커밋 API 로 확인) 그 회차는 체크아웃 전에 끝낸다. 워커가 사는 날엔 GitHub 회차가 대부분
> 여기서 빠지고(과금 1분), 워커가 죽은 날만 실제 수집을 한다. **주기는 이제 `refresh.yml` 의
> cron 과 워커 Cron Trigger 둘 다 본다** — 한쪽만 고치면 다른 쪽이 옛 주기로 계속 돈다.

## 무엇이 언제 도는가 (2026-08-05 정리)

**트리거는 워커 하나, 수집기는 워크플로 하나.** 이 두 줄이 규칙의 전부다.
실행 483회를 뜯어 보니 어긴 자리마다 비용이나 고장이 있었다.

| 워크플로 | 언제 | 무엇이 |
|---|---|---|
| `refresh.yml` | 워커. 평일 **08·10·12·14·16·18**, 주말 12·20 | 시세·뉴스·DART(10/14/18)·잠정(10/18)·치지직·SOOP·트위치·버즈·Steam(10시)·게임머니 재시도(15시) |
| `events.yml` | 워커 05:45 (+ cron 백업) | 아침 전체수집 + 데일리 레터 |
| `letter.yml` | 수집 완료 후(workflow_run) | 데일리 레터 + **트렌드 레터**(`trend_letter.py` · 움직인 것만 ±15% · SAMG·제타 제외) |
| `screens.yml`·`movie.yml` | **수동 전용**(2026-09-21 트리거 차단) | 스크린 배정·예매 · KOBIS 흥행 (하츄핑 보관) |
| `boxseats.yml` | 시세 완료 후 평일 12·18 · 주말 12·20 (+cron 20:40) · 11/15 까지 | 극장가 3사 좌석(치이카와·흥행작) — 수집 잡/커밋 잡 분리 |
| `trade.yml`·`digest.yml` | **수동 전용** | 급할 때만 |

- **`refresh.yml` 의 슬롯은 `collector_health.deploy_slot()` 과 같은 값이어야 한다.**
  다르게 잡으면 수집은 했는데 배포 슬롯이 아니라 `[CI Skip]` 이 붙어 화면이 안 바뀐다.
  슬롯을 바꾸면 안쪽 시간대 판정(DART·잠정)도 같이 옮길 것 — 09/13/17 로 두면 영영 안 온다.
- **게이트는 체크아웃 *앞*에 둔다.** 스텝 안에서 시간을 보면 이미 러너·체크아웃·파이썬
  설치가 끝난 뒤라 아무 일도 안 하는 회차가 분을 먹는다. 앞에 두면 빈 회차가 12초다.
- **GitHub cron 은 3~12시간씩 밀린다.** 실측: 영화 10:00 예약 → 18~22시,
  아침레터 05:40 예약 → 09:14. 정시성이 필요하면 워커에 걸 것. cron 은 백업으로만.
- **cron 백업을 지우지는 말 것.** 워커도 죽는다 — 8/05 낮에 5슬롯을 통째로 걸렀다.
  대신 "워커가 이미 했으면 즉시 종료"를 넣어 중복 수집만 막는다(`events.yml` 의 `gate` 잡).

### 실패를 삼키지 말 것

`python X.py || echo "건너뜀"` 은 쓰지 않는다. 실패가 사라져서 Actions 실패율이
0% 로 보이는데 화면 숫자는 며칠째 옛날 값에 멈춰 있는 상태가 된다(실제로 그랬다).

```
python runstep.py "종목 뉴스" fetch_news.py
```

`runstep.py` 는 성패를 `health.json` 에 남기고 **종료코드는 항상 0** 이다.
(수집기 하나가 죽었다는 이유로 아침 레터가 통째로 안 나간 전례가 있다.)
`watchdog.py` 가 그걸 읽어 **12시간 넘게 이어진 것만 하루 한 번 묶어서** 알린다 —
즉시 쏘면 네이버가 잠깐 흔들릴 때마다 울려서 진짜 고장을 무시하게 된다.
새 데이터 블록을 추가하면 `watchdog.py` 의 `LIMITS` 에도 넣을 것.

## index.html 에 쓰는 법 — 합치지 말고 다시 주입할 것 (2026-08-08)

`public/index.html` 한 파일에 최상위 상수가 **48개** 들어 있고 821~4817줄에 흩어져 있다.
서로 **다른** 상수를 고쳐도 git 은 '인접 변경'으로 봐서 충돌한다.

2026-08-08 실측 사고:

| | 우리(아마존) | 원격 |
|---|---|---|
| `AMAZON` | 08-08 | 08-07 |
| `LIVE`·`APPRANK`·`TOURISM`·`SPOTIFY` | 08-06 | 08-07 |

한 덩어리로 충돌했고, **어느 쪽을 통째로 골라도 하루치가 날아간다.** 리베이스가 멈춘 채
아마존 데이터가 PC 에 갇혔고, 그 상태에서는 이후 회차도 전부 막힌다.

### 규칙

⚠ **재주입 흐름의 함정 — `reset --soft` 는 봇 파일까지 스테이징한다 (두 번 밟음).**
`git reset --soft origin/main` 뒤에는 '내 작업트리 전체 vs 새 HEAD' 의 차이가 전부
스테이징된 상태다. 원격에서 봇이 방금 갱신한 파일(archive/booking.jsonl 등)도 내 옛
로컬본으로 덮여 들어간다. **커밋 직전 `git status` 에서 내가 만지지 않은 파일이
스테이징돼 있으면 `git restore --staged <파일>` 로 뺄 것.**

**푸시가 거절되면 `git pull --rebase` 하지 말 것.** 대신:

```
git fetch origin main
git reset --soft origin/main
git checkout origin/main -- public/index.html   # 원격 최신을 그대로 받는다
<내 블록만 다시 주입>                            # 저장해 둔 데이터로 — 재수집 없음
git add … && git commit && git push              # 부모가 origin/main 이라 fast-forward
```

합치지 않으므로 충돌이 **원리적으로** 생길 수 없고, 남의 상수는 원격 것이 그대로 남는다.
`amazon-beauty-tracker/main.py` 의 `git_push()` 가 이 방식이다. 빈 저장소 두 개로
사고 상황을 재현해 확인했다 — 옛 방식은 충돌·중단, 새 방식은 양쪽 데이터 전부 보존.

**안전장치**: 로컬에 내 것이 아닌 커밋이 있으면 자동 재시도를 멈춘다. 사람이 작업 중일 수 있다.

### 새 수집기를 만들 때

- GitHub 워크플로면 **반드시 `concurrency: group: repo-write`** 에 넣을 것.
  이 그룹이 직렬화해 주기 때문에 워크플로끼리는 실제로 충돌하지 않는다
  (커밋 462건 분석 + 최근 실행 로그 확인: 리베이스 충돌 실출력 0건).
- **그룹 밖에서 도는 것**(이 PC 의 아마존 트래커 같은 것)은 위 재주입 방식을 쓸 것.
  충돌은 사실상 여기서만 난다.

### 로그를 셀 때 조심할 것

GitHub 로그는 **실행한 명령문도 그대로 찍는다**(`^[[36;1m` 로 시작하는 초록 줄).
`grep "리베이스 충돌"` 하면 그 에코가 잡혀서 실패율이 60~75% 로 보인다 — 실제로는 0 이었다.
**본문이 `^[[` 로 시작하면 명령문이지 출력이 아니다.** 같은 함정을 `|| echo "건너뜀"` 에서도 밟았다.

## 두 개의 무료 한도 — 여기서 다 터진다

| | 한도 | 실측 | |
|---|---|---|---|
| GitHub Actions | 2,000분/월 (비공개) | 4,300~5,200분 | 조정 후 ~1,900분 |
| Cloudflare Pages | **500 빌드/월** | 472건/30일 | 조정 후 ~230건 |

**Cloudflare Pages는 푸시 1건 = 빌드 1건이다.** 데이터 갱신이 잦아 이게 먼저 터진다.

- 봇 커밋은 **배포 슬롯**에서만 빌드한다. 나머지는 커밋 메시지 끝에 **`[CI Skip]`** 을 붙이면
  Cloudflare가 그 푸시를 건너뛰고, **다음 배포가 그 데이터까지 같이 싣는다**(데이터 손실 없음).
  슬롯 판정은 `collector_health.deploy_slot()` 한 곳에 있다 — 평일 08·10·12·14·16·18시, 주말 12·20시.
- **새 워크플로를 만들 때**: ① `concurrency: group: repo-write` 에 넣을 것(index.html을 쓰는 것끼리
  동시에 돌면 리베이스 충돌로 죽는다) ② 배포가 꼭 필요한 게 아니면 `[CI Skip]` 을 달 것.
- 사람이 작업할 때도 **커밋을 뭉쳐서 밀 것.** 30일 472건 중 263건이 수동 푸시였다.
- Actions 무거운 단계(`refresh_live.py` 4.7분)는 `refresh.yml` 에서 시간대로 게이팅한다.

## 차단 대응 — `collector_health.py`

수집기가 공유하는 모듈. **새 수집기도 여기를 쓸 것.**

```python
from collector_health import ua, nap, note_health, looks_blocked
UA = ua(referer="...")     # 실제 브라우저 헤더 (고정 'Mozilla/5.0' 은 차단 1순위)
nap(0.2)                   # 요청 간격을 ±25% 흔든다 — 정확히 일정한 간격이 더 잘 걸린다
note_health("소스명", msg) # 막히면 기록 → watchdog.py 가 별도 텔레그램 알림 (msg=None 이면 해제)
```

- **차단 위험 1순위는 네이버 금융**(`api.stock.naver.com`·`m.stock.naver.com`) — 키 없는 비공개
  엔드포인트에 단일 Actions IP로 하루 2,000~3,000요청. 공식 API(DART 2%·YouTube 6%)는 여유.
- **부분 실패는 기록하지 않는다.** 종목 몇 개 빠지는 건 늘 있는 일이라, 절반 넘게 실패할 때만 남긴다.
- 워크플로가 통째로 죽는 건 watchdog 이 못 본다 → `alert.yml` 이 `workflow_run` 실패를 잡아 알린다.

## 작업 절차

```
git pull --rebase        # 봇이 매시간 커밋하므로 필수
# ... 수정 ...
git add -A && git commit -m "..." && git push
```

배포는 1~2분 걸린다. **로컬 프리뷰가 캐시된 옛 버전을 잡는 일이 잦으니**,
검증은 배포 후 실제 사이트에 캐시 무력화 쿼리를 붙여서 할 것.

되돌리기: `git revert <커밋>` 또는 Cloudflare → Deployments → Rollback.

### 과금 구조 — 사용량을 매출로 넘기는 고리 (2026-09-01)

`/api/chat/pricing` 이 열려 있다(캐릭터를 지정하든 말든 같은 값 = 전 캐릭터 공통).

```
daily 100 · thrill 120(정가 150) · honey 300 · love 70 · pro 150 · high 150 · deluxe 300
isDiscounted: true · selectableDepths: ["daily","thrill"]
```

- **과금은 턴당**이다(응답 1회 = 1턴). FAQ: "일상톡 1회 응답당 100개, 설렘톡 150개".
- 화폐는 **'냥'**. 한국은 **냥 = 원 1:1**. 탑툰 코인을 충전하면 냥으로 전환되고
  FAQ 가 "1코인 = 220원" 이라고 적어 둔 건 **1코인 충전 = 220냥**이라는 뜻이다.
- 지역마다 단위가 다르다 — JP 는 10/12(정가 15), TW·GLOBAL 은 100/120.
  **한국만 원화 앵커가 확인됐고 나머지는 단위 미상**이다. 함부로 환산하지 말 것.
- **네 지역 모두 지금 할인 중**(thrill 150→120). 할인 종료는 그 자체로 ARPU 이벤트라
  매일 찍어 `sites[].price` 에 **값이 바뀐 날만** 기록한다.
- 추가 과금선: 음성통화(분당 n냥) · 일러스트 해금(건당) · 확장모드(메시지당 추가).
  즉 우리 추정은 **채팅 턴 매출만 잡는 하한**이다.
- ⚠ `chatCount` 는 **대화(세션) 수**이지 턴 수가 아니다. 번들에 `turnCount` 가 따로 있다.
  박채원 조회 208만 / 대화 4.3만 = 48:1 도 '세션 시작 수'로 읽어야 자연스럽다.
  **세션당 유료 턴 수(T)가 유일한 미지수**이고, 매출 = 세션 × T × 단가 다.
  무료 턴(`freeTurns: "최대 {count}턴 무료"`)이 있어 짧은 세션은 매출이 0 이다.

### 북미는 7월 3주에 실질 개시했다

`periods` 목록만 보면 GLOBAL 이 7월 1주부터라 뉴스('7월 말')와 어긋나 보인다. 값을 보면 풀린다:

```
7월 1주 n=7  tot=10      <- 내부 테스트
7월 2주 n=12 tot=27      <- 테스트
7월 3주 n=45 tot=1,917   <- 실질 개시
이후 2,500~2,900 에서 6주째 정체
```

**주차 목록이 아니라 값으로 런칭 시점을 판정할 것.**
