# 인수인계 — v2 이사 (2026-08-08)

새 채팅에서 이어받을 때 이 문서부터 읽으면 된다.
같이 볼 것: `개보수계획.md`(진단과 단계), `CLAUDE.md`(작업 규칙).

---

## 0. 먼저 알아야 할 사실 3가지

1. **저장소가 공개다.** `visibility: public` (2026-08-08 확인).
   그래서 GitHub Actions 분이 **무료·무제한**이다. 예전 대화에 나오는
   "2,000분 한도" 걱정은 이 저장소엔 더 이상 해당하지 않는다.
   (뉴스봇 저장소는 비공개 그대로다.)

2. **⚠ 워커가 무방비다.** `CLAUDE.md` 에 적힌 워커 주소가 공개 저장소에 노출돼 있는데,
   키 검사가 **배포되지 않았다**. 실측: 키 없이 `?wf=bot-check.yml` → HTTP 200.
   코드(`cloudflare-worker/worker.js`)에는 키 검사가 들어 있고 배포만 남았다.
   **순서 주의**: Cloudflare 에 `TRIGGER_KEY` Secret 을 **먼저** 넣고 그다음 Deploy.
   코드가 fail-closed 라 반대로 하면 수동 실행이 전부 404 가 된다(자동 갱신은 무관).
   → 사용자가 직접 눌러야 하는 일. 아직 안 됨.

3. **엑셀은 이제 안 쓴다.** 파이프라인은 2026-07-26 폐지(`e076167`).
   `DATA` 의 `rev27`/`op27`/`eps27`(29/37종목)만 사내 엑셀 출신이고 지금은
   코드에 박혀 있다. 갱신은 사용자가 숫자를 주면 직접 편집.

---

## 1. 사용자가 원하는 것

- `public/index.html` 을 **v2 로 이사**. 새로 만들되 로직은 옮겨 붙인다(재작성 아님).
- v2 의 **UI/UX 는 디자인 스킬을 설치해서 새로 잡을 예정**이다.
  → 그 스킬이 준비되기 전에는 **화면 디자인을 확정하지 말 것.**
  이번 준비 단계에서는 데이터 층만 만들어 두었다.
- 사용자는 개발자가 아니다. 터미널 명령을 시키지 말고 직접 고쳐서 push 까지 할 것.

---

## 2. 이미 해 둔 것 (이사 준비)

### `export_blocks.py` — 데이터 층 분리

`index.html` 의 데이터 상수를 `public/data/<이름>.json` 으로 떼어낸다.

```
python export_blocks.py          # 내보내기
python export_blocks.py --check  # 검증만
```

- 21개 블록 · 452KB (index.html 823KB 의 56%)
- 내보낸 JSON 이 원본 상수와 **완전히 일치**함을 확인했다.
- **화면은 아직 이 파일을 안 읽는다.** 사본을 하나 더 두는 것뿐이라 지금 상태에서
  돌려도 아무것도 안 바뀐다. 그래서 안전하다.
- 색상표·`TABS`·`SECTORS` 같은 설정은 일부러 뺐다(코드에 속하고 수집기가 안 건드린다).

---

## 3. 다음에 할 일 (순서대로)

### ① 배포 전 자동 점검 (개보수계획 4단계) — 아직 안 함

두 겹으로. Actions 가 무제한이라 비용 제약은 없다.

| | 언제 | 무엇 | 예상 |
|---|---|---|---|
| 가벼운 검사 | 모든 갱신 | `export_blocks.py --check` + `asOf` 신선도 | ~15초 |
| 무거운 검사 | 코드 변경 시 | 헤드리스 브라우저로 렌더 → JS 오류 0 확인 | ~1분 |

실패하면 텔레그램. 정상이면 침묵. (`alert.yml`·`watchdog.py` 에 이미 발송 경로가 있다.)

### ② 수집기가 JSON 에도 쓰게

지금은 `export_blocks.py` 를 돌려야 JSON 이 갱신된다.
수집기가 `index.html` 과 `public/data/*.json` **양쪽에** 쓰게 만든다.
이 단계까지는 화면 무영향이라 안전하다.

### ③ v2 껍데기 + 탭 하나

`public/v2/index.html` 을 만들고 `data/*.json` 을 fetch 해서 그린다.
탭 하나(아마존이나 극장)로 방식을 검증한다.
**이 시점에 디자인 스킬이 필요하다** — UI 는 그때 잡는다.

### ④ 나머지 탭 이사 → ⑤ 스위치

탭 14개, 렌더 함수 20개, 함수 88개(죽은 코드 0). 탭당 반나절 예상.

---

## 4. 옮길 때 반드시 지켜야 할 것 (여기서 사고가 났다)

- **비동기 전환이 최대 위험.** 지금 렌더링은 동기(`render();`)다.
  v2 는 처음부터 `await` 로 데이터를 읽고 그리게 설계할 것.
- **`TODAY` 를 그대로 가져갈 것.** `LIVE.asOf` 를 오늘로 쓰면 주말에 며칠 전으로 멈춘다.
  `toISOString()` 도 UTC 라 한국 아침 9시 이전엔 어제가 나온다.
  (`index.html` 의 `TODAY`/`TODAY_C` 가 정답이다.)
- **도메인 규칙을 다시 짜지 말 것.** 주석에 사고 날짜와 함께 이유가 적혀 있다.
  예: 스크린 '최댓값 규칙'(오후엔 오전 회차가 사라져 판매율이 부풀던 것),
  체인 지평선으로 *미오픈*과 *안 함* 가르기, `natF` 오염일 배제,
  서프라이즈 부호 안전판(적자일 때 방향 뒤집힘), 예매 배수 곡선(D-0 1.35 · D-1 1.88).
- **`file://` 로는 검증이 안 된다.** JSON 을 fetch 하는 순간 CORS 로 막힌다.
  로컬 확인은 정적 서버를 띄우거나 배포본으로 할 것.

---

## 5. 최근에 고친 것 (되돌리지 말 것)

| 커밋 | 내용 |
|---|---|
| `0274143` | 아마존 푸시를 '최신 위에 재주입'으로 — rebase 충돌 원천 차단 |
| `f2dedf0` | CGV 403 수정(`collector_health.ua()` 한 벌을 통째로 보낼 것) + 차단을 '미편성'으로 읽던 것 + 주말 게이트 |
| `b467b32` | 예매 증감을 '전일 같은 시각'과 비교 |
| `9fea225` | 예상 관객을 '예매좌석 × 실측 배수(D-N)'로 — 교차검증 +83% → −7% |
| `ebe35ea` | `TODAY` 를 한 곳으로 + 극장 차트 세로선을 개봉일 → 오늘 |
| `eda19ee` | 실적 프리뷰를 분기 탭(2Q26…)으로 통합 |
| `0808709` | `[CI Skip]` 제거 — 수집하면 바로 배포 |

---

## 6. 지금 걸려 있는 자동화

실측 확인(2026-08-08):

| | 언제(KST) | 어디서 |
|---|---|---|
| 아마존 수집 | 05:15 | 이 PC 작업스케줄러 (`AmazonBeautyTracker`) |
| 레터·뉴스봇 트리거 | 05:35 | 이 PC (`CoverageMorningTrigger` → `morning_trigger.bat`) |
| `events.yml` 아침 전체수집 | 05:00 (`0 20 * * 0-4`) | 워커 + GitHub cron 백업 |
| `letter.yml` 데일리 레터 | 06:00 (`0 21 * * 0-4`) | 워커 + GitHub cron 백업 |
| `refresh.yml` 시세 | **장 시간대 매시간** (`5 23,0-11 * * *`) | 워커 + cron |
| `screens.yml` 스크린 | `refresh` 뒤 + cron 2건 | |

- **아침 파이프라인은 2026-08-07 에 둘로 쪼갰다** — 수집(05시)과 레터(06시)가 별개다.
  옛 대화에 나오는 "events.yml 이 05:45 에 수집+레터를 같이 한다"는 낡은 설명이다.
- **`[CI Skip]` 은 지금 안 쓴다**(2026-08-08 `0808709`). 수집하면 바로 배포한다.
  `deploy_slot()` 은 아직 남아 있지만(평일 09~16 매시간·주말 12·20) 커밋 배포 판정에는
  더 이상 안 쓰인다. CLAUDE.md 의 "슬롯에서만 빌드한다" 서술은 그 앞 시점 것이다.
- **GitHub cron 은 3~12시간 밀린다.** 정시성이 필요하면 워커에 걸 것. cron 은 백업.

`.bat` 은 **ASCII 로만** 쓸 것 — cmd 가 cp949 로 읽어 한글이 든 줄이 깨지는데
종료코드는 0 이 나온다(조용히 아무 일도 안 함). 파이썬도 같은 이유로
`sys.stdout.reconfigure(encoding="utf-8")` 가 필요하다.
