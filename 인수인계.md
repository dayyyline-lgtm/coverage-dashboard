# 인수인계 — 커버리지 대시보드 (2026-08-08 기준)

새 세션에서 **이 파일 하나만** 읽으면 이어받을 수 있게 전부 담았다.
같이 볼 것: `CLAUDE.md`(작업 규칙 — 여전히 유효).

---

## 0. 먼저 알아야 할 사실 4가지

1. **저장소가 공개다** (`visibility: public`, 실측 확인).
   → GitHub Actions 분이 **무료·무제한**. 옛 대화의 "2,000분 한도" 걱정은 이 저장소엔 끝났다.
   (뉴스봇 저장소는 비공개 그대로.) **단, Cloudflare Pages 빌드 500건/월은 그대로다.**

2. **⚠ 워커가 무방비다 — 아직 미해결.**
   공개 저장소 `CLAUDE.md` 에 워커 주소가 적혀 있는데 키 검사가 배포되지 않았다.
   실측: 키 없이 `?wf=bot-check.yml` → **HTTP 200**(누구나 수집기를 돌릴 수 있음).
   코드(`cloudflare-worker/worker.js`)에는 키 검사가 들어 있고 **배포만 남았다.**
   → 사용자가 직접 눌러야 한다. **순서 주의**: Cloudflare 에 `TRIGGER_KEY` Secret 을
   **먼저** 넣고 그다음 Deploy(코드가 fail-closed 라 반대로 하면 수동 실행이 전부 404).
   자동 갱신(cron)은 `scheduled()` 로 들어와 이 검사와 무관하므로 영향 없다.
   돈 문제는 아니고(공개 저장소), **네이버·CGV·아마존 IP 차단 위험**이 이유다.

3. **엑셀은 안 쓴다.** 파이프라인 폐지(2026-07-26 `e076167`), 저장소에 엑셀 파일 없음.
   `DATA` 의 `rev27`·`op27`·`eps27`(29/37종목)만 사내 엑셀 출신이고 지금은 코드에 박혀 있다.
   갱신은 사용자가 숫자를 주면 직접 편집. (엑셀 *내보내기* 기능은 살아 있다.)

4. **사용자는 개발자가 아니다.** 터미널 명령을 시키지 말고 **직접 고쳐서 push 까지** 할 것.
   한국어로 응답.

---

## 1. 지금 구조

```
  트리거                     수집기 26개              산출물
  ─────────                 ──────────              ──────
  Cloudflare Worker  ─┐
  GitHub cron(백업)   ─┼──▶  fetch_*.py  ──┐
  이 PC 작업스케줄러    ─┘     refresh_live.py│
                             inject_amazon.py│
                                             ▼
                          public/index.html  646KB  ← 데이터 상수 37개. 봇이 쓴다
                          public/js/app.js   270KB  ← 화면 코드. 사람이 쓴다
                          public/data/*.json 452KB  ← 사본(아직 아무도 안 읽음)
                                             ▼
                          Cloudflare Pages ──▶ https://coverage-dashboard.pages.dev
                                             │
                          digest.py · watchdog.py · notify.py · trade_digest.py
                                             ▼
                                       텔레그램 봇 3종
```

### 자동화 시각 (실측)

| | 언제(KST) | 어디서 |
|---|---|---|
| 아마존 수집 | 05:15 | 이 PC (`AmazonBeautyTracker`) |
| 레터·뉴스봇 트리거 | 05:35 | 이 PC (`CoverageMorningTrigger` → `morning_trigger.bat`) |
| `events.yml` 아침 전체수집 | 05:00 (`0 20 * * 0-4`) | 워커 + cron 백업 |
| `letter.yml` 데일리 레터 | 06:00 (`0 21 * * 0-4`) | 워커 + cron 백업 |
| `refresh.yml` 시세 | 장 시간대 매시간 | 워커 + cron |
| `screens.yml` 스크린 | `refresh` 뒤 + cron 2건 | |

- 아침 파이프라인은 2026-08-07 에 **수집(05시) / 레터(06시)** 로 쪼갰다.
  옛 대화의 "events.yml 이 05:45 에 수집+레터를 같이 한다"는 낡은 설명이다.
- **`[CI Skip]` 은 안 쓴다**(2026-08-08 `0808709`). 수집하면 바로 배포.
- **GitHub cron 은 3~12시간 밀린다.** 정시성이 필요하면 워커에. cron 은 백업.

---

## 2. 왜 문제가 반복됐나 (측정값)

| | 숫자 |
|---|---|
| `index.html` 크기 | 125KB(7/21) → 903KB(8/08) · 18일 만에 7.2배 → **지금 646KB**(코드 분리 후) |
| 그 파일을 정규식으로 읽고 쓰는 스크립트 | **23개** (읽기 46곳 · 쓰기 12곳) |
| 그 파일에 쓰는 주체 | 워크플로 6 + 이 PC 1 = **7** |
| 사람 커밋 중 '고치는' 커밋 | 7월 12% → **8월 19%** |
| 화면 JS 함수 | 88개 · **죽은 코드 0개** |

**로직은 건강하고 포장이 병들었다.** 지울 코드가 없다 — 뒤엉킨 게 아니라 빽빽하다.
그래서 **재작성이 아니라 재포장**이 답이다(전면 재작성은 4주치 교훈을 버리는 거래).

### 사고 유형 5가지 (2026-08-04~08 실제)

- **A. 한 파일에 7명이 씀** → 8/08 아마존 하루치가 PC 에 갇힘.
  `AMAZON`(우리)과 `LIVE`·`SPOTIFY`(원격)는 **다른 상수인데 충돌**했다 — 줄이 붙어 있어서.
- **B. 실패를 조용히 삼킴** → CGV 403 이 '미편성'으로 둔갑, **29시간** 멈췄는데 워크플로는 초록불.
- **C. 기준값 출처가 여럿** → 화면이 오늘을 8/7 로 앎(`LIVE.asOf` 사용).
  예매 전일비가 **다른 시각끼리** 비교.
- **D. 조건이 흩어짐** → 스크린 주말 분기 누락. 시세 백업 예약이 21경우 중 **0건** 통과.
- **E. 검증 없는 규칙** → 예상 관객 **+83%** 오차. `[CI Skip]` 로 화면 정지.

새 기능 하나마다 ①상수 ②정규식 복사 ③게이트가 같이 늘었고, **결합이 곱으로** 늘었다.
지금 데이터 블록 하나를 더하면 건드릴 곳이 최소 4군데(상수·수집기·게이트·watchdog 한도).

---

## 3. 오늘(8/08) 한 일 — 되돌리지 말 것

| 커밋 | 내용 |
|---|---|
| `0274143` | **아마존 푸시를 '최신 위에 재주입'으로** — rebase 충돌 원천 차단 |
| `66a74a8` | 극장 예매관객 칸 복원(개봉 후에도) |
| `f2dedf0` | **CGV 403 수정** + 차단을 '미편성'으로 읽던 것 + 주말 게이트 |
| `b467b32` | 예매 증감을 **전일 같은 시각**과 비교 |
| `0808709` | 탐침 막혀도 계속 수집 + `[CI Skip]` 제거 |
| `65d1873` | 스크린 3사 전수 복구 |
| `9fea225` | **예상 관객 모델 교체** — 교차검증 +83% → −7% |
| `ebe35ea` | `TODAY` 를 한 곳으로 + 차트 세로선을 개봉일 → 오늘 |
| `eda19ee` | 실적 프리뷰를 분기 탭(2Q26…)으로 통합 |
| `16a9b4a` | `export_blocks.py` — 데이터 층 분리 준비 |
| `586f46c` | **화면 코드를 `js/app.js` 로 분리** |

### 특히 중요한 두 가지

**① 푸시 충돌 대처 — `git pull --rebase` 금지**

```
git fetch origin main
git reset --soft origin/main
git checkout origin/main -- public/index.html   # 원격 최신을 그대로
<내 블록만 다시 주입>                            # 저장해 둔 데이터로 — 재수집 없음
git add … && git commit && git push              # 부모가 origin/main → fast-forward
```
합치지 않으므로 충돌이 **원리적으로** 안 생긴다. 빈 저장소 두 개로 사고를 재현해 확인했다.
`amazon-beauty-tracker/main.py` 의 `git_push()` 가 이 방식이다.

**② 예상 관객 모델 (극장)**

옛 식 `총좌석 × 기준일 판매율 × 전국보정` 은 개봉일 판매율(30%)을 평일에 갖다 써서 틀렸다.
지금은 **`예매좌석 × 실측 배수(D-N)`**. 배수는 하드코딩이 아니라 아카이브에서 매번 다시 잰다.

  D-0 1.35 (1.31·1.40) · D-1 1.88 (1.75·2.00) · D-2 2.79

**D-2 이후는 예상치를 내지 않는다** — 그 배수는 개봉일·금요일에서 나온 값이라 평일에 안 맞는다
(8/10 월요일에 2.79 를 곱하면 1.5만인데 실제론 4만+ 나올 날). 대신 1편 같은 일차 실측을 보여준다.

---

## 4. 설치된 디자인 스킬 (2026-08-08)

`C:\Users\dayline\.claude\skills\` — **14개 · 150파일 · 2.8MB · 실행 코드 0개**(마크다운·CSV만).

| 출처 | 스킬 |
|---|---|
| `pbakaus/impeccable` (Apache 2.0) | `impeccable` — 감사·비평·재설계. **대시보드를 명시적으로 다룸(Operate 모드)** |
| `Leonxlnx/taste-skill` (MIT) | `redesign-existing-projects` · `design-taste-frontend` · `minimalist-ui` · `high-end-visual-design` · `industrial-brutalist-ui` · `full-output-enforcement` |
| `nextlevelbuilder/ui-ux-pro-max-skill` (MIT) | `ui-ux-pro-max`(스타일84·팔레트192·폰트쌍74·차트25종 CSV 포함) · `design-system` · `design` · `ui-styling` · `banner-design` · `brand` · `slides` |

- ⚠ **2026-08-09 정정: 실행 스크립트를 다 받았다.** 예전 기록은 "일부러 안 받았다"였는데,
  그 상태로는 스킬이 반쪽만 돈다. 실측: 14개 스킬 통틀어 **실행 파일 0개**,
  문서가 부르는 스크립트 **38개**가 없었다(`impeccable` 만 20개). 그래서 `critique` 를
  degraded 로밖에 못 돌렸고, 결정론적 탐지기·스냅샷·live 모드를 전부 못 썼다.
- **Node.js 를 설치했다** — `winget install OpenJS.NodeJS.LTS`, v24.19.0 (`C:\Program Files\nodejs`).
  `.mjs`/`.cjs` 스크립트가 전부 이걸 요구한다. 없으면 스크립트를 받아도 무용지물이다.
- **스킬은 상류 버전으로 통째로 맞췄다**(문서만 옛날, 스크립트만 최신이면 인자가 어긋나
  조용히 이상하게 돈다). 백업: `~/.claude/skills/_backup-20260809-145743`.
  삭제된 파일은 0개 — 전부 추가·갱신이었다.

| 스킬 | 출처 | 결과 |
|---|---|---|
| `impeccable` | `pbakaus/impeccable` 의 `.claude/skills/impeccable` | 파일 41→**148** (실행 107개) |
| `ui-styling` | `nextlevelbuilder/ui-ux-pro-max-skill` | 1→**98** |
| `ui-ux-pro-max` | 〃 | 38→**44** |
| `design`·`design-system`·`brand`·`slides`·`banner-design` | 〃 | 갱신 |
| taste-skill 계열 6개 | `Leonxlnx/taste-skill` | **원래 마크다운 한 장짜리라 빠진 것 없음** |

**동작 확인(실측):**
- `node scripts/detect.mjs public/index.html` → exit 2, `.sector-band` 의
  `border-left:4px solid var(--accent)` 를 지적. **손으로 한 감사와 결론이 같았다.**
- `node scripts/context.mjs --target public/index.html` → 셋업 지시문 정상 출력
- `python scripts/search.py "financial dashboard dark theme" --domain product` → 정상
  (인자 형식 주의: `--type/--query` 가 아니라 **위치인자 + `--domain`**)

- `design-taste-frontend`(taste-skill 본체)는 스스로 *"Not dashboards, not data tables"* 라고
  적어 놨다 — 우리 용도엔 `redesign-existing-projects` 와 `impeccable` 이 맞다.
- `ui-styling` 은 shadcn/React/Tailwind 전제라 **우리 스택과 안 맞는다**(참고만).

---

## 5. impeccable 감사 (2026-08-08 실측 · 주요 항목 수정 완료)

사용자 표현: *"지금 html은 너무 내 방식이고 비효율적"*.
**Design Health Score 25/40 (Acceptable).** 점수를 끌어내린 건 시각 완성도가 아니라
상호작용 어포던스였다 — 오류복구 1, 통제·자유 2, 회상보다인식 2, 유연성 2.

⚠ **옛 감사(소스 grep 기반)의 수치 셋이 틀렸다. 브라우저 실측으로 정정한다.**

| 옛 기록 | 실측 |
|---|---|
| 본문 12px | **14.5px** (`body`). 12px 는 표 헤더, 10px 는 헤더 보조설명 |
| 한 줄 92자 | **151자** — 더 나빴다 (`.sec-desc` 가 1200px 를 가로지름) |
| `:focus-visible` 없음 | 규칙은 없었지만 브라우저 기본 링은 떴다. '없음'이 아니라 '설계 안 됨' |
| 이모지 18개 | 개요 탭 기준 **9종 23개** (🕑 8 · 🚀 4 · ⭐ 3 …). 탭마다 다르다 |

### 고친 것 (`7ee2e26`)

| | 무엇 | 어디 |
|---|---|---|
| P1 | **조용한 빈 화면** — 수집이 멈추면 섹션 헤더에 '30시간째 갱신 없음' 배지 | `app.js` `staleNote()` |
| P1 | **탭 넘침** — 넘칠 때만 오른쪽 페이드. 시세 스트립도 같이 | `.overflowing` + `syncTabOverflow()` |
| P2 | **URL 해시 라우팅** — 링크 공유·새로고침·뒤로가기 | `activateTab()` |
| P2 | 설명문 `max-width:68ch`, 표 헤더 76px→**58px** | `.sec-desc` · `headCell()` |
| P3 | `::selection`·`caret-color`·`:focus-visible`·`accent-color`·`prefers-reduced-motion` | `index.html` CSS 끝 |

- **`::selection` 은 취향이 아니다.** 기본값이면 드래그할 때 크롬 파랑이 뜨는데,
  이 화면에서 **파랑은 '하락'** 이다. 의미가 충돌한다.
- **표 헤더 76px 의 범인은 화살표였다.** `.th-sub` 이 `display:block` 이라 정렬 화살표가
  보조설명 *아래로* 밀려 3줄이 됐고, 행 높이는 가장 큰 칸을 따르므로 `OP성장/26·27평균`
  한 칸이 헤더 13칸을 전부 키웠다. `headCell()` 이 화살표를 첫 줄에 붙여 해결.
- **신선도 한도는 두 벌이지만 어긋나면 실패한다.** 브라우저가 파이썬을 못 읽으니
  `app.js` 의 `STALE_H` 를 따로 둘 수밖에 없는데, `precheck.py` 가 `watchdog.LIMITS` 와
  대조해 다르면 배포를 막는다. **새 블록은 두 곳을 같이 고칠 것.**
- 배지에 글리프(⚠)를 안 붙였다 — 색·테두리가 이미 경고를 말하고, 이모지를 아이콘 자리에
  쓰는 것이 이 화면의 지적 사항이기 때문이다.

### 아직 안 고친 것 (의도적)

- **이모지 9종 23개** — 없애려면 SVG 아이콘 체계를 새로 들여야 한다. 내용·레이아웃을
  건드리는 일이라 별건으로 둘 것.
- **`.sector-band` 의 4px 컬러 왼쪽 테두리 88개** — impeccable 금지 목록(1px 초과)이지만,
  섹터 색을 인코딩하는 것이라면 정보라 정당하다. 2px 로 줄이거나 색 점으로 바꾸는 게 후보.
- **표 헤더 정렬이 키보드로 안 된다** — `<th>` 에 `cursor:pointer` 만 있고 `<button>` 이 아니다.
- **단축키·종목 검색·필터 없음** — 파워 유저(=이 화면의 주인) 관점의 가장 큰 남은 구멍.
- **같은 값의 이름이 둘** — 개요 KPI '평균 상승여력' vs 표 헤더 '당사 여력'.

---

## 6. 다음에 할 일 (권장 순서)

### ① 배포 전 자동 점검 — **완료 (2026-08-08)**

`check.yml` 이 `public/**` 푸시마다 두 겹으로 본다. 실패할 때만 텔레그램, 정상이면 침묵.

| | 파일 | 무엇 | 시간 |
|---|---|---|---|
| 정적 | `precheck.py` | 상수·앵커·껍데기·크기·연결·`.bat`·신선도한도 일치 | ~1초 |
| 렌더 | `check_render.py` | 크롬으로 ① 탭 11개 클릭 ② 탭 11개를 **`#해시`로 새로 열기** → JS 예외 0 | ~60초 |

②를 나중에 덧붙인 이유는 8절 '주소로 여는 것과 클릭으로 여는 것' 항목에 있다 —
①만으로는 못 잡는 버그가 실제로 배포까지 나갔다.

**두 겹인 이유 — 고장을 심어서 확인했다.**
`app.js` 가 곳곳을 `typeof X !== "undefined"` 로 방어하고 있어서,
상수가 사라져도 **예외가 안 나고 화면만 조용히 빈다.**

| 심은 고장 | 정적 | 렌더 |
|---|---|---|
| 상수 이름이 사라짐 (`MOVIE_SCREENS`) | ✓ | ✗ 놓침 |
| 블록이 껍데기만 남음 (`AMAZON = {}`) | ✓ | ✗ 놓침 |
| `app.js` 참조가 끊김 | ✓ | ✓ |

멀쩡한 사본은 거짓 경보 없이 통과하는 것도 같이 확인했다.

- **필수 상수 목록을 새로 만들지 않았다.** `watchdog.py` 의 `LIMITS` 가 이미 등록처라
  거기서 읽어 온다 — 새 블록을 `LIMITS` 에 넣으면 점검도 자동으로 따라온다.
- **껍데기 검사가 `watchdog` 의 사각을 메운다.** `watchdog` 은 `asOf` 가 없는 블록을
  통째로 건너뛰므로, 블록이 `{}` 로 비면 신선도로는 영영 안 잡힌다.
- 로컬에서도 그대로 돈다: `python precheck.py` · `python check_render.py --shot shots`
  (`--shot` 은 탭별 스크린샷을 남긴다. 브라우저를 손으로 여는 대신 쓸 것.)
- `repo-write` 그룹에 **넣지 않았다** — 저장소에 쓰지 않으므로 수집기 뒤에 줄 설 이유가 없다.

곁가지로 잡힌 것: `업데이트.bat` 이 폐지된 `rebuild_from_excel.py`(2026-07-26 삭제)를
부르고 있었다. 눌러도 "엑셀을 읽지 못했습니다" 만 뜨는 죽은 파일이라 지웠다.

### ② 디자인 개편 (impeccable) — **1차 완료 (2026-08-08)**

5절 참고. P1·P2·P3 를 한 번에 처리했고, 남은 것은 5절 '아직 안 고친 것'.

⚠ **"`js/app.js` 만 고치면 된다"는 틀린 설명이었다.** CSS 는 `index.html` 12~503줄에 있다.
다만 데이터 상수는 819줄부터라 **300줄 넘게 떨어져 있어 봇과 git 충돌은 안 난다**(실측).

✅ **impeccable 은 이제 온전히 돈다**(2026-08-09). node + 스크립트 107개 설치 완료 — 4절 참조.
1차 감사는 그 전에 한 것이라 degraded 였다. 다시 돌리면 결정론적 탐지기·스냅샷·트렌드가
붙는다. **단, 서브에이전트가 막힌 세션에서는 여전히 degraded 배너를 달아야 한다**
(`critique` 는 Assessment A/B 를 별도 에이전트로 돌리라고 요구한다).

### ③ `app.js` 를 탭별로 쪼개기 (ES 모듈)

지금은 270KB 한 파일. 탭 11개 · 렌더 함수 20개 → `js/tabs/극장.js` 식으로.

**전제는 이미 실측 확인했다**: 모듈(`type="module"`)이 보통 스크립트의 `const` 를 읽을 수 있다.
최소 예제로 `DATA`·`LIVE` 둘 다 읽히는 것, `window` 오염이 없는 것을 확인했다.
그래서 **데이터는 index.html 에 그대로 두고 코드만 모듈로** 바꿀 수 있다.

⚠ 모듈은 **strict mode** 다. 옮기기 전에 선언 없는 대입 등을 확인할 것.
그래서 1단계는 일부러 보통 스크립트(`<script src>`)로 뺐다.

### ④ 수집기가 `public/data/*.json` 에도 쓰게

지금은 `export_blocks.py` 를 돌려야 갱신된다. 수집기가 양쪽에 쓰게 만들면
나중에 화면이 JSON 만 읽도록 바꿀 수 있다(파일 크기↓, 스크래핑 방어).

---

## 6-1. 데일리 레터가 이틀 끊긴 사고 (2026-08-08~09) — 원인과 구조적 대응

**증상:** 8/07 이후 레터가 안 왔다. 워크플로는 전부 초록불이었고 알림도 없었다.

**직접 원인:** 8/08 05:32 에 레터를 `events.yml` 에서 `letter.yml` 로 떼어냈는데
**부르는 쪽을 아무도 안 고쳤다.** `letter.yml` 실행 기록 **0건**.

트리거 셋이 전부 비껴갔다:

| 트리거 | 왜 안 불렀나 |
|---|---|
| PC `morning_trigger.bat` (유일하게 안 밀리는 트리거) | `events.yml` 만 부른다. 분리 때 안 고침 |
| Cloudflare 워커 | Cron Trigger 가 아직 옛 `45 20` 이다. 새 코드는 UTC **21시**에 레터를 부르는데 21시 트리거가 없다 |
| GitHub cron `0 21 * * 0-4` | 평일만. 분리 직후가 토·일이라 예약 자체가 없었다 |

**워커는 살아 있다** — `refresh.yml` dispatch 가 매번 `:00:04`(정각)에 찍힌다.
PC 는 `:35:02`. **초 단위 타임스탬프가 호출자를 드러낸다**(아래 RC5 참조).

### 근본 원인 5가지와 대응

| | 원인 | 대응 |
|---|---|---|
| RC1 | **암묵적 의존을 리팩터링이 끊었다.** "events.yml 을 부르면 레터가 나간다"가 어디에도 안 적혀 있었다 | `letter.yml` 에 **`workflow_run`** — 수집이 끝나면 레터가 따라 나간다. 누가 수집을 불렀든(PC·워커·cron). 이제 고아가 될 수 없다 |
| RC2 | **이름 참조가 조용히 끊긴다.** GitHub 은 없는 워크플로 이름을 가리켜도 아무 말 안 한다 | `precheck.py` 의 **`workflow_links()`** — `workflow_run` 대상·워커·PC 가 부르는 것이 실재하는지 대조. 개명하면 즉시 배포가 막힌다 |
| RC3 | **로그가 거짓말했다.** `.bat` 이 `OK daily-letter` 를 찍었는데 그 줄은 수집만 했다 | 라벨 수정 + "다시 이렇게 쓰지 말 것" 주석 |
| RC4 | **감시가 전부 '실패' 모양이다.** 안 도는 것은 실패가 아니라 안 걸린다 | `watchdog.py` 의 **`letter_gap()`** — '왔어야 할 것이 왔나'를 본다(40시간) |
| RC5 | **`workflow_dispatch` 가 호출자 신원을 지운다.** 워커인지 PC 인지 사람인지 구분이 안 된다 | 미해결. 아래 참조 |

**같은 부류의 버그를 하나 더 찾았다.** `alert.yml` 이 개명 전 이름
`"아침 데이터 갱신 + 데일리 레터"` 를 계속 보고 있었다 — 8/08 개명 이후
**수집 실패 알림이 한 번도 안 나갔다.** 아무도 몰랐다. RC2 의 검사가 이걸 잡는다.

### 남은 것

- **Cloudflare Cron Trigger 를 손볼 것** — `45 20 * * SUN-THU` → `0 20 * * SUN-THU`(수집)
  + `0 21 * * SUN-THU`(레터) 추가. 레터는 이제 `workflow_run` 으로 나가므로 **급하지 않다.**
  다만 워커의 아침 수집은 아직 옛 시각(05:45)에 돈다.
- **RC5(호출자 신원)** — `workflow_dispatch` 에 `src` 입력을 두고 워커는 `src=worker`,
  PC 는 `src=pc` 로 보내면 실행 기록만으로 '어느 트리거가 살아 있나'를 알 수 있다.
  지금은 초 단위 시각으로 역추적해야 하는데, 이번엔 그게 통한 게 운이었다.
  워커 재배포가 필요해서 미뤘다.

## 6-2. 이 PC 작업 두 개가 죽고 뉴스봇이 이틀 끊긴 건 (2026-08-11)

**증상:** 데일리 레터·시세는 멀쩡한데 **아마존(💄)과 뉴스봇 브리핑만** 안 왔다.

**직접 원인:** 이 PC 의 작업 스케줄러 두 개가 8/10~8/11 이틀 다 죽었다.

| 작업 | 시각 | 종료코드 | 로그 |
|---|---|---|---|
| `AmazonBeautyTracker` | 05:15 | **-1073741510** | 헤더만, 본문 0줄(8/09 은 132줄) |
| `CoverageMorningTrigger` | 05:35 | **-1073741510** | `^C` 만 |

`-1073741510` = `0xC000013A` = **STATUS_CONTROL_C_EXIT** — 정상 종료가 아니라 강제 중단.
서로 무관한 두 스크립트(파이썬 / gh CLI)가 같은 코드로 죽었으니 **코드 문제가 아니다.**
두 작업 다 `Logon Mode: Interactive only` 라 절전·잠금 전환에 취약한 구성이다.
⚠ **작업 스케줄러 운영 로그가 꺼져 있어 이유가 기록되지 않았다**(켜려면 관리자 권한 필요).

### 뉴스봇은 백업이 0개였다 — 문서가 틀렸다

`daily-briefing.yml` 주석에 *"이제 워커가 매일 05:45 한 번만 부른다"* 고 적혀 있었는데,
**실행 기록을 세어 보니 워커가 부른 적이 한 번도 없다.** 전부 `20:35`(이 PC)였다.

| 경로 | 실제 |
|---|---|
| GitHub cron | 2026-08-05 에 제거됨(늦게 뜬다는 이유) |
| Cloudflare 워커 | **호출 0건** — 아래 '왜' 참조 |
| 이 PC `.bat` | 유일한 실트리거 → 죽자 이틀 끊김 |

**왜 워커가 안 불렀나:** 워커는 `isLetter`(UTC 21시)일 때만 뉴스봇을 부르는데,
**21시가 들어간 Cron Trigger 가 없었다.** 그런데 추가하려니 **무료 플랜은 Cron Trigger 가
계정당 5개**라 Add 버튼이 안 눌린다(오류 메시지도 안 뜬다).

### 고친 것

- **워커 분기를 `event.scheduledTime`(실제 발화 시각)으로** 바꿨다(`7dc0f0a`).
  예전엔 cron **문자열**의 시 칸을 파싱해서 `0 20,21` 처럼 묶을 수가 없었다
  (`"20"` 이 있으면 21시에도 수집으로 오인). 이제 묶어도 정확히 갈린다.
  → `45 20 * * SUN-THU` 를 **`0 20,21 * * SUN-THU`** 로 바꾸면 트리거를 늘리지 않고
    05:00 수집 + 06:00 레터·뉴스봇이 둘 다 된다. **덤으로 수집이 05:45→05:00 로 당겨졌다.**
  node 로 시각별 분기를 검증했다(05:00→events, 06:00→letter+뉴스봇, 그 외→refresh).
- **뉴스봇에 GitHub cron 백업 + 중복 방지 게이트**(news-bot `c6c5f6c`·`10ef3b7`).
  2026-08-05 에 cron 을 지운 진짜 이유가 '중복 방지 장치가 없어서' 였으므로,
  장치를 만들고 되살렸다. 게이트는 실행 기록 API 로 '오늘 이미 나갔거나 도는 중'을 본다
  — **진행 중인 회차도 센다**(PC 가 05:35 에 시작해 아직 도는데 워커가 06:00 에 또 부르면
  '성공한 것' 만 봐서는 못 거른다). 손으로 다시 보내려면 `force` 입력.

### 지금 트리거 지도

| | 데일리 레터 | 뉴스봇 | 아마존 |
|---|---|---|---|
| Cloudflare 워커 | 06:00 | **06:00** | — |
| GitHub `workflow_run`/cron | ✅ | 백업(늦음) | — |
| 이 PC | 05:35 | 05:35 | **05:15 단독** |

**아마존만 대안이 없다.** 트래커 문서의 '로컬 실행 고정(클라우드 금지)' 때문이다 —
데이터센터 IP 는 아마존이 막는다. 클라우드로 옮기는 유일한 길은 Keepa(€49/월) 전환이다.

### 내가 두 번 잘못 짚은 것 (같은 실수 반복 금지)

1. **로컬이 38커밋 뒤쳐진 채로** `public/index.html` 을 읽고 "시세가 나흘 멈췄다"고 했다.
   `git pull` 이 봇 데이터 파일(`amazon-beauty-tracker/data/*`)의 unstaged 변경 때문에
   조용히 실패하고 있었다. **진단 전에 반드시 `git status --branch` 로 `behind` 를 볼 것.**
2. 그다음엔 내 코드 수정을 의심했는데, 서로 무관한 두 작업이 같은 종료코드로 죽었으니
   아니었다. **공통 증상이면 공통 원인부터 찾을 것.**

## 7. 절대 지킬 것 (여기서 사고가 났다)

- **`index.html` 에서 상수를 옮길 때는 규칙 두 가지를 지킬 것**
  ① 순수 JSON 상수 ② **수집기가 이름으로 찾는 상수**.
  `TRADE_PRELIM`·`TRADE_FLASH` 는 안에 주석이 있어 JSON 이 아닌데 `fetch_trade.py` 가 찾는다 —
  규칙 ②가 없었으면 조용히 깨졌다. 새 상수 삽입 앵커인 `const LIVE` 도 남겨야 한다.
  (`split_js.py` 가 이 규칙을 구현하고 있다.)
- **`TODAY` 를 쓸 것.** `LIVE.asOf` 는 주말·장마감 뒤 며칠 전에 멈춘다.
  `toISOString()` 은 UTC 라 한국 아침 9시 이전엔 어제가 나온다.
- **도메인 규칙을 다시 짜지 말 것.** 주석에 사고 날짜와 이유가 있다:
  스크린 '최댓값 규칙'(오후엔 오전 회차가 사라져 판매율이 부풀던 것),
  체인 지평선으로 *미오픈*/*안 함* 가르기, `natF` 오염일 배제,
  서프라이즈 부호 안전판(적자일 때 방향 뒤집힘), 예매 배수 곡선.
- **`file://` 로 검증하지 말 것.** 외부 스크립트가 CORS 로 막힌다.
  `python -m http.server` 로 띄우거나 배포본으로 볼 것.
- **주소로 여는 것과 클릭으로 여는 것은 다른 코드 경로다.** `#탭` 으로 새로 열면
  렌더가 **스크립트 평가 도중** 불린다 — `app.js` 아래쪽 `const`(MOVIE_UPCOMING 2021줄 등)를
  건드리면 TDZ 로 죽고 페이지가 백지가 된다. `(X||[])` 방어는 TDZ 에 안 먹는다.
  첫 로드의 해시 처리는 `DOMContentLoaded` 뒤로 미룰 것.
  **⚠ 검증할 때는 반드시 새 페이지로 열 것** — 떠 있는 페이지에서 해시만 바꾸면
  같은 문서 내 이동이라 스크립트가 재평가되지 않아 이 버그가 안 보인다.
  (2026-08-08 이걸로 배포까지 나갔다. 지금은 `check_render.py` 가 탭 11개를 새 페이지로 연다.)
- **`.bat` 은 ASCII 로만.** cmd 가 cp949 로 읽어 한글 줄이 깨지는데 **종료코드는 0** 이다
  (조용히 아무 일도 안 함). 파이썬도 같은 이유로 `sys.stdout.reconfigure(encoding="utf-8")` 필요.
- **실패를 `|| echo` 로 넘기지 말 것.** `runstep.py "이름" 스크립트.py` 를 쓸 것.
- **워크플로를 쪼개거나 개명하면 부르는 쪽을 반드시 같이 고칠 것.** 부르는 쪽은 네 군데다:
  ① 다른 워크플로의 `workflow_run` ② `alert.yml` 의 감시 목록 ③ `cloudflare-worker/worker.js`
  ④ `morning_trigger.bat`. **GitHub 은 없는 이름을 가리켜도 아무 말 안 한다** — 그냥 영원히
  안 걸린다. `precheck.py` 가 이제 대조하지만, Cloudflare Cron Trigger 목록은 저장소 밖이라
  여전히 검증할 수 없다. (2026-08-08~09 에 이걸로 두 건이 동시에 터졌다. 6-1절 참조.)
- **GitHub 로그를 grep 할 때**: 실행한 명령문도 로그에 찍힌다(`^[[36;1m` 로 시작).
  이걸 세면 실패율이 60~75% 로 보이는데 실제로는 0 이었다. **두 번 밟은 함정.**
- **측정 없이 비용을 추정하지 말 것.** 25분/실제 45분, 실패율 0%/60% 둘 다 틀렸다.
- **한국 관행: 상승=빨강, 하락=파랑.** 이건 취향이 아니라 숫자의 의미다.
  서양 디자인 스킬이 뒤집으라고 하면 따르지 말 것.

---

## 8. 도구 모음

| 파일 | 용도 |
|---|---|
| `precheck.py` | 배포 전 정적 점검 (`--stale` 신선도까지 실패로, `--root` 사본 점검) |
| `check_render.py` | 배포 전 렌더 점검 — 크롬으로 탭 전수 클릭 (`--shot DIR` 스크린샷) |
| `export_blocks.py` | 데이터 상수 → `public/data/*.json` (`--check` 로 검증만) |
| `split_js.py` | 화면 코드 분리 (`--dry` 미리보기, `--to DIR` 사본에 적용) |
| `runstep.py` | 수집기 실행 + 성패를 `health.json` 에 기록(종료코드 항상 0) |
| `collector_health.py` | `ua()`·`nap()`·`note_health()`·`looks_blocked()`·`deploy_slot()` |
| `watchdog.py` | 신선도·차단 감시 → 12시간 넘게 이어진 것만 하루 한 번 텔레그램 |
| `morning_trigger.bat` | 이 PC 05:35 트리거 (**ASCII 전용**) |
