# 영화 수집 — 멈춤 기록과 되살리는 법 (2026-09-21)

> **2026-09-26 추가**: 새 영화(극장판 치이카와 + 흥행작 3편)는 여기 두 수집기를 되살리지 않고
> **범용 수집기 `fetch_boxoffice.py` → `BOXOFFICE`** 로 받는다(영화 목록 `FILMS` 만 바꿔 끼우는 구조).
> 3사 좌석 요청 함수는 `fetch_screens.py` 것을 import 해 쓰므로 **그 파일의 체인 함수는 지우지 말 것.**
> 아래 '되살릴 때 먼저 볼 것' ①(CGV 러너 403)은 여전히 유효 — 그래서 `--seats` 는 이 PC 에서만 돈다.

SAMG엔터 극장판(사랑의 하츄핑) 추적용으로 만든 두 수집기를 **자동 실행에서 뺐다.**
스크립트·워크플로·데이터는 전부 그대로 있다. 지운 것은 '주기적으로 도는 것'뿐이다.

| | 스크립트 | 상수 | 지금 상태 |
|---|---|---|---|
| KOBIS 박스오피스·예매 | `fetch_movie.py` | `MOVIE` | `movie.yml` 수동 dispatch 만 |
| 3사 스크린 배정·예매 | `fetch_screens.py` | `MOVIE_SCREENS` | `screens.yml` 수동 dispatch 만 |

## 왜 멈췄나

1. **끝난 영화다.** 하츄핑2 는 8/5 개봉이라 9월 셋째 주엔 롯데 단독 70여 관이 남았다.
   그걸 잡자고 3사 순회(롯데 237개관 + 메가 지역8)를 회당 20~30분씩 하루 몇 번 돌고 있었다.
2. **이미 반쪽이었다.** 메가박스는 8/30 이후 이 영화를 내렸고(미편성 — 고장이 아니다),
   CGV 는 9/13 이후 GitHub 러너에서 403 이다. CGV 가 예매석의 **55~60%**(스크린의 45%)라
   9/13 이후 쌓인 건 롯데 단독의 하한이다. 그 앞 구간과 크기를 견주면 안 된다.
3. **CGV 는 죽지 않았다.** 2026-09-21 이 PC(한국 가정용 IP)에서 같은 요청이 **200 · 상영작 68편**.
   아이템매니아·올리브영과 같은 계열의 **러너 IP 문제**다. 되살릴 땐 이걸 먼저 처리해야 한다
   (아래 '되살릴 때 먼저 볼 것' ①).

## 무엇을 건드렸나 — 되살릴 때 되돌릴 목록

| 파일 | 한 것 |
|---|---|
| `.github/workflows/screens.yml` | `schedule`·`workflow_run` 을 주석 처리(‘on:’ 블록). `workflow_dispatch` 는 남김 |
| `.github/workflows/movie.yml` | 변경 없음(원래 수동 전용). 머리에 경위만 적음 |
| `.github/workflows/refresh.yml` | '영화 예매·박스오피스 수집' 스텝 삭제(짝수 시 2시간마다였다) |
| `.github/workflows/events.yml` | 같은 스텝 삭제(아침 05시 회차) |
| `watchdog.py` | `LIMITS` 의 `MOVIE_SCREENS`·`MOVIE` 두 줄 주석. **안 빼면 매일 '고장' 알림이 온다** |
| `digest.py` | 데일리 레터 🎬 예매 섹션에 신선도 가드(마지막 점이 2일 넘으면 통째로 뺀다) |
| `public/js/app.js` | `STALE_H` 에서 `MOVIE:10` 제거 · 두 섹션 제목에 '수집 중단(보관 기록)' 표기 |
| `alert.yml` | **건드리지 않음.** 두 워크플로 이름이 그대로 있어 손으로 돌렸다 죽으면 알림이 온다 |

⚠ `alert.yml` 의 목록은 파일명이 아니라 워크플로의 `name:` 이다. 워크플로를 **지우거나 개명하면**
`precheck.py` 가 배포를 막는다. 그래서 파일을 지우지 않고 트리거만 껐다.

## 되살릴 때 먼저 볼 것

1. **CGV 403 부터.** 러너에서 한 번 찔러 보고(`diag_probe.py` 에 `www.cgv.co.kr` 이 이미 있다)
   여전히 403 이면 세 갈래다 — ⓐ 올리브영 방식(403 재시도 3회 8·16초) ⓑ `kr_collect.py` 로
   옮겨 이 PC 에서 수집 ⓒ CGV 없이 2사로 감수(그럼 전국보정 `natF` 상한 2.5 가 걸린다.
   3사 89% → 2사 약 40% 라 `natF` 가 2.5 를 넘어 관객 예측이 조용히 틀어진다. 상한도 같이 올릴 것).
2. **편성 탐침이 CGV 에 매여 있다.** `fetch_screens.py` 의 `programmed()` 는 CGV 대형점 4곳
   (`PROBE_SITES`)을 찔러 '이 날짜에 이 영화가 걸렸나'를 본다. 이게 막히면 전 날짜가 '미편성'으로
   보여 수집이 통째로 선다(2026-08-07 에 29시간 그렇게 날아갔다. 지금은 `PROBE_FAILED` 로
   구분해 지평선 안쪽은 탐침 없이 진행한다). CGV 를 안 쓸 거면 탐침을 갈아야 한다 —
   **2026-09-21 실측으로 대안 둘 다 확인했다**:
   - 메가박스 강남점 1요청: `mb_post({masterType:"brch", brchNo:"1372", …, playDe:날짜})` → 그날 상영작 전부.
     제일 싸지만 메가는 예매 달력을 제일 짧게 연다(먼 날짜가 '미편성'으로 오독된다).
   - 롯데 대형관 3곳: `GetTicketingPage`(1요청, 239개관 목록 · 이름은 `CinemaNameKR`)로 ID 를 얻고
     `GetPlaySequence` 에 `representationMovieCode: ""` → 그 극장 전 영화. 롯데가 달력을 제일 멀리 연다.
   - 권장: 메가 1요청 먼저 → 없으면 롯데 3요청. 둘 중 하나라도 걸리면 편성.
3. **`WATCH`·`PEER_TITLES` 를 새 영화로 바꿀 것.** 지금은 `["하츄핑","티니핑"]` 과
   `["스파이더맨","오디세이","호프"]` 로 박혀 있다. 비교군이 없으면 '예매율 12% 가 낮은 건가'를 가를 수 없다.
4. **`archive/screens.jsonl` 은 지우지 말 것.** index.html 시계열은 `KEEP=90` 으로 잘리지만
   여기는 append 만 한다. 다음 극장판의 기준선이 이 파일이다(1편 실측이 2편 판단의 잣대였던 것과 같다).

## 남겨 둔 지식 — 다시 알아내려면 비싼 것들

- **3사 API 규격**과 좌석 필드의 의미(총좌석 `stcnt`/`totSeatCnt`/`TotalSeatCount`,
  잔여 `frSeatCnt`/`restSeatCnt`/`BookingSeatCount`)는 `fetch_screens.py` 머리 주석에 실측 검증
  3건과 함께 그대로 있다. **롯데 `BookingSeatCount` 는 이름과 달리 '잔여'다** — 여기서 한 번 틀렸다.
- 어느 API 도 '팔린 좌석'을 직접 주지 않는다. `판매 = 총 − 잔여` 로 만든다. 차단석 같은
  유령 판매분이 없다는 건 8/10 분포(0석이 최빈값)로 확인했다.
- 3사 예매석 합 = KOBIS 전국 예매관객의 **89%**. 독립·소형관은 예매 API 가 없다.
- `peer_key()` — 체인이 상영 포맷을 별개 영화처럼 준다('오디세이(IMAX 2D)'·'(S)스파이더맨').
  키워드로 접지 않으면 비교군이 24개로 불어나고 한 영화 좌석이 여러 줄로 쪼개진다.
- 지평선(`horizon`)은 '가장 먼 편성일'이 아니라 **'첫 편성일부터 끊기지 않고 이어지는 마지막 날'**이다.
  최댓값으로 잡으면 극장 한 곳이 먼저 연 파편 때문에 지평선이 뒤로 밀려 오독이 생긴다.
- `MOVIE.scanned` — KOBIS 일별 Top10 밖이라 값이 없는 날과 수집이 빠진 날은 **다른 사건**이다.
  구멍을 보고 수집기를 고치려 들기 전에 `scanned` 부터 볼 것.
