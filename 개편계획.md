# 대시보드 전면 개편 계획 (2026-09-24 시작)

> 새 세션은 **이 파일 + `CLAUDE.md` + `인수인계.md`** 순으로 읽고 이어받는다.
> 진행 상황은 맨 아래 '진행 로그'에 날짜와 함께 남긴다. 완료한 항목은 `[x]`.

---

## 0. 요청 (2026-09-24, 사용자 원문 요약)

1. 대시보드를 **싹 고친다** — 깔끔하게, 데이터 가독성 좋게.
2. **평일 07:30 출근 시점**에 트렌드·기본 데이터가 전부 갱신돼 있어야 한다.
3. **아마존 갱신이 자꾸 끊긴다** — 원인을 잡고 방안을 세운다.
4. 쇼핑몰을 아마존 밖으로 — **중국·일본·유럽·러시아·동남아 현지 몰**까지 다각도로.
5. 트렌드 탭의 **디시·채용(모집인원) 같은 노이즈 많은 데이터**를 깔끔한 시각화로.
6. 긴 작업이다 — 깊게 고민하고 단계별로 오래 진행한다.

---

## 1. 진단 — 실측한 것만 적는다 (2026-09-24)

### 1-1. 로컬 클론이 6주째 8월 13일 상태였다 → 복구 완료

아마존 트래커의 `git reset --soft origin/main` 재주입 방식은 **자기 파일만** 받고 다른 파일은 옛것으로 둔다.
그래서 `HEAD` 는 9/23 인데 작업 트리·인덱스는 8/13 이었고, `git diff --cached` 가 **원격 6주치 작업(수집기 14개·CLAUDE.md 1,070줄)을 '삭제'로** 들고 있었다.
그 상태에서 `git add -A && git commit` 을 했으면 원격 작업이 통째로 날아갔다.
→ 옛 로컬 상태를 패치로 보관한 뒤 `git checkout origin/main -- .` 로 맞췄다. 손실 0.
**규칙: 작업 시작 전 반드시 `git status --branch` + `git diff --cached --stat` 를 볼 것.**

### 1-2. 아침 수집(events.yml)이 9/15 부터 매일 30분 컷에 잘린다

| 실측 (KST) | 05:00 워커 회차 | 05:35 PC 회차 |
|---|---|---|
| 9/21 · 9/22 · 9/23 | 30분에 **취소** | 30분에 **취소** |
| 잘린 스텝 | 컴투스 모델 · **월간 수출** | 월간 수출 |

- 수집기가 20개로 늘면서 회당 32~35분이 됐는데 `timeout-minutes: 30` 그대로였다.
- 게이트가 `schedule` 회차만 거르므로 **워커 05:00 과 PC 05:35 가 매일 같은 수집을 두 번** 했다(둘 다 잘림).
- 커밋 스텝이 `if: always()` 라 잘리기 전까지 받은 건 올라갔다 — 그래서 겉보기엔 대부분 갱신돼 보였다.
- 수출(`TRADE`)은 10~26일 확인 구간인데 잘려서 9/22 이후 확인을 못 했다.

### 1-3. 네이버 데이터랩 전일치는 KST 10시경에 열린다 — 07:30 엔 물리적으로 D-2 까지

`refresh.yml` 의 '트렌드 일간 갱신' 스텝 소요를 회차별로 재면(9/22·9/23): 08:04·09:12 회차는 1초(아직 없음), **10:08·10:11 회차에서 11~17초(수신)**.
→ 07:30 에 볼 수 있는 네이버 일별 계열의 끝은 **그제**다. 어제치는 10:08 회차가 자동으로 붙는다(이미 그렇게 돈다).
구글·얀덱스·나머지 실측 계열(동접·순위·디시·채용 …)은 05시 수집이 전일치를 준다.

### 1-4. 아마존 — 53일 중 12일 결측, 원인 셋

`data/history.csv` 결측일: 8/14 · 8/16~19 · 8/22~24 · 9/9 · 9/11 · 9/14~16 · 9/19 · **9/24(오늘)**.

| 원인 | 근거 |
|---|---|
| **① 절전 복귀 후 재수면** | 실패일엔 전원 로그에 05:15 기상 + 05:34 기상이 **둘 다** 찍힌다(9/19·9/21·9/24). 타이머 기상 후 2분간 아무 입력이 없으면 Windows 는 다시 잔다. 수집은 40~50분 걸린다 |
| **② 네트워크 미준비** | 9/21 05:19 `DNSError: Could not resolve host: www.amazon.de` — 기상 직후 Wi-Fi 가 붙기 전에 요청 |
| **③ 페이지 변형** | 9/21 IT · 9/24 ES: 정상 200 인데 `data-client-recs-list` 없는 껍데기(294KB). 재시도 1회로 대개 복구 |
| (증상) 로그가 비어 있다 | 실패일 `run.log` 가 헤더 한 줄뿐 — stdout 이 파일로 갈 때 블록 버퍼라 죽으면 통째로 사라진다 |

### 1-5. 트렌드 탭 — 노이즈가 신호를 덮는다

- 종목 버튼 **35개** 한 줄 + 접힘 블록 3개(화제·올리브영·채용) + 하이라이트 칩 + 분류·주제·구간 줄. 차트가 화면 아래로 밀린다.
- 일별 계열의 **'전일비'** 가 대표값이다 — 실측: 아이온2 국가별 `전일비 +147.1% · +184.6%`. 30~40 대 지수가 하루 튄 것이지 뉴스가 아니다.
- '주목할 추이' 칩도 같은 계산(`4주 +482%`) → 저점발 노이즈가 매일 1순위로 뜬다.
- 디시 글수·채용 공고는 본질이 **주 단위 이하로는 의미 없는** 계열인데 일별 점을 그대로 그린다.

### 1-6. 해외 쇼핑몰 후보 탐침 (이 PC · 가정용 IP · 키 없이 1회씩)

| 열림 (200 + 상품 목록 흔적) | 막힘 |
|---|---|
| **아마존 JP·CA·SG·IN·MX·NL·PL·SE·AE** (기존 트래커에 마켓만 추가하면 됨) | 아마존 AU (캡차) |
| **Qoo10 JP** 검색 432KB · **올리브영 글로벌** 베스트 308KB · **Lazada SG/TH** ajax JSON · **Ulta US** · **화해** 랭킹 | Shopee(API 403) · Coupang 403 · Ozon 403 · Yandex Market(스마트캡차) · Douglas 403 · Stylevana 403 |
| 확인 필요: iHerb(2.5MB, 캡차 문구 포함) · Tokopedia · YesStyle · Watsons TW/SG(JS 껍데기 8KB → API 별도 탐침) | 중국: JD(로그인 벽) · Tmall(로그인 벽) — **무료로는 못 한다** |
| Yahoo JP 쇼핑 = 공식 API(앱ID 등록 필요, 무료) | Kaspi 403(러너에선 KTG 수집기가 열었음 — 헤더 차이) |

⚠ 러너(GitHub Actions) IP 에서는 결과가 다를 수 있다 — 아마존은 PC 고정, 나머지는 러너에서 다시 탐침한다(`diag_probe.py` 방식).

---

## 2. 단계별 계획

### Phase 0 — 응급조치 (2026-09-24, 이 세션)

- [x] 로컬 클론을 원격과 동기화 (1-1)
- [x] `events.yml`: 타임아웃 30→55분 · 필수 스텝 앞으로/무거운 선택 스텝 뒤로 · **모든 트리거에 '오늘 이미 돌았으면 종료'** 게이트(수동 `force` 예외)
- [x] 아마존 트래커: 실행 중 **절전 금지**(`SetThreadExecutionState`) · 시작 시 **네트워크 대기** · stdout 무버퍼 · `--skip-if-done`
- [x] 아마존 **2차 실행 작업**(06:45, 오늘치 없을 때만) 등록
- [ ] 오늘(9/24) 결측분 수동 수집 (진행 중)
- [x] 기록: 이 문서 · CLAUDE.md · 메모리

### Phase 1 — 데이터 신뢰성과 신선도 (1주)

- [x] **신선도 보드**: 시세 스트립 아래 `#freshStrip` — 정상 N/25 · 최근 갱신 · 지연 블록 칩 · 전체 보기(블록별 시각·경과·한도). 한도표 `FRESH_LIMITS`(app.js) = watchdog `LIMITS` 와 같은 값이어야 한다
- [ ] 아침 파이프라인 실측 재검: 55분 안에 끝나는지 · 레터 도착 시각 · 07:30 기준 각 블록의 데이터 끝 날짜 표(D-1/D-2)
- [ ] 아마존 3일 연속 정상 확인 후 `watchdog` 한도(72h) 재검토
- [ ] 아마존 페이지 변형(1-4 ③) 대응: 껍데기 응답이면 30초 뒤 다른 지문(chrome124 등)으로 1회 즉시 재시도

### Phase 2 — 트렌드: 노이즈 → 시그널 (1~2주)

- [x] **일별 계열 기본 표시를 '7일 이동평균 선 + 원값 점(흐리게)'** 으로. 순위·지수·실측 공통
- [x] 표의 '전일비' → 일별 계열은 **'최근 7일 평균 vs 직전 7일 평균'**(주간 변화) 으로 교체. 전일비는 툴팁으로 강등
- [x] '주목할 추이' 점수식을 스무딩 값 기준으로 + **기준선 최소 크기** 조건 강화(저점발 급등 배제)
- [x] 디시 글수·채용 공고 = **주간 뷰(완결 주 합계/주평균 · 3주 이상 창에서 자동)** 를 기본으로, 단기(≤20일)만 일별 7일 평균 — 막대 대신 선(스택 드로어 재사용은 Phase 4 에서 판단)
- [x] 종목 버튼 35개 → **섹터(7) → 종목** 2단 선택(`renderTrendStockPicker`). 섹터를 누르면 첫 종목이 바로 잡힌다 · [ ] 접힘 블록 3개(화제·올리브영·채용)는 트렌드 탭 하단으로(index.html 편집 필요 → 트래커 커밋 뒤)
- [ ] 종목당 **시그널 카드**(주제별 스파크라인 + 주간 변화 + 고점比)를 차트 위에 두어 한눈에 훑고 클릭으로 확대

### Phase 3 — 채널 다각화 (2주)

우선순위는 '커버리지 종목 매출과 직결되는 시장' 순: **일본 > 동남아 > 미국 오프라인 > 유럽 확장 > 중동·인도·중남미**.

- [x] 아마존 **JP** 추가 (JPY · 순위 표기 `位` · 판매량 `万点以上` · 별점 `のうち` · 카타카나 별칭 `BRAND_ALIASES`) — 오프라인 파서 검증 통과, 첫 실수집은 9/25 05:15 → 실측 소요 확인 후 **SG · CA · NL · PL · SE · AE** 순차 추가 (회당 시간 예산 안에서)
- [ ] **Qoo10 JP** 수집기 신설(브랜드 검색 → 상품 수·리뷰·판매 카운트) — 주 1~2회
- [ ] **올리브영 글로벌** 베스트 수집기 — 하루 1회(브랜드별 진입 SKU·순위)
- [ ] **Lazada** SG·TH·MY·ID·PH·VN — `?ajax=true` JSON 검증 후 수집기(리뷰·판매수) — 주 1회
- [ ] Ulta · 화해 · Watsons API · iHerb 재탐침 후 채택 여부
- [ ] Yahoo JP 쇼핑 API — 사용자 앱ID 등록 후 랭킹 수집
- [ ] 러시아: Wildberries 유지(주 1회→주 2회), Ozon/Yandex Market 은 막힘 → 얀덱스 검색·Kaspi 로 보완
- [ ] 중국: 마켓 직접 수집 불가 → **관세청 대중국 수출 + 홍콩·대만 채널(Watsons)** 을 중국 프록시로 명시
- [ ] 화면: **'채널' 탭** 신설 — 아마존·현지몰·올리브영(국내/글로벌)을 **브랜드 × 국가 매트릭스** 하나로. 모든 값은 '추이'(각 채널 자기 기준=100) + 절대값 툴팁

### Phase 4 — 정보구조·디자인 개편 (2~3주) — ⚠ 구조안은 사용자 확인 후 착수

현재 탭 11개: 개요 · 커버리지 요약 · 밸류에이션 · 실적 프리뷰 · 이벤트 캘린더 · 뉴스 · 리서치 리포트 · 트렌드 비교 · 탑툰챗 · 수출 데이터 · 아마존

**권장안 (6개)**
| 새 탭 | 합치는 것 | 첫 화면 |
|---|---|---|
| **아침** | 개요 + 오늘의 시그널(스무딩 기준 상위) + 오늘·이번 주 일정 + 어젯밤 뉴스 헤드라인 | 07:30 에 열어 3분에 훑는 화면 |
| **종목** | 커버리지 요약 + 밸류에이션 + 실적 프리뷰 (종목 클릭 → 서랍) | 표 하나, 열 토글 |
| **시그널** | 트렌드 비교 (Phase 2 결과) | 종목 시그널 카드 → 차트 |
| **채널** | 아마존 + 해외몰 + 올리브영 (Phase 3) | 브랜드 × 국가 |
| **딥다이브** | 탑툰챗 · 컴투스 모델 · 게임 매출 추정 · 수출 데이터 | 종목별 모델 |
| **일정·뉴스** | 이벤트 캘린더 + 뉴스 + 리서치 리포트 | 주간 캘린더 + 피드 |

디자인 원칙(변경 없음): Rosé Pine 토큰 유지 · 상승=빨강/하락=파랑 · 이모지 아이콘 → SVG 아이콘 세트로 교체 · 표는 숫자 우측정렬·고정폭 숫자(`font-variant-numeric: tabular-nums`) · 본문 14.5px 유지 · 카드 안 정보 3단계(제목/값/보조) 이상 금지.

- [ ] 사용자 확인: 6탭 안 채택 여부 / '아침' 탭을 기본 탭으로 할지
- [ ] 디자인 토큰 정리(간격 4px 그리드 · 타이포 스케일 4단) · 공통 컴포넌트(카드·표·칩·세그) CSS 재작성
- [ ] 탭별 순차 이관 (아침 → 시그널 → 채널 → 종목 → 딥다이브 → 일정·뉴스), 이관마다 `check_render.py` 통과

### Phase 5 — 구조 정리 (Phase 4 와 병행 가능)

- [ ] 수집기가 `public/data/*.json` 에도 쓰게(현재 `export_blocks.py` 수동) → 화면이 JSON 을 fetch. `index.html` 1.8MB → 수십 KB
- [ ] `app.js`(431KB·6,261줄) 를 탭별 파일로 분할 (보통 스크립트 여러 개, 빌드 없음)
- [ ] 자동 점검 확장: 신선도 표 자동 생성 · 아침 파이프라인 소요 리포트

---

## 3. 사용자가 정해 줘야 하는 것

| # | 질문 | 권장 | 급한가 |
|---|---|---|---|
| 1 | 아마존을 **PC 에 두고 안정화**(무료) vs **클라우드 + 가정용 프록시**(월 $5~10, PC 꺼도 됨) | 우선 PC 안정화 → 2주 관찰 후 결정 | 아니오 |
| 2 | Phase 4 **6탭 구조안** 채택? '아침' 탭을 첫 화면으로? | 채택 | Phase 4 전까지 |
| 3 | **Yahoo JP 쇼핑 API** 앱ID 등록(무료, 야후재팬 계정 필요) | 하면 일본 채널 하나 더 | 아니오 |
| 4 | Cloudflare Cron Trigger 변경 | **지금은 불필요**(05:00 수집 → 55분 안 종료 → 레터 ~05:45) | 아니오 |

---

## 4. 진행 로그

- **2026-09-24** 진단 완료(1절). 로컬 동기화. Phase 0: events.yml 55분·전 트리거 게이트·스텝 순서 / 아마존 절전 방지·네트워크 대기·`-u`·`--skip-if-done` / PC 작업 `AmazonBeautyTrackerRetry`(06:45) 등록. 9/24 결측분 수동 수집 실행.
- **2026-09-24 (2)** Phase 2 1차: 일별 계열 7일 이동평균 본선·원값 흐리게(토글) · 표를 '7일 평균·주간 변화·4주 변화'로 · 주목할 추이 판정을 평균+기준선 25% 규칙으로 · 디시 글수/채용은 주간 뷰(`weeklyAlt`·`preferWeek`) · `check_render.py` 탭 목록 정정(boxoffice→toptoon). Phase 3: 아마존 JP 마켓 코드·설정 추가(첫 수집 9/25).
