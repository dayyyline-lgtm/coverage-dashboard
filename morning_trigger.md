# 아침 6시 도착 — 트리거 구조

`morning_trigger.bat` 과 이 PC 의 작업 스케줄러가 하는 일. (2026-08-06 정리)

## 왜 PC 가 트리거를 하게 됐나

원래 정시성을 담당한 건 **Cloudflare Worker** 였다. 그게 유일하게 안 밀리는
트리거였는데(07:00·08:00·09:00 정각 발화) **2026-08-05 13:00 을 마지막으로
죽었다.** 그 뒤 33시간 동안 자동 발화가 한 번도 없었다.

GitHub 자체 cron 은 이 계정에서 못 쓴다. 실측 지연:

| 워크플로 | 예약 | 실제 | 지연 |
|---|---|---|---|
| 아침레터 | KST 05:40 | 08:42 · 09:14 · 09:15 | **+3.0~3.6h** |
| 영화 | KST 10:00 | 17:58~21:44 | +8~12h |
| 수출입 | KST 07:00 | 16:58 | +10h |

아침 6시 도착에는 쓸 수 없다. 그래서 **확실히 통제되는 곳**을 하나 더 뒀다.

## 지금 구조 — 3중

| 층 | 무엇이 | 언제 | 비고 |
|---|---|---|---|
| 1 | `AmazonBeautyTracker` (PC) | 05:15 | 아마존 수집 → 텔레그램 |
| 1 | `CoverageMorningTrigger` (PC) | 05:35 | `events.yml` + 뉴스봇 `daily-briefing.yml` |
| 2 | Cloudflare Worker | 05:45 | 살아나면 같은 것을 또 부른다 |
| 3 | GitHub cron | 05:40 예약(실제 08~09시) | 최후 백업 |

**중복은 걱정 없다.** 2·3층이 겹쳐 떠도

- 레터는 `digest_sent.json` 이 막고,
- 수집은 `events.yml` 의 `gate` 잡이 막는다 — 2026-08-06 08:42 회차에서
  실측 확인: `gate: success`(5초) → `events: skipped`. 예전이면 15분짜리
  전체 수집을 또 돌렸을 회차다.

## 시각을 왜 이렇게 잡았나

도착이 아니라 **시작** 시각이다. 실측 소요:

| | 소요 | 05:35 출발 시 도착 |
|---|---|---|
| 아마존 (05:15 출발) | ~12분 | **05:27** |
| 데일리 레터 | 12분 | **05:47** |
| 뉴스봇 브리핑 | 14~17분 | **05:49~05:52** |

## PC 가 잠들어 있어도 된다

두 작업 모두 **절전 해제하고 실행**(`WakeToRun`)과 **놓치면 따라잡기**
(`StartWhenAvailable`)를 켜 뒀다. 전원 옵션의 절전 해제 타이머도 켜져 있는 것을
확인했다(AC/DC 모두 1).

2026-08-06 아침에 아마존이 안 온 게 이것 때문이었다 — PC 는 꺼진 게 아니라
자고 있었는데, 작업에 깨우기가 꺼져 있어 05:15 을 그냥 넘기고 다음 실행이
곧장 8/07 로 밀렸다. (완전히 종료된 상태면 이것도 소용없다.)

## .bat 은 반드시 ASCII 로만 쓸 것

`cmd.exe` 가 이 PC 에서 **cp949** 로 .bat 을 읽는다. UTF-8 한글이 들어가면
그 줄이 통째로 깨지는데 — **종료코드는 0 으로 나온다.** 조용히 아무 일도
안 하고 성공한 척한다. 2026-08-06 에 실제로 그렇게 한 번 헛돌았다.

한글 설명은 이 파일에 쓰고, `.bat` 에는 영문만 남긴다.

같은 함정을 파이썬 쪽에서도 밟았다 — 스케줄러가 stdout 을 파일로 넘기면
파이썬도 cp949 를 쓴다. 줄표(—)가 든 `print` 한 줄에 아마존 수집 전체가
죽었다(2026-08-05). 지금은 `main.py` 와 `run.bat` 양쪽에서 UTF-8 로 고정한다.

## 워커를 고치면

이 PC 작업을 지울 필요는 없다. 겹쳐도 위 게이트들이 막고, PC 가 꺼진 날엔
워커가, 워커가 죽은 날엔 PC 가 받는다. **단일 장애점을 다시 만들지 말 것.**
